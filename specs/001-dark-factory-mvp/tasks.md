---

description: "Task list for Dark Factory MVP implementation"
---

# Tasks: Dark Factory MVP

**Feature**: `001-dark-factory-mvp` | **Spec**: [`spec.md`](./spec.md) | **Plan**: [`plan.md`](./plan.md)

**Input**: Design documents from `/specs/001-dark-factory-mvp/`

**Prerequisites**: `plan.md` (tech stack, structure), `spec.md` (user stories P1–P6), `research.md`, `data-model.md`, `contracts/`, `quickstart.md`, `.specify/memory/constitution.md` v3.0.0

**Тесты**: Отдельные тест-задачи не генерируются — спецификация не запрашивает TDD. Тесты входят в DoD каждой задачи (`ruff` + `mypy --strict` + `pytest`, см. `docs/plan.md` §6) и исполняются как часть реализации (unit, contract, integration, crash-тесты).

**Организация**: Задачи сгруппированы по user stories из `spec.md` для независимой реализации и проверки. Якорь `[docs T-0NN]` в описании — канонический реестр задачи в `docs/plan.md`; ID здесь (`T0NN`) — порядок исполнения в этом файле и не совпадает с `T-0NN` из реестра.

**Traceability**: `T0NN` (этот файл) ↔ `T-0NN` (`docs/plan.md`). Ни одна задача реестра P0–P3 не выпадает.

## Формат: `[ID] [P?] [Story] Описание`

- **[P]**: можно выполнять параллельно с другими задачами той же волны (разные файлы); перечисленные в задаче зависимости всё равно должны быть выполнены до её старта
- **[Story]**: `[US1]`…`[US6]` — принадлежность к user story `spec.md`
- Каждая задача содержит конкретный путь к файлу и ссылку на контракт/ADR

## Path Conventions

- Модульный монолит (src-layout, ADR-002): код — `src/dark_factory/<модуль>/`, тесты — `tests/{unit,contract,integration}/`.
- Миграции — `migrations/`; Helm — `charts/dark-factory/`; CI — `.github/workflows/`; деплой — `deploy/`; пакеты — `packs/`.
- Документы фичи — `specs/001-dark-factory-mvp/`.

**Состояние на старт**: `T001`–`T003` (`docs T-001`…`T-003`) уже выполнены (✅ 2026-09-13), остальные — в работе.

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Решения, каркас репозитория и доменные контракты. Выполнено до этой итерации.

- [x] T001 [docs T-001] Принять и зафиксировать ADR-пакет по открытым архитектурным вопросам (ADR-002…ADR-019), включая стек, PostgreSQL state store, merge policy, трекер, UI pack, границы репозиториев — `docs/adr/`
- [x] T002 [docs T-002] Scaffolding репозитория и базовый CI: пакет `dark_factory` (src-layout, 8 модулей HLD), `pyproject.toml` (uv, ruff, mypy strict, pytest), тест границ импортов (ADR-015 §3), pipeline lint+typecheck+test — `pyproject.toml`, `src/dark_factory/`, `tests/`, `.github/workflows/ci.yml`
- [x] T003 [docs T-003] Доменная модель изменения: `Change`, `ChangeRun`, `StageRun`, immutable `StageResult`, `NextAction` (закрытое объединение из 8 вариантов), `ArtifactRef`, `Evidence`, `ChangeRequestRef`/`RepositoryRef`, `Decision`, `Finding`, `GateResult`, `Usage`, `BudgetSnapshot`, `RunManifest`/`RunRecord` + `schema_version`; таблицы переходов run/stage (`apply_status`), инварианты завершения (ADR-009 §9), key-функции ADR-006 §3, JSON/YAML round-trip — `src/dark_factory/changes/`

**Checkpoint**: Каркас и контракты готовы — можно начинать Foundational.

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Общее ядро, блокирующее все user stories: схема состояния, детерминированный Flow, порты и фейки.

**⚠️ CRITICAL**: Ни одна user story не начинается до завершения этой фазы.

- [x] T004 [docs T-006] Схема состояния PostgreSQL и Alembic-миграции: таблицы `execution`, `stage`, `attempt` со `state_revision`; `execution_leases` (`resource_type`, `resource_id`, `owner_id`, `fencing_token` монотонный, `acquired_at`, `expires_at`, `heartbeat_at`); `outbox` (envelope: `eventId`, `eventType`, `eventVersion`, `occurredAt`, `changeId`, `runId`, `stage`, `aggregateId`, `aggregateVersion`, `correlationId`, `causationId`, `artifactRefs`, `payload`) и `event_delivery` (`event_id`, `consumer_id`, `status`, `attempts`, `next_attempt_at`, `last_error`); durable effect ledger (`effect_key` уникален, статусы `planned/in_progress/succeeded/unknown`, `external_ref`); `usage`-агрегаты. Инварианты покрыть тестами: уникальность `operation_key`/`effect_key`, уникальность пары (`event_id`, `consumer_id`), монотонность `fencing_token`, атомарность «изменение состояния + запись в outbox»; crash-тесты `crash-before-call` и `crash-after-call-before-commit` не создают второй внешний эффект (ADR-004, ADR-006 §3, ADR-016) — `migrations/`, `src/dark_factory/orchestration/state/`, `tests/integration/`
- [x] T005 [docs T-004] Factory Flow: стадии (`specification → planning → construction → review_verification → release`), 7 гейтов MVP (`specification`, `planning`, `code`, `ui`, `review`, `verification`, `release`), маршруты `quick`/`standard`, TaskGraph (pydantic-graph) только внутри стадии, меж-job продолжение через `StageResult`/`NextAction`, лимиты rework/бюджета на уровне Flow, стоп-условия → `Blocked`. DoD: таблица переходов покрыта целиком; обработчики всех 8 вариантов `NextAction` проходят exhaustiveness (`assert_never`); переход вне таблицы невозможен в рантайме; мёртвых рёбер нет (ADR-005) — `src/dark_factory/orchestration/`, `src/dark_factory/flows/`, `src/dark_factory/rules/`
- [x] T006 [docs T-005] Порты (`Protocol`) и in-memory фейки: `RepositoryPort`/`MergeRequestPort`/`PipelinePort` (единый `ChangeRequestRef`, ADR-019), `TrackerPort`, `HarnessPort`, `ArtifactStorePort`, `TelemetryPort`, `WorkflowEnginePort` (`start`/`resume`/`cancel`/`get_status` с `idempotency_key` и `expected_revision`), `ReconciliationService`, `EventPublisherPort`. Все mutating-методы принимают `idempotency_key`/`effect_key` и возвращают `external_ref`. DoD: contract-тесты каждого порта на фейках; ни один модуль ядра не импортирует внешний SDK (ADR-015 §3); `KnowledgePort`/`ExecutionPort` (T015), `CIPort` (T024), `SDDPort` (T016) вводятся в своих задачах — не авансом — `src/dark_factory/ports/`, `src/dark_factory/adapters/`, `tests/contract/`, `tests/test_import_boundaries.py`

**Checkpoint**: Схема, Flow и порты готовы — user stories можно начинать параллельно.

---

## Phase 3: User Story 1 - Исполнение одной стадии фабрики (walking skeleton) (Priority: P1) 🎯 MVP

**Goal**: Оператор запускает одну стадию (без LLM) локально через CLI и в CI, получает типизированный `StageResult` и запись запуска; процесс не требует постоянно живущего сервиса.

**Independent Test**: `uv run factory doctor --json` и `uv run factory stage run --change ./fixtures/chg_smoke.yaml --stage construction --json` (quickstart §2) дают валидный `StageResult` (`schema_version: 1`); повтор с той же `operation_key` не создаёт второй внешний эффект; отключённый LLM-эндпоинт не мешает детерминированным шагам.

### Реализация для User Story 1 (слой `cli/` поверх Foundational)

- [x] T007 [P] [US1] Factory Runner CLI: точка входа и разбор команд `stage run`, `stage resume`, `run status`, `reconcile`, `outbox dispatch`, `doctor` по контракту `contracts/cli.md` (опции `--change`, `--stage`, `--route`, `--input-revision`, `--run-id`, `--json`, `--evidence-dir`, `--non-interactive`) — `src/dark_factory/cli/__main__.py`, `src/dark_factory/cli/main.py`
- [x] T008 [US1] `factory doctor`: проверка окружения и конфигурации, отсутствие секретов в выводе, exit-код 2 при невалидной конфигурации — `src/dark_factory/cli/doctor.py`
- [x] T009 [US1] `factory stage run`: фиксация снапшота входа до агентной работы (FR-001), вычисление `input_revision` → `operation_key` (ADR-006 §3), исполнение стадии, сериализация immutable `StageResult` (`schema_version: Literal[1]`, `status` только `waiting|succeeded|failed|blocked`), маппинг exit-кодов 0/10/20/1/2 — `src/dark_factory/cli/stage.py`
- [x] T010 [US1] Детерминированные шаги стадии без LLM: сборка контекста, машинные проверки, агрегация, решение о доработке (детерминированный путь не обращается к harness) — `src/dark_factory/orchestration/stages/`
- [x] T011 [US1] Персистенция записи запуска: `RunRecord` (`schema_version` + `manifest` + `change` + `run` + `stage_results` + `decisions`) в `--evidence-dir`/артефакт; immutable refs, без `latest` (ADR-015 §5) — `src/dark_factory/cli/run_records.py`
- [x] T012 [US1] Идемпотентность запуска: повтор `stage run` с той же `operation_key` не создаёт второй внешний эффект; `find_existing` перед созданием, сверка эффекта перед повтором (FR-017, ADR-006 §3) — `src/dark_factory/orchestration/idempotency.py`, `tests/integration/`
- [x] T013 [US1] Fixture и паритет локально/CI: `fixtures/chg_smoke.yaml`; job, запускающий тот же релиз ядра тем же CLI (US1 scenario 2), `--non-interactive` в CI; `doctor` + `stage run` в pipeline — `fixtures/chg_smoke.yaml`, `.github/workflows/ci.yml`

**Checkpoint**: US1 полностью функциональна и проверяется независимо (quickstart §2).

---

## Phase 4: User Story 2 - От задачи к согласованной спецификации (Priority: P2)

**Goal**: Задача нормализуется в снапшот, Product публикует MR с proposal'ом изменения; реализация разрешена только для согласованной ревизии спецификации.

**Independent Test**: Подать задачу → получить MR с ChangeSet'ом (`.factory/changes/<year>/<id>/`) → согласовать конкретную ревизию (SHA) → убедиться, что `construction` без approval не стартует, а смена ревизии инвалидирует approval.

- [x] T014 [US2] [docs T-011] Профили ядра Product/Develop/Quality и минимальные skills (`intake`, `requirements-refinement`, `spec-авторинг`, `implementation`, `implementation-rework`, `code-review`, `acceptance-verification`, `change-request`); версионированные манифесты (входы/выходы, tools, ограничения, stop-conditions) + контракт `AgentProfile → TaskEnvelope → AgentResult` (ADR-007) — `src/dark_factory/agents/profiles/`, `src/dark_factory/agents/skills/`. Зависит от T015 (docs T-012) и T019 (docs T-010 — harness, US3, interleaved: T019 до T014).
- [x] T015 [P] [US2] [docs T-012] `ContextBundle`: сбор источников (`repo`, `specs/` до T-020 → `.factory/` после, конституция, ADR, engineering pack) с provenance, revision/hash; версионирование набора на запуск; изолированный worktree. Ввести `KnowledgePort` и `ExecutionPort` (contract-тесты на фейках); bundle воспроизводим — одинаковый hash на одинаковых входах — `src/dark_factory/context/`, `src/dark_factory/ports/`
- [x] T016 [US2] [docs T-020] Контрольная точка Native SDD Core (ADR-020, `docs/sdd-native-core.md`): Product Baseline `.factory/` (`product/` + `changes/`), ChangeSet с манифестом `change.yaml` (workflow profile, risk_class) и дельтой `spec/delta.yaml` (`add/modify/supersede/retire`), reconciliation к baseline; `SDDPort` с `NativeChangeSetAdapter` (основной), `SpecKitAdapter` (bootstrap-импорт legacy `specs/`) и `OpenSpecAdapter` (compatibility, ADR-020 п.8); строгость по типу задачи (fix — без SDD-артефактов); `.specify/` и `specs/` — read-only bootstrap evidence. Зависит от T005, T014 — `src/dark_factory/context/sdd/`, `.factory/` (реализовано: модуль `context/sdd/` — Pydantic v2 модели артефактов change/v1, delta, task-graph/v1, requirement/v1 frontmatter, verification/evidence/reconciliation с YAML round-trip; таблица переходов lifecycle draft→…→closed с единой точкой валидации; политика строгости по profile × risk_class (`bugfix-r0` — пустой набор, R2+ — обязательный reconciliation); `SDDPort` в `ports/protocols.py` по контракту ports.md с реэкспортом; `NativeChangeSetAdapter` (create/read/apply_delta, guard ожидаемого baseline revision — детекция параллельных изменений), `SpecKitAdapter` (read-only импорт legacy `specs/` с делегацией нативному адаптеру), `OpenSpecAdapter` (конвертер ADDED/MODIFIED/REMOVED ↔ add/modify/retire); начальный Product Baseline `.factory/` (factory.yaml + product/, revision — детерминированный sha256 content-hash); нормализованный ChangeSet-контракт с 5 осями как данные (гейт — T021); unit + контрактные тесты SDDPort на трёх адаптерах. ruff + ruff format + mypy strict + pytest — зелёные (586 passed + 13 skipped без БД))
- [x] T017 [US2] [docs T-021] Specification gate: детерминированные проверки (наличие AC, трассировка AC → сценарий → задача, противоречия scope/out-of-scope, связность с конституцией), blocking/non-blocking; результат фиксируется как `GateDecision` (policy и версия, revision ChangeSet, результат, evidence, объяснение, агент/человек, разрешённый override — ADR-020); поле `risk_class` из Implementation Contract (T023) в нормализованном контракте ChangeSet. Зависит от T016 (T-020), T023 (T-016, US3 — interleaved) — `src/dark_factory/quality/gates/` (реализовано: `evaluate_specification_gate` поверх `normalize()` — находки 5 осей как данные, гейт взвешивает; обязательные артефакты и R2+-reconciliation пересчитываются по эффективному риск-классу из Implementation Contract (повышение — информационная находка, понижение без политики — блокирующая); AC-наличие и связка AC→сценарий — каноническая проверка `acceptance-scenario` в verification plan, трассировка →задача — coverage-ось (`requirement_without_task`); противоречия scope in/out без учёта регистра; классификация blocking/non-blocking — данные `SpecGatePolicy` (12 blocking-кодов; `risk_class_raised` и `missing_evidence_for_accepted_change` — non-blocking); `GateDecision` (`dark-factory.dev/gate-decision/v1`: policy+версия, changeset_id+revision, эффективный risk_class, результат passed/failed, находки с blocking-меткой, evidence-ссылки, объяснение, `decided_by=policy`, human-only override через `apply_override` — только для failed); persist в `gates/specification.yaml`; качество не зависит от orchestration (порядок риск-классов продублирован локально, граница импортов важнее DRY); 16 тестов. ruff + format + mypy strict + pytest — зелёные (602 passed + 13 skipped без БД), CI на MR зелёный)
- [x] T018 [US2] [docs T-022] Product Baseline/шаблоны ChangeSet для продуктовых репозиториев (`.factory/`: `factory.yaml`, `product/`, `changes/`; frontmatter-профили артефактов): правила минимальных изменений и evidence-based validation; проверка через `NativeChangeSetAdapter` (ADR-020 п.8). Зависит от T016 (T-020), T041 (T-070) — `packs/` (реализовано: пак `packs/product-baseline/` — `baseline/` (factory.yaml + product/ с примерами нод requirement/scenario/adr/capability в минимальном frontmatter §7-8, все active) и `changeset/` (канонический скелет ChangeSet R1/product-feature/draft: 6 обязательных слотов без reconciliation, delta add с AC в body, task-graph с трассировкой satisfies, verification plan с unit-test + acceptance-scenario, пустой evidence-индекс); placeholder-ревизия (строка из 64 нулей) в change.yaml и spec/delta.yaml заменяется на актуальную `current_revision()` при применении; `rules.md` — правила минимальных изменений (принцип II конституции: дельта add/modify/supersede/retire вместо копии спецификации, baseline только active/superseded/retired, прямые правки baseline мимо ChangeSet запрещены) и evidence-based validation (принцип III: фактические проверки, evidence в evidence/index.yaml, наличие артефакта ≠ пройденный гейт — GateDecision в gates/specification.yaml); README с порядком применения и правилом «пустые каталоги не создаются»; проверка через `NativeChangeSetAdapter` — `tests/test_packs_product_baseline.py`: baseline-шаблон валиден (FactoryManifest + frontmatter всех нод), changeset-шаблон соответствует строгим схемам (CHANGE_ID_PATTERN, ровно 6 слотов, консистентность delta↔manifest), e2e: копия baseline → read_change → спецификационный гейт PASSED → read_requirements → apply_delta (новая ревизия, REQ в product/ со status active, reconciliation/result.yaml) → повторный apply_delta кидает BaselineMismatchError; применение пака к пилотному репо — за T041 (T-070). ruff + format + mypy strict + pytest — зелёные (605 passed + 13 skipped без БД), CI на MR повторяет проверки)

**Checkpoint**: US2 проверяется независимо — реализация блокируется без version-bound approval.

---

## Phase 5: User Story 3 - Агентная реализация с независимым review и ограниченным rework (Priority: P3)

**Goal**: На согласованной спецификации — реализация в изолированном workspace, независимый review отдельным контекстом, ограниченный rework в том же MR, машинные проверки на итоговом SHA.

**Independent Test**: На согласованной спецификации одного небольшого изменения пройти цикл реализация → review → rework → зелёный CI; проверить лимит раундов (≤3) и параллелизм ≤2.

- [x] T019 [P] [US3] [docs T-010] `HarnessPort` + PydanticAI-адаптер: model/tools/structured output, учёт usage (токены, стоимость), ограничения инструментов по профилю, LLM endpoint через существующий LiteLLM (ADR-002 §2, R-17) — `src/dark_factory/adapters/harness/`
- [x] T020 [P] [US3] [docs T-013] Quality — независимая приёмка: контекст строится заново из закреплённой спецификации, актуального diff и evidence (не наследует Develop); `Finding` (severity/category/evidence/confidence); `GateResult` pass/fail/blocked для конкретного SHA; blocking/non-blocking; блокирующий finding останавливает merge; человеческие замечания MR включаются в общий набор `Finding` как данные, без влияния на права и гейты (FR-007) — `src/dark_factory/quality/` (реализовано: модуль `quality/acceptance.py` — `build_acceptance_context` собирает свежий `ContextBundle` из `SpecMaterial`/`DiffMaterial`/`EvidenceMaterial` с sha256-provenance (дубликаты evidence — ValueError; `SourceKind.EVIDENCE` добавлен в context/bundle, hash воспроизводим, `retrieved_at` не входит); `human_comment_findings` нормализует комментарии MR в `Finding(origin=human, severity=info, status=open)` — текст не интерпретируется, `required_action` пуст, полный текст/автор — в evidence-артефакте комментария; `classify_findings` → blocking (open + severity в политике, default BLOCKER) / non_blocking / stale (чужой `reviewed_sha` — новый SHA аннулирует прежний review); `evaluate_review_gate` → `GateResult(REVIEW, passed|failed, sha, summary, evidence_ids)`; поле `confidence` (0.0–1.0, optional) в `Finding` (changes/findings.py) — документирует уверенность источника, вес не меняет; блокирующий finding останавливает merge через существующую политику flow (failed review-гейт в `MergeAction` → Blocked, доказано интеграционным тестом `test_blocking_finding_stops_merge`; контрольные тесты: merge проходит при passed-гейте и при только-human findings). ruff + format + mypy strict + pytest — зелёные (622 passed + 13 skipped без БД))
- [x] T021 [US3] [docs T-014] Bounded rework loop: `review → rework → re-review` с лимитом 3; новый attempt не перезаписывает прежнее состояние; новый SHA аннулирует прежний допуск; исчерпание лимита/повторяющаяся ошибка/конфликт требований → `Blocked` с диагностикой (условия эскалации — подмножество ADR-018 §5). Зависит от T005, T020 — `src/dark_factory/orchestration/` (реализовано: модуль `orchestration/rework.py` — чистая детерминированная loop-политика уровня над переходами flow (flow.py, rules/, changes/, quality/ не изменены; зависимости — только `changes.*`, `rules.limits`, `orchestration.policy.escalation`); `FindingSignature` — frozen-hashable идентичность блокирующего finding (origin, category, file, line) через `finding_signature()`: id/role/reviewed_sha не входят — id перегенерируется каждым проходом review, SHA меняется каждый раунд; `ReviewPass` — frozen-запись завершённого прохода review (round — номер раунда цикла: начальный review = 1, re-review после rework-раунда k = k+1; sha; blocking — кортеж сигнатур; escalations) в append-only истории: новый attempt добавляет запись, прежние неизменяемы (FR-008); `plan_rework(budget, passes)` → `ReworkDecision(outcome=rework|blocked, round, max_rounds, reason, signatures)` — чистая функция (budget/passes не мутируются; пустая история и последний проход без блокирующих — ValueError); детерминированный порядок stop-условий, первое срабатывание → Blocked (подмножество ADR-018 §5): эскалации последнего прохода (`escalation_stop_reason` — эскалация не расходует раунд, конфликт требований REQUIREMENTS_DEFICIENT останавливается здесь) → лимит rework (`rework_violation` с requested_round = used+1 — счётчик бюджета авторитетен, FR-016) → тот же SHA у двух последних проходов («rework не произвёл новый commit — прежний допуск на SHA остаётся, re-review невозможен»; механика «новый SHA аннулирует прежний допуск»: проход без нового SHA ничего не отменяет) → повторяющаяся ошибка (множество блокирующих сигнатур не изменилось после раунда) → иначе rework round = used+1, reason перечисляет блокирующие находки (детерминированная сортировка (origin, category, file, line)); решения о прошлых проходах не переиспользуются как допуск — вердикт прохода действителен только для своего SHA, повтор — stop-условие, а не допуск (ADR-018 §5: исправимые findings возвращаются в цикл, повтор/исчерпание — эскалация Blocked); классификация blocking остаётся у вызывающего (T020, quality не импортируется — направление зависимости сохранено); wiring в stages/executor — за следующими задачами; 17 тестов в `tests/test_orchestration_rework.py`. ruff + format + mypy strict + pytest — зелёные (639 passed + 13 skipped без БД))
- [x] T022 [US3] [docs T-015] TaskGraph внутри стадии: branch/join для ≤2 независимых подзадач, агрегация через reducers, конкурентная запись в state через типизированные outputs. Зависит от T005 — `src/dark_factory/orchestration/` (реализовано: модуль `orchestration/taskgraph.py` — типизированный фасад над pydantic-graph 2.43 (GraphBuilder fork/join, ADR-005): `TaskGraph[InputT, OutputT, AggregateT](name, reducer, initial_factory)` + `add_branch(Branch(name, call))` + `await run(inputs) -> AggregateT`; ≤2 веток — третья отклоняется на этапе конструирования (`TooManyBranchesError`, FR-005), дубликат имени/0 веток — ValueError; ветка — async-подзадача, получает только вход (иммутабельный по контракту) и возвращает frozen-выход — API не имеет разделяемого state, мутационное разделение невыразимо, агрегат персистит вызывающий ровно один раз; детерминизм: выход помечается индексом объявления, join буферизует в порядке завершения, финальный шаг проверяет полноту набора индексов (инвариант «join не теряет результаты» → RuntimeError) и сворачивает reducer в порядке объявления — порядок агрегата от порядка завершения не зависит; политика отказов — fail-fast: первая упавшая ветка прерывает run исходным исключением (не ExceptionGroup), соседняя отменяется, агрегат не создаётся; `initial_factory` — свежий агрегат на каждый run. pydantic-graph объявлен явной зависимостью (`pydantic-graph>=2.43,<3`, резолюция не изменилась — pydantic-ai-slim 2.43.0 pins ==2.43.0). Ограничение: mypy 2.3.1 не резолвит TypeVar'ы pydantic-graph (typing_extensions/infer_variance на py3.12) — классы библиотеки используются без параметризации с точечными type: ignore/cast, контракт типов несут сигнатуры фасада (кандидат в tech-debt: обновление typing_extensions вернёт проверяемость дженериков pydantic-graph). Wiring в stages/executor — за следующими задачами; 16 тестов в `tests/test_orchestration_taskgraph.py`. ruff + format + mypy strict + pytest — зелёные (655 passed + 13 skipped без БД))
- [x] T023 [US3] [docs T-016] Implementation Contract и границы автономной реализации (ADR-018): (1) схема контракта (scope, acceptance criteria, архитектурные ограничения, UI evidence, риск-класс R0–R4, бюджет, правила эскалации); (2) классификатор Known/Bounded choice/New path; (3) машинно проверяемые условия эскалации (публичный API, схема данных, IAM, архитектурная граница, превышение бюджета, необратимая операция); (4) режимы участия человека по фазам → `human_gates` flow-профиля; (5) минимальный контракт риск-класса (LLM может повысить, понизить — только политика). DoD: изменение без утверждённого контракта не проходит в реализацию. Зависит от T005, T014 — `src/dark_factory/orchestration/policy/`, `src/dark_factory/agents/profiles/` (реализовано: схема контракта и `EscalationViolation` в `changes/`; 10 проверок эскалации, классификатор, риск-монотонность и таблица участия в `orchestration/policy/`; `HUMAN_GATES` в `flows/routes.py`; гейт входа в construction и бюджет автономии — в `flow.apply_result`, `agents/profiles/` не менялись)

**Checkpoint**: US3 проверяется независимо — машинные проверки зелёные на итоговом SHA, лимиты срабатывают детерминированно.

---

## Phase 6: User Story 4 - Автономный MR-контур и merge по правилам (Priority: P4)

**Goal**: При включённом расписании сверки фабрика сама продолжает активные инициативы (CI/MR/замечания), запускает разрешённые попытки; merge — доверенный финализатор по правилам.

**Independent Test**: Включить reconcile и merge policy; провести инициативу от реализации до merge без ручного запуска промежуточных шагов; изменения вне разрешённого класса требуют человека; повтор webhook не создаёт дубль MR.

- [x] T024 [US4] [docs T-030] GitHub-адаптер (первый провайдер MVP): `GitHubAdapter` реализует `RepositoryPort`/`MergeRequestPort`/`PipelinePort` и `CIPort`; ветки/worktree, push, CR (`ChangeRequestRef`), diff, комментарии/review, Checks API, rebase, merge через finalizer; GitHub App с короткоживущими installation tokens; webhooks — ускоритель. DoD: единая контрактная сюита зелёная + интеграционный тест против тестового репозитория (ADR-019). Зависит от T006 — `src/dark_factory/adapters/scm/github/` (реализовано: `CIPort` в `ports/protocols.py` по контракту ports.md + `StageJobRequest`/`stage_gate()` в `ports/common.py`; `GitHubAdapter` в `adapters/scm/github/` — `RepositoryPort` (get_revision, ensure_branch c идемпотентностью по ключу — семантика фейка 1:1), `MergeRequestPort` (open с дедупликацией по head-ветке, find_existing по change-маркеру в body переживает рестарт, add_comment идемпотентен через невидимый маркер-комментарий, merge — сверка head.sha с expected_sha → `HeadMismatchError`, squash, replay уже слитого — no-op), `PipelinePort` (check-runs/actions-runs → `PipelineStatus`, нет запусков → queued/None как у фейка), `CIPort` (workflow_dispatch, детерминированный `job_ref` `github:<slug>:<stage>:<run_id>`, маппинг conclusion → `GateStatus` fail-closed, артефакты → `ArtifactRef`); GitHub App auth: RS256 JWT (PyJWT) → короткоживущий installation token с кэшем по сроку и refresh по 401, токен-провайдер инъецируемый для тестов; фейк `FakeCI` (fakes/ci.py); контрактная сюита параметризована fake|github через stateful эмулятор GitHub API на `httpx2.MockTransport` (tests/contract/github_api.py) — тест-модули сюиты не менялись; новые suite `tests/contract/test_ci_port.py` (9×2), юнит `tests/test_github_auth.py` (15), integration `tests/integration/test_github_adapter.py` (skip без `DARK_FACTORY_TEST_GITHUB_*`); зависимости `httpx2>=2.12` (уже транзитивно в графе через openai/genai-prices — единый HTTP-клиент) и `pyjwt[crypto]>=2.9`; webhooks/push/diff/rebase — вне контракта портов, отложены (задокументировано в docstring модулей). ruff + format + mypy strict + pytest — зелёные (693 passed + 14 skipped), CI на MR повторяет проверки)
- [x] T025 [P] [US4] [docs T-031] CI-шаблоны фабрики (agent jobs): один job = одна стадия Flow, временный pod, сохранение `StageResult`/`NextAction`/usage в artifacts + run record, детерминированные jobs (build/test/lint/security), повторный проход гейтов на итоговом SHA. Зависит от T005, T024, T030 (T-041, US5 — interleaved) — `.github/workflows/`, `src/dark_factory/cli/`, `tests/` (реализовано: reusable workflow `.github/workflows/factory-stage.yml` (`workflow_call`, one job = одна стадия Flow: checkout на итоговый SHA — FR-009/SC-004, `factory doctor`, `factory stage run --json --non-interactive --evidence-dir factory-evidence` с shell-гейтом exit-кодов cli.md — 0/10 job-continuing, waiting ≠ ошибка; артефакт stage_result.json + factory-evidence/ (снапшот, run_record.json) с retention-days и `if: always()` — evidence сохраняется и при blocked/failed; outputs status/next_action/run_id/change_id/usage (токены/стоимость); permissions `contents: read`, без секретов — FR-023; MVP-изоляция — GitHub-hosted эфемерные раннеры, T030 (T-041) сменит runs-on на self-hosted K8s pod); job-script `src/dark_factory/cli/ci_job.py` — валидация StageResult зеркально us1-parity (schema_version=1, результатный статус, непустой next_action.type), рендер outputs `$GITHUB_OUTPUT`, `::error::`-аннотация с коротким stop-reason без секретов (ADR-009), exit 0 для waiting (не ошибка) / 20 blocked / 1 failed / 2 invalid input; `ci.yml` — job `checks` разбит на детерминированные lint/typecheck/test/build/security (postgres-сервис и pytest без изменений; security — pip-audit по locked-набору `uv export --frozen --no-emit-project --no-dev` + `uvx pip-audit`, без новых зависимостей), все checkout на итоговый SHA, `factory-us1-parity` сохранён, dogfood `factory-stage-smoke` вызывает шаблон на `./fixtures/chg_smoke.yaml` (construction) на каждом MR; 53 теста в `tests/test_cli_ci_job.py` (5 exit-кодов → outcome, валидация, outputs, аннотации, waiting → 0). ruff + format + mypy strict + pytest — зелёные (746 passed + 14 skipped без БД/GitHub-интеграций), YAML обоих workflow провалидирован, сквозной прогон stage run → ci_job → outputs локально воспроизведён
- [x] T026 [US4] [docs T-032] Merge policy MVP: protected branch, обязательные human approvals, обязательные checks на итоговом SHA, squash merge, запрет merge при непройденных гейтах; проверка неизменности ожидаемого SHA перед merge (FR-011); publisher/finalizer отделены от sandbox-исполнения агентов. Зависит от T024, T020 — `src/dark_factory/orchestration/policy/`, `src/dark_factory/rules/` (реализовано: `orchestration/policy/merge.py` — детерминированная merge-политика без I/O: `MergePolicy` (auto-merge риск-классы — по умолчанию пусто = ручной режим FR-010, squash-only T-032, гейт merge-авторизации REVIEW = `HUMAN_GATES`), `MergeRequestContext` (executor human|trusted_finalizer|agent, риск-класс, expected/head SHA, human approvals, gate results), `MergeDecision` (blocked / manual_merge_required / human_merge_authorized / finalizer_merge_allowed) и `evaluate_merge` с фиксированным порядком проверок — первая причина побеждает: ① executor=agent → blocked (FR-004/FR-023 — у agent jobs нет merge-полномочий, DoD); ② SHA неизвестны или expected ≠ head → blocked (FR-011); ③ обязательные гейты не удовлетворены результатами на итоговом SHA (результат на старом/неизвестном SHA — устаревший, последний по гейту выигрывает — зеркально `rules.gates`, ADR-009 п.7) → blocked; ④ нет human-решения на гейте REVIEW, привязанного к итоговому SHA (`Decision.commit_sha` — version-bound approval, контракт API T-035), либо последнее — не APPROVED → manual (новый SHA инвалидирует прежний допуск, ADR-009 п.7); ⑤ human → authorized; ⑥ trusted_finalizer + риск-класс явно в разрешённых → allowed с merge_method=squash — финализаторный путь тоже требует SHA-привязанного human approval: human release gate ADR-011 п.2 держится и на auto-merge пути до T-085; ⑦ иначе manual (FR-010). Врезка в flow: `apply_result` получил keyword-only `merge_context` (сигнатура обратно совместима); обработчик MergeAction — сперва прежние stage-гейты/эскалации (причины остановок сохранены), затем политика: blocked → `_stop` (Blocked), manual → run/stage в WAITING c `WaitForInputAction("waiting for human merge (ADR-011 p.2): …")` — повторное применение результата с approval продвигает, authorized → advance to release; `merge_context=None` = manual (fail-safe: на отсутствующих данных машина не мержит); контекст якорится к run/result через `dataclasses.replace` (route/stage/gate_results авторитетны — вызывающий не может сформировать набор гейтов); шов `MERGE_POLICY` в flow — точка конфигурации T-085. `rules/merge_protection.py` — требования branch protection/rulesets как данные без сети: `MergeProtectionPolicy` (protected branch, ≥1 обязательный human approval, обязательные checks на итоговом SHA, squash-only, dismiss stale approvals — провайдерский аналог инвалидации допусков новым SHA) + `protection_violations(ObservedBranchProtection)` — детерминированный список нарушений в фиксированном порядке (наблюдения подаст адаптер T-030, исполнение — на стороне провайдера). `changes/findings.py`: `Decision.commit_sha: str | None` (None = unbound — никогда не авторизует merge на конкретный SHA). Тесты: 21 политика (все ветки, включая «approval на старом SHA не авторизует новый»), 11 branch protection, врезка в flow (merge без approval → WAITING, с approval → release, SHA mismatch → Blocked, agent executor → Blocked); существующие merge-тесты обновлены на авторизованный контекст; `test_blocking_finding_stops_merge` сохранён без изменений. ruff + format + mypy strict + pytest — зелёные (789 passed + 14 skipped без БД/GitHub-интеграций))
- [x] T027 [US4] [docs T-063] Reconcile и recovery: идемпотентный CronJob (2–5 мин, `concurrencyPolicy: Forbid`), восстановление по PostgreSQL/Git/MR/StageResult, правила (истёкшая блокировка, дубликаты запусков, merged MR без run, approved+passed без merge, branch behind, повторяющаяся ошибка → эскалация); webhook — только ускоритель; разрешение рассинхрона «CI ↔ PostgreSQL» в пользу PostgreSQL после сверки. Зависит от T025, T024 — `src/dark_factory/orchestration/reconcile/` (реализовано: пакет `orchestration/reconcile/` — `rules.py`: таблица «аномалия → действие» как данные (`ANOMALY_RULES`, 6 правил в фиксированном порядке, первое срабатывание побеждает; `plan_action` — чистая функция без I/O и часов; `resolve_drift` — сверка CI ↔ PostgreSQL всегда в пользу PostgreSQL, ADR-006 п.9); `models.py`: frozen-журнал прохода `AnomalyKind`/`ReconcileAction`/`RunReconciliation`/`ReconcileReport`; `service.py`: `GlobalReconciler` — один идемпотентный проход в одной транзакции: глобальный PG-lease `("reconciler", "global")` с fencing token (K8s `Forbid` — только оптимизация; параметры CronJob 2–5 мин / `activeDeadlineSeconds=300` — константы `RECONCILE_INTERVAL_MINUTES`/`ACTIVE_DEADLINE_SECONDS`, манифест — T040/T042), скан активных статусов, опциональные швы наблюдения (WorkflowEnginePort, `ReconcileObserver` для Git/MR/StageResult), планирование и применение. Правила: ① повторяющаяся ошибка (два последних attempt одной стадии FAILED) → ESCALATE в Blocked; ② истёкшая блокировка → LEASE_TAKEOVER через семантику LeaseRepository (свежий монотонный fencing token; живой чужой lease → applied=False с note); ③ дубликаты запусков (активные run одного change; канонический — лексикографически минимальный id) → SUPERSEDE не-канонического; ④ merged MR без завершённого run → RECORD_MERGE (→ SUCCEEDED, факт merge не теряется); ⑤ approved+passed без merge → решение существующей merge-политики (T-026) для executor=trusted_finalizer: PLAN_MERGE (squash) / WAIT_FOR_HUMAN (ручной режим, MVP-дефолт) / ESCALATE (нарушение политики; авторизация merge по-прежнему SHA-привязана); ⑥ branch behind → UPDATE_BRANCH для provider wiring. Reconciler исполняет только state-мутации (takeover/supersede/record merge/escalate) с двойной защитой (fencing token + ожидаемая ревизия, проверка таблицы переходов; домен-невалидные — journal-only), агентные задачи не исполняет; PLAN_MERGE/WAIT_FOR_HUMAN/UPDATE_BRANCH — report-only для своих исполнителей; идемпотентность прохода и webhook-независимость зафиксированы тестами и докстрингами. `PostgresReconciliationService` — адаптер порта `ReconciliationService` (контракт портов/моделей не менялся). CLI: `factory reconcile [--json]` (`cli/reconcile.py`) — один проход, отчёт text/JSON, exit 0 (включая `lease_acquired=False` — норма при параллельном запуске) / 2 (БД недоступна; URL и текст исключений не эхо — ADR-009), URL из `DATABASE_URL` (как у doctor), liveness-probe. Тесты: 33 юнит (таблица, приоритеты правил, негативы, детерминизм/сходимость, drift, observe, порт) + 6 CLI + 13 интеграционных против PostgreSQL (takeover с инкрементом token, supersede, record merge, эскалация, идемпотентность второго прохода, глобальный lease, живой чужой lease, drift в пользу PostgreSQL, report-only) — `tests/test_orchestration_reconcile.py`, `tests/test_cli_reconcile.py`, `tests/integration/test_reconcile.py`; стабы `test_cli_parser.py` обновлены. ruff + format + mypy strict + pytest — зелёные (826 passed + 27 skipped без БД; 852 passed + 1 skipped с PostgreSQL), сквозной smoke `factory reconcile --json` против тестовой БД воспроизведён)
- [x] T028 [US4] [docs T-064] Outbox Dispatcher CronJob: единственный владелец доставки; резервирование через `FOR UPDATE SKIP LOCKED`/короткий lease; запуск внутренних обработчиков или CI pipeline; фиксация в `event_delivery`, exponential backoff, `dead` при исчерпании; порядок только в рамках `aggregateId`/`changeId`/`runId` по `sequence`; очистка outbox — только после терминального статуса всех обязательных потребителей и retention. DoD: at-least-once тест (повтор `eventId` не создаёт второй эффект), dead-letter и ручной replay. Зависит от T004 (T-006), T029 (T-040, US5 — interleaved) — `src/dark_factory/orchestration/events/`, `deploy/` (реализовано: пакет `orchestration/events/` — `rules.py`: чистые детерминированные правила без I/O и часов (`now` — явный параметр): backoff `backoff_delay_seconds` (константы `MAX_ATTEMPTS=5`, base 30 с, ×4, cap 3600 с — расписание 30с→2м→8м→32м→dead, ~1 ч до парковки), `is_dead`, `is_undelivered`, `min_undelivered_sequence`, `ordering_eligible` (строгое равенство: не минимальный недоставленный sequence → defer; `dead` блокирует stream до replay/skip, `delivered`/`waived` разблокируют), `is_cleanup_eligible` (все доставки in {delivered, waived} И occurred_at + `RETENTION_DAYS=30` ≤ now; dead запрещает удаление; связь с retention аудита ADR-009 §9 задокументирована; архивация не реализована — задокументировано); `models.py`: frozen-журнал прохода `DeliveryOutcomeKind`/`DeliveryOutcome`/`DispatchReport` + `sort_outcomes` (детерминированный порядок по (event_id, consumer_id)); `handlers.py`: consumer-side SPI `DeliveryHandler` (Protocol, без изменений в `ports/`), `NoOpHandler` (MVP-заглушка — реальные потребители T-063/T-090), `HandlerRegistry` (неизвестный потребитель → NoOp — проход не ломается; CI-pipeline-хук не реализован (YAGNI), реестр — точка расширения); `repository.py`: `DeliveryRepository` рядом с пакетом events (не в `state/repositories.py` — связность доставки, существующие репозитории не тронуты): `reserve_batch` — SELECT … JOIN с `FOR UPDATE OF event_delivery SKIP LOCKED` + lease `LEASE_SECONDS=120` (запись `next_attempt_at`), ordering-gate внутри транзакции резервирования (gap → defer, строка буквально не тронута), `commit_outcome` с guard'ом `status IN (pending, failed)` (не перезаписывает терминальные/операторные состояния при гонке), `replay` (dead/failed → pending, attempts=0, ошибки и backoff сброшены), `skip` (pending/failed/dead → waived, админское решение ADR-016 п.6); `cleanup.py`: удаление только cleanup-eligible событий (FK CASCADE уносит доставки; кандидаты — только старше cutoff); `dispatcher.py`: `OutboxDispatcher.run_pass` — один идемпотентный проход: короткая транзакция резервирования → вызов обработчика строго вне транзакций → отдельная короткая транзакция фиксации исхода (успех → delivered attempts+1; ошибка → attempts+1, last_error ≤1024, backoff → next_attempt_at, исчерпание → dead); crash между вызовом и фиксацией → повторная доставка после lease (at-least-once целевое поведение); опциональный шаг очистки отдельной транзакцией. Решение по stream: `aggregate_id` (уникальный ключ (aggregate_id, sequence) уже в схеме; change/run делят sequence-пространство агрегата) — задокументировано в докстрингах; глобальный порядок не гарантируется. CLI (`cli/outbox.py` по образцу reconcile): `factory outbox dispatch [--json] [--limit N] [--cleanup]` (имя команды — по контракту cli.md `outbox dispatch [--once]`, `--once` сохранён как no-op совместимости — проход всегда один), `factory outbox replay --event-id ID [--consumer ID]`, `factory outbox skip --event-id ID --consumer ID`; exit 0 (включая «делать нечего»), 1 (replay/skip — нет подходящей пары), 2 (БД недоступна/невалидный ввод; URL и текст исключений не эхо — ADR-009); wiring в `cli/main.py`, liveness-probe как у reconcile. Деплой: `deploy/events/outbox-dispatcher-cronjob.yaml` — CronJob ns `factory`, каждые 2 мин (`timeZone: Etc/UTC`), `concurrencyPolicy: Forbid` (только оптимизация — корректность даёт SKIP LOCKED), `activeDeadlineSeconds: 300`, `backoffLimit: 0`, `restartPolicy: OnFailure`, non-root securityContext (runAsUser/Group 65532, seccomp RuntimeDefault, readOnlyRootFilesystem, drop ALL), resources 50m/64Mi → 200m/128Mi, SA `factory-api` из bootstrap (automount=false; выделенный SA — T030/T042), команда `factory outbox dispatch --cleanup`; образ и Secret — плейсхолдеры с явными комментариями (реальный образ — T033/T042; Secret `factory-outbox-dispatcher-database` создаётся вне git, пример в README); `deploy/events/README.md` на русском (назначение, механика, расписание, ручные операции, ограничения). Тесты: 19 юнит (таблица backoff, dead-правило, ordering-eligibility (gap/dead блокируют, delivered/waived разблокируют), cleanup-матрица, детерминизм sort_outcomes, реестр/NoOp) — `tests/test_orchestration_events.py`; 6 CLI (render text/JSON, невалидный URL без эха, недостижимый store, --limit 0, wiring трёх команд) — `tests/test_cli_outbox.py`; стабы `test_cli_parser.py` заменены на парсинг реальных опций (dispatch/replay/skip, missing-required, help — файл теперь 52 теста); 12 интеграционных против PostgreSQL (at-least-once: crash между handler и фиксацией → повторная доставка, дедупликация по event_id даёт ровно один эффект; два перекрывающихся прохода не резервируют одну delivery (SKIP LOCKED) + lease; happy path; failure → backoff 30 с, undue-проход не берёт; исчерпание 5 попыток → dead; replay dead → delivered; gap: seq=2 не доставляется до delivered seq=1, 4 прохода, порядок вызовов; skip dead → waived разблокирует, повторный skip → exit 1; cleanup-матрица (eligible удаляется + FK-каскад, retention/dead/не-все-потребители удерживают), cleanup opt-in; CLI dispatch --json) — `tests/integration/test_events_dispatcher.py`. Валидация: ruff check + ruff format --check + mypy strict — чисто; pytest без БД 857 passed + 39 skipped (интеграции), с PostgreSQL 16 (эфемерный docker-контейнер) 895 passed + 1 skipped; манифест — kubeconform -strict 1/1 valid; сквозной smoke `factory outbox dispatch --json` / `replay` / `skip` / `--limit 0` против тестовой БД — exit 0/1/1/2 без эха URL)

**Checkpoint**: US4 проверяется независимо — дубликаты не создаются, merge вне разрешённого класса требует человека.

---

## Phase 7: User Story 5 - Релиз в dev через GitOps (Priority: P5)

**Goal**: После merge — immutable digest, GitOps-изменение, применение в dev, smoke; статус «выпущено» только при успешном smoke.

**Independent Test**: После merge пилотного изменения проверить цепочку сборка → GitOps MR → deploy → smoke → «выпущено»; провал smoke не даёт статус «выпущено».

- [x] T029 [P] [US5] [docs T-040] Bootstrap локального Kubernetes: Docker Desktop K8s, namespaces (`argocd`, `factory`, `ci`, `factory-runs`, `apps-dev`), quotas, service accounts, baseline NetworkPolicy deny-by-default + негативные egress-тесты; PostgreSQL фабрики (PVC, backup, автоматизированный restore-тест, MVP RPO/RTO) вместе с БД Plane логически раздельно; проверка доступности и учётных данных GitHub App; capacity smoke полного контура (idle + один e2e job). DoD: скрипт воспроизводим на чистом Docker Desktop (ADR-010) — `deploy/bootstrap/` (реализовано: идемпотентный `bootstrap.sh` (флаги --context/--only-license-check/--strict-license/--skip-*) — prereq + детект выключенного K8s в Docker Desktop (settings-store.json KubernetesEnabled=false → точная инструкция + exit 1, настройки пользователя не меняет), best-effort license-check по ADR-010 (warn-only, --strict-license — fail); namespaces ×5 + ResourceQuota/LimitRange ×5 (потолки ≈12.5Gi при concurrency=1, обоснование в README) + 5 service accounts (automountServiceAccountToken=false; RBAC — T030); NetworkPolicy deny-by-default ×19 (deny-all/allow-dns/allow-intra-ns во всех ns, allow-egress-https в factory/factory-runs/ci/argocd с обоснованием allow-листа в манифесте; сужение — T030); PostgreSQL 16.4 StatefulSet (PVC 5Gi, non-root, probes, Secret генерируется openssl при первом запуске и не попадает в git) с двумя логически раздельными БД factory/plane (отдельные роли-владельцы, init-скрипт); backup CronJob 02:30 UTC (pg_dump -Fc обеих БД на PVC 2Gi, retention 7 дней, RPO=24h, RTO≈15min — ручная процедура в README); автоматический restore-test (последний дамп → restore_test_<ts>, сверка таблиц/записей с source, DROP, SKIP без дампов); негативные egress-тесты (DNS ok, 443 ok, HTTP 80/TCP 25 блокируются, factory→postgres ok, cleanup подов); capacity smoke (allocatable узла, kubectl top при наличии metrics-server, e2e-job busybox с cgroup v2-замером peak RAM/CPU, вердикт против профиля 10–12GB); check-github-app.sh (env DARK_FACTORY_GITHUB_APP_ID/_PRIVATE_KEY/_INSTALLATION_ID/_API_URL → RS256 JWT на bash+openssl → installation access token, печать только фактов без секретов; без env — SKIP; --self-test); teardown.sh с подтверждением. README на русском: требования (включение K8s, ресурсы Docker Desktop 10–12GB), флаги, RPO/RTO, ограничения. Валидация: bash -n ×7 и shellcheck -x ×7 (docker koalaman/shellcheck:stable) — зелёные; kubeconform -strict — 49/49 ресурсов валидны; check-github-app.sh --self-test — зелёный; живой сценарий bootstrap.sh --only-license-check против выключенного K8s — корректный детект + инструкция + exit 1. Ограничение: живой кластерный прогон не выполнялся — Kubernetes в Docker Desktop был выключен; первый полный прогон: включить K8s → ./bootstrap.sh)
- [x] T030 [P] [US5] [docs T-041] CI-раннеры (K8s executor) + безопасность job pods: self-hosted GitHub Actions runners в `ci`, pods только в `factory-runs` (non-root, read-only rootfs, без privileged/Docker socket/hostPath, `automountServiceAccountToken=false`, лимиты, active deadline, TTL cleanup, отдельные SA, закреплённые digests). DoD: pod создаётся/удаляется по TTL; agent job не имеет merge/deploy credentials. Зависит от T029 — `deploy/ci/` (реализовано: двухуровневая архитектура — ephemeral GitHub Actions runner (Deployment `factory-runner`, 1 реплика, Recreate, concurrency=1 по ADR-010) в ns `ci`, agent-стадии — K8s Job ТОЛЬКО в `factory-runs` (ADR-006: один эфемерный pod = одна стадия); без ARC — осознанное решение (listener и work-поды ARC в одном ns не выражают разделение `ci`/`factory-runs`, обоснование в README): `install.sh` (идемпотентный apply, без секретов, падает с подсказкой при отсутствии bootstrap), `verify.sh` (RBAC-матрица через impersonation + probe-job с 9 негативными/позитивными проверками изнутри пода: no_sa_token/env_clean/api_blocked/api_ip_blocked + TTL-удаление), `teardown.sh` (возврат baseline T029); `scripts/entrypoint.sh` (preflight SSAR → минт registration token по GitHub App: RS256 JWT → installation token → registration-token endpoint, тот же пайплайн что check-github-app.sh T029 → `config.sh --ephemeral` → `run.sh`; registration token живёт 1 ч и никогда не хранится, минтуется на каждый старт пода), `run-agent-job.sh` (рендер шаблона → создание Job через K8s REST API curl+jq (kubectl в образе нет) → polling → извлечение StageResult по маркерам из лога → маппинг exit-кодов 0/10/20), `common.sh` (самодостаточная копия хелперов bootstrap). RBAC (`manifests/rbac.yaml`): Role `factory-agent-jobs` в `factory-runs` — jobs CRUD, pods get/list/watch, pods/log get; БЕЗ pods/exec, БЕЗ secrets, БЕЗ доступа к control-plane namespaces; cross-ns RoleBinding SA `factory-ci` (из `ci`) → `factory-runs`; SA `factory-agent` — без RBAC и без токена (pod-level `automountServiceAccountToken: false`). securityContext agent-подов (`agent-job-template.yaml`): runAsNonRoot uid/gid 65532, readOnlyRootFilesystem, privileged=false, allowPrivilegeEscalation=false, drop ALL, seccomp RuntimeDefault, без docker socket/hostPath; writable — emptyDir `/tmp` + `/workspace` 2Gi; лимиты 250m/512Mi → 1 CPU/1Gi; TTL `ttlSecondsAfterFinished: 3600` (DoD «удаляется по TTL»), `activeDeadlineSeconds: 3600`, backoffLimit 0. NetworkPolicy (`manifests/networkpolicies.yaml`): широкое bootstrap-правило allow-egress-https в `factory-runs` ЗАМЕНЯЕТСЯ на «публичный 443» (ipBlock 0.0.0.0/0 с except приватных диапазонов: pod/service CIDR, host-сеть Docker Desktop с apiserver:6443, link-local; то же имя объекта — политики аддитивны, «вычесть» allow отдельной политикой нельзя); install.sh пере-применяет после bootstrap, teardown.sh возвращает baseline. Регистрация: GitHub App permission только Administration:write — недостаточно для merge (contents:write), deploy или repo secrets; Secret `factory-github-app` в `ci` создаётся вне git (envFrom optional: true — без секрета под стартует и печатает точную команду remediation). Pinned digests (проверены imagetools inspect): `ghcr.io/actions/actions-runner@sha256:551dc313…` (v2.327.1), `busybox@sha256:9db7b599…` (1.37); образ фабрики — плейсхолдер `__IMAGE__` до T033. README на русском (архитектура, инвентарь credentials, безопасность, установка, ограничения); ADR не потребовался — решение покрыто ADR-006/010/019, обоснование в README. Валидация: bash -n ×6 и shellcheck ×6 — зелёные; kubeconform -strict — 7/7 ресурсов (включая all-digits регресс рендера шаблона, вскрывший латентный баг незакавыченных string-плейсхолдеров `__GIT_SHA__`/`__RUN_ID__` — пофикшено в шаблонах); ruff check + ruff format --check + mypy strict + pytest без БД (857 passed, 39 skipped) — зелёные. Ограничение: живой кластерный прогон не выполнялся — Kubernetes в Docker Desktop выключен; первый прогон: bootstrap.sh → install.sh → verify.sh. runs-on swap в factory-stage.yml, транспорт evidence-каталога и приватные репо (contents:read токен) — T031; образ с immutable digest — T033)
- [x] T031 [US5] [docs T-042] Helm chart dark-factory: Console/API с зависимостью PostgreSQL фабрики, values-local, ресурсы по профилю MacBook, probes, локальный ingress. DoD: helm lint + тестовый deploy в namespace `factory`. Зависит от T029 (T-040), T035 (T-050, US6 — interleaved) — `charts/dark-factory/` (реализовано: chart `charts/dark-factory/` (apiVersion v2, 0.1.0) ставит API T035 (`factory api serve --host 0.0.0.0`) в ns `factory`: Deployment (1 реплика, Recreate — квота допускает ровно один API-под рядом с PG) + Service ClusterIP + условный Ingress (networking.k8s.io/v1, в values-local включён, className nginx, host factory.localhost — без контроллера инертен, штатный доступ port-forward); PostgreSQL не бандлится — используется bootstrap-овский `factory-postgres` (T029, «зависимость PostgreSQL фабрики»); `DATABASE_URL` из existingSecret `factory-api-database` (паттерн T028, секрет вне git, в README предупреждение о percent-кодировании base64-пароля с `+/=`), опциональный existingSecret для `DARK_FACTORY_API_TOKENS` (пусто = mutating fail-closed, ADR-009 §7); pre-install/pre-upgrade hook-Job миграций (`alembic upgrade head`, weight 0, before-hook-creation,hook-succeeded, deadline 300 с, backoffLimit 0, restartPolicy Never — fail-fast) — требование к образу T033 (alembic + migrations/ в образе) зафиксировано; probes без изменения кода API (T035 слит): liveness `GET /openapi.json` (200 без БД — процесс не рестартует при простое state store), readiness `GET /api/v1/runs` (200 только при живой БД — снимает с трафика и возвращает автоматически); безопасность по паттерну T028: SA `factory-api` bootstrap (переиспользование, serviceAccount.create=false), automountServiceAccountToken false, runAsNonRoot 65532, readOnlyRootFilesystem, drop ALL, seccomp RuntimeDefault, emptyDir /tmp; ресурсы requests 100m/128Mi, limits 500m/512Mi (пик контура 400m/448Mi req / 1.7 CPU / 1.64Gi lim ≤ квота 1 CPU/2Gi / 2 CPU/4Gi — расчёт в README); labels по конвенции репозитория (managed-by: dark-factory); образ-плейсхолдер `ghcr.io/vadagama/dark-factory:bootstrap` до T033. Валидация: helm lint (дефолт и values-local) — 0 failed; helm template обоих профилей — чисто; pytest chart-тестов `tests/test_chart_dark_factory.py` — 11 passed (рендер через helm template: probes, securityContext, ресурсы, отсутствие `:latest`, отсутствие Secret-ов в чарте, hook-аннотации, Ingress только в values-local; скип без helm); ruff + format + mypy strict — чисто; ruff/mypy/pytest по всему репо — зелёные. Тестовый deploy в ns `factory` (DoD): bootstrap T029 применён к живому docker-desktop (первый полный прогон: ns, квоты, SA, NetworkPolicy, PostgreSQL ready; негативные egress-тесты в этом окружении упали — `https443=FAIL`/`http80_blocked=FAIL` — средовое отклонение T029, к chart не относится); throwaway-образ собран локально (не коммитился); `helm install --wait` — Install complete: hook-Job применил схему 0001+0002 к реальной PG и самоудалился (hook-succeeded), API 1/1 Running; функционально: `GET /openapi.json` 200, `GET /api/v1/runs` 200 `[]`, `POST /api/v1/changes` без токена 401 RFC 7807 (fail-closed); после проверки release удалён чисто (схема в PG и секрет остались — штатно)
- [ ] T032 [US5] [docs T-043] Argo CD + GitOps-репозиторий: установка Argo CD (Helm), read-only GitOps-репо, Applications для `factory` и `apps-dev`; поток merge → GitOps MR с immutable digest → sync. DoD: GitOps MR меняет digest, Argo разворачивает в `apps-dev`; откат — revert коммита. Зависит от T029 — `deploy/argocd/`, `dark-factory-gitops`
- [ ] T033 [US5] [docs T-044] OCI build job (immutable digest): доверенный build для итогового SHA без privileged DinD, SBOM, SAST/SCA/secrets/image scan, публикация immutable digest, запрет `latest` для promotion. DoD: образ по digest; повторная сборка того же SHA даёт тот же digest (или задокументированное отклонение). Зависит от T025 — `.github/workflows/`, `deploy/ci/`
- [ ] T034 [US5] [docs T-045] Smoke + release evidence: проверка неизменности ожидаемого digest перед деплоем (FR-011); health + smoke после Argo sync; release evidence (digest, Argo status, smoke result) в run record; неуспешный smoke не переводит изменение в Released. DoD: e2e — успешный и неуспешный smoke дают корректные статусы и evidence. Зависит от T032, T033 — `src/dark_factory/quality/release/`

**Checkpoint**: US5 проверяется независимо — 100% «выпущено» имеют успешный smoke на зафиксированном digest.

---

## Phase 8: User Story 6 - Контекст, знания, трекер и наблюдаемость (Priority: P6)

**Goal**: Контекст из закреплённых источников с фиксацией версий/provenance; синхронизация с трекером через сменный адаптер; Console показывает стадии, блокеры, стоимость, гейты, ссылки на evidence; полная трассируемость.

**Independent Test**: Для одной инициативы собрать `ContextBundle` (проверить фиксацию версий), синхронизировать статусы с Plane (без дублей), открыть Console (стадия, блокеры, стоимость, гейты, ссылки); отключение трекера не останавливает конвейер.

- [x] T035 [P] [US6] [docs T-050] FastAPI API: `GET /runs`, `/runs/{id}`, `/runs/{id}/stage-results`, `/runs/{id}/trace`, `/changes`, `/changes/{id}`, `/changes/{id}/trace`, `/changes/{id}/approvals`, `/runs/{id}/evidence`, `/runs/{id}/gates`, `/runs/{id}/findings`; token AuthN (единый оператор + service-токены CI/CLI), version-bound approval `{actor, role, contract_hash/commit_sha, timestamp, decision}`, audit mutating-операций, `Idempotency-Key`, `409` при `state_revision` mismatch, RFC 7807-подобные ошибки. DoD: негативные AuthN-тесты, OpenAPI-схема. Зависит от T003, T006 — `src/dark_factory/api/` (реализовано: пакет `api/` — `app.py` (create_app, префикс `/api/v1`, RFC 7807-хендлеры 401/403/404/409/422/500 без утечки внутренностей), `auth.py` (Bearer-токены из `DARK_FACTORY_API_TOKENS` (JSON `{token, actor, role, scopes}`), хранение только SHA-256-дайджестов, сравнение `hmac.compare_digest`, fail-closed при пустом store, роли operator/service, scopes `changes:write`/`approvals:write`, approvals дополнительно требуют роль operator — агенты не согласовывают), `dto.py`, `aggregates.py` (trace/gates «последний результат по гейту выигрывает»/findings/evidence с дедупликацией по id), `routes_runs.py` (8 GET: runs+фильтры, карточка с usage-агрегатом и открытыми блокерами, stage-results, trace SC-007, evidence (+/{id}), gates, findings с фильтрами severity/status), `routes_changes.py` (POST /changes — intake FR-001 с дедупликацией по id и external_ref FR-017, GET /changes, карточка, change-trace, approvals GET/POST — version-bound `Decision.commit_sha=subject_revision`, опциональный `expected_state_revision` → 409 до записи, bump revision; состояние+audit в одной транзакции). Миграция `0002_api_state` (обратима): таблицы `change` (unique partial external_ref), `decision` (unique partial idempotency_key), `stage_result` (PK=attempt_id, payload JSONB, ON CONFLICT DO NOTHING — данные попыток не перезаписываются, FR-014), `audit_log`; ORM-модели в `state/models.py`, репозитории `state/stage_results.py` + `state/change_store.py` (идемпотентные pg_insert). CLI `factory api serve` (`cli/api.py`: DATABASE_URL без эха, exit 2 при отсутствии; uvicorn). Зависимости +fastapi +uvicorn. Тесты: 10 no-DB (401/403-матрица, OpenAPI-пути, RFC 7807, парсинг CLI) + 10 интеграционных с PG (intake-идемпотентность, replay по Idempotency-Key, 409, version-bound approvals, гейты/findings/evidence/trace, usage-агрегат); `_TABLES` в conftest дополнен. Ограничения: CLI/движок пока не пишут StageResult в PostgreSQL (запись — публичный репозиторий `StageResultRepository`, durable wiring — следующая задача); GET-эндпоинты без токена (локальный контур, ADR-009 §7); pause/retry/cancel — вне чеклиста T035 (нужен durable wiring движка). ruff + format + mypy strict — чисто; pytest без БД 872 passed + 49 skipped; с PostgreSQL 16.4 (эфемерный docker) 920 passed + 1 skipped)
- [ ] T036 [US6] [docs T-051] Console MVP (5 экранов): список изменений, карточка изменения с evidence и цепочкой стадий, гейты/approvals, бюджеты/лимиты, настройки/профили; режимы «с согласованиями» и «автономно до MR». DoD: vitest + Playwright smoke 5 сценариев; сборка chart'ом (T031). Зависит от T035 — `src/dark_factory/console/`
- [ ] T037 [P] [US6] [docs T-033] `TrackerPort` + адаптер Plane (self-hosted, webhook+HMAC): получение задачи, публикация статусов/ссылок, запрос уточнений; webhook — ускоритель, подпись + timestamp window, дедупликация по event ID, ротация секрета; до готовности — NoOp-заглушка; недоступность трекера не блокирует CLI/Console (FR-020). DoD: e2e создание задачи → Change → статус отражён. Зависит от T006 — `src/dark_factory/adapters/tracker/`
- [ ] T038 [P] [US6] [docs T-060] `TelemetryPort` + OpenTelemetry-корреляция: трейсы `change → run → stage → agent call → tool call → CI job → deployment`, атрибуты usage/cost, экспорт в job logs/artifacts (внешний backend опционально). DoD: по trace восстанавливается полная цепочка запуска. Зависит от T006 — `src/dark_factory/adapters/telemetry/`
- [ ] T039 [US6] [docs T-061] Run records в `dark-factory-runs` — **P0-минимум** (блокирует T043): `RunManifest` + итоговый `StageResult` + evidence-ссылки с первого e2e-прогона; запись идемпотентна (lookup по `change_id`). Полный протокол (partitioning, single-writer, sanitization, подпись manifest), retention и аналитика — P1. Зависит от T003, T024 — `dark-factory-runs`
- [ ] T040 [US6] [docs T-062] Бюджет-координатор и allowance: лимиты токенов/времени/стоимости на запуск и роль, allowance-политики, исчерпание → Awaiting Decision с диагностикой, совокупный бюджет в `RunSnapshot`. Зависит от T005, T019 — `src/dark_factory/orchestration/budget/`

**Checkpoint**: US6 проверяется независимо — трекер отключаем, Console показывает полную картину, trace восстанавливается.

---

## Phase 9: Pilot & Metrics (Cross-Story Validation)

**Purpose**: Пилотный продукт и сквозная валидация P0-цепочки (SC-001…SC-008).

- [ ] T041 [P] [docs T-070] Engineering pack пилотного продукта: blueprint (React + Small UIKit + FastAPI + PostgreSQL), CI-конфигурация продукта, Helm chart приложения, тестовые конвенции; pack версионируется. DoD: blueprint разворачивается в `apps-dev` через фабрику. Зависит от T032 (T-043) — `packs/web-app/`
- [ ] T042 [P] [docs T-071] Small UIKit минимум + UI-гейты: токены (DTCG), 10–12 компонентов + 5–7 паттернов, Storybook как живая спека; гейты UIKit-policy (ESLint), stylelint, axe/WCAG 2.2 AA, visual regression (Playwright); запрет прямых импортов вне `@small/ui`. Зависит от T041 — `packs/ui/`
- [ ] T043 [docs T-072] E2E-пилот: 10 реальных задач (5 quick, 5 standard) по сквозному сценарию intake → SDD → реализация → MR → CI/review → merge → image → GitOps → dev → smoke; сбор метрик vision §7 и метрик FR-024 на уровне инициативы (стоимость с учётом попыток, принятые с первого прохода, число раундов rework, время до принятого MR, число ручных вмешательств), журнал отклонений. DoD: ≥7/10 e2e без ручных правок артефактов агента; отчёт по SC-001…SC-008. Зависит от T023, T025, T026, T031, T033, T034, T035, T039, T041 — `dark-factory-runs`, `specs/001-dark-factory-mvp/quickstart.md`
- [ ] T044 [docs T-073] Отклонения пилота → backlog и eval-кейсы: каждый сбой — eval-case (вход/ожидание/факт) и элемент backlog; базовый eval-набор для промптов и skills; прогон локально и в CI, отчёт по регрессиям. Зависит от T043 — `evals/`

---

## Phase 10: Polish & Cross-Cutting Concerns (Extensions P1–P3)

**Purpose**: Расширения после стабилизации пилота (не блокируют MVP); финальная синхронизация документации.

- [ ] T045 [P] [docs T-080] Риск-классы R0–R4 и точки контроля: классификация, обязательные гейты и участие человека по классу, 4 точки контроля, «LLM повышает, понижает — только политика», маршруты architecture/foundation; машинное условие эскалации R2+. DoD: маршрутизация детерминирована, опасные изменения не проходят по короткому пути. Зависит от T005, T023, T040 — `src/dark_factory/orchestration/policy/`
- [ ] T046 [P] [docs T-081] Роли Design и Architect + гейты UI/Planning: UX-flow/состояния/UIKit/accessibility и impact/контракты/данные/NFR/ADR-proposal. Зависит от T014, T042 — `src/dark_factory/agents/profiles/`
- [ ] T047 [P] [docs T-082] Роли Infrastructure, Security, CI/CD, Operation: инфраструктурные изменения (Helm/K8s), security-анализ (IAM/secrets/зависимости), pipeline/поставка, эксплуатация (smoke, rollback-анализ), release-гейт с Operation-вердиктом. Зависит от T014, T034 — `src/dark_factory/agents/profiles/`
- [ ] T048 [P] [docs T-084] Learning loop (минимальный контур): сигнал → RuleProposal/SkillProposal → проверка на eval-наборе (Pydantic Evals) → MR с независимым review; защита holdout-набора, сравнение с baseline. Зависит от T044, T039 — `src/dark_factory/learning/`
- [ ] T049 [P] [docs T-085] Auto-merge низкого риска (policy-controlled): по статистике T043 — политика для R0/R1 при зелёных гейтах, audit-log оснований, лёгкое отключение. Зависит от T045, T043 — `src/dark_factory/orchestration/policy/`
- [ ] T050 [P] [docs T-086] Preview namespace на MR: детерминированное создание/удаление (TTL, cleanup после merge/close). Зависит от T032 — `deploy/preview/`
- [ ] T051 [P] [docs T-090] Event-driven fast jobs (`@factory review` / `fix-ci`): короткие агентные операции по CI-событиям без полного Change Flow, те же политики безопасности и лимиты. Зависит от T025, T020 — `.github/workflows/`
- [ ] T052 [P] [docs T-087] Foundation: каталог блоков (owner, maturity, зависимости) и scorecards, управляемые обновления через migration MR. Зависит от T042, T048 — `src/dark_factory/foundation/`
- [ ] T053 [P] [docs T-088] Marketplace packs: формат CapabilityPack/FactoryPack (skills+agents+gates+policies+evaluations), SemVer, CI-валидация манифестов, публикация в registry, установка в проект. Зависит от T052 — `packs/`
- [ ] T054 [P] [docs T-089] Второй harness-адаптер: внешний CLI-агент как `HarnessPort`-адаптер; проверка смены исполнителя без изменения Flow. Зависит от T019 — `src/dark_factory/adapters/harness_alt/`
- [ ] T055 [P] [docs T-091] Миграция в shared/DC-контур: TLS/OIDC, Vault/external secrets, устойчивое хранилище артефактов, retention/backup, повышение concurrency после capacity-теста, разделение prod/non-prod Argo CD; пересмотр Temporal только при подтверждённых требованиях. Зависит от T031, T032, T043 — `charts/dark-factory/` (values-dc)
- [ ] T056 Синхронизация документации и финальная приёмка: обновить `docs/plan.md` (статусы), `docs/hld.md`/ADR при значимых решениях, проверить `quickstart.md` end-to-end, зафиксировать DoD-отчёт — `docs/`, `specs/001-dark-factory-mvp/quickstart.md`

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: без зависимостей — выполнено.
- **Foundational (Phase 2)**: зависит от Setup — **БЛОКИРУЕТ все user stories**. Порядок внутри: T004 → T005 → T006.
- **US1 (Phase 3)**: зависит от Foundational (T005, T006).
- **US2–US6 (Phases 4–8)**: зависят от Foundational; внутри каждой — свой порядок.
- **Pilot (Phase 9)**: зависит от P0-цепочки US3–US6.
- **Polish/Extensions (Phase 10)**: зависит от соответствующих ранних задач; не блокирует MVP.

### User Story Dependencies

- **US1 (P1)** — MVP, не зависит от других историй.
- **US2 (P2)** — независима; использует T005/T006.
- **US3 (P3)** — независима; использует T005, T014 (US2).
- **US4 (P4)** — зависит от Foundational; использует `ChangeRequestRef`/порты (T006).
- **US5 (P5)** — зависит от Foundational и результата T025 (CI-шаблоны, US4). Фазовая зависимость «US5 после US4» не применяется: T025 и US5 связаны interleaved-цепочкой T029 → T030 → T025 → остальная US5.
- **US6 (P6)** — зависит от Foundational и артефактов US4 (T024) для трассировки.

### Критические межстадийные зависимости (interleaved)

Порядок внутри каждой истории корректен, но `docs/plan.md` задаёт несколько рёбер между историями — их нужно учитывать при планировании прогона:

- `T017 [US2 / T-021]` требует `T023 [US3 / T-016]` → T023 выполняется до T017.
- `T014 [US2 / T-011]` требует `T015 [US2 / T-012]` и `T019 [US3 / T-010]` → T019 выполняется до T014.
- `T018 [US2 / T-022]` требует `T041 [Pilot / T-070]`.
- `T025 [US4 / T-031]` требует `T030 [US5 / T-041]` (реестр: docs T-031 зависит от T-041, не T-040).
- `T028 [US4 / T-064]` требует `T029 [US5 / T-040]`.
- `T031 [US5 / T-042]` требует `T035 [US6 / T-050]`.
- `T039 [US6 / T-061]` требует `T024 [US4 / T-030]`.
- `T043 [Pilot / T-072]` требует всей P0-цепочки: T023, T025, T026, T031, T033, T034, T035, T039, T041.

### Внутри каждой задачи

- Миграции/схема → Flow → порты (Foundational).
- Модели/контракты → сервис → endpoint/CLI.
- Реализация → интеграция.
- DoD (`ruff` + `mypy --strict` + `pytest`) зелёные до перехода к следующей задаче.

### Parallel Opportunities

- Foundational: после T004 задачи T005 и T006 можно вести параллельно (разные файлы).
- US2: T015 → T014 (реестр: docs T-011 зависит от T-012); затем T016 → T017.
- US3: T019 и T020 параллельны; затем T021/T022/T023.
- US5: T029 и T030 параллельны.
- US6: T035, T037, T038 параллельны.
- Polish: T045–T055 в основном параллельны (разные модули), при наличии зависимостей из Phase 9.

---

## Sequential Example: User Story 2

```bash
# Последовательно (реестр: docs T-011 зависит от T-012):
Task: "T015 [US2] ContextBundle + KnowledgePort/ExecutionPort — src/dark_factory/context/"
Task: "T014 [US2] Профили ядра Product/Develop/Quality + skills — src/dark_factory/agents/"
Task: "T016 [US2] Native SDD Core + SDDPort — .factory/"
Task: "T017 [US2] Specification gate — src/dark_factory/quality/gates/"
```

## Parallel Example: User Story 3

```bash
Task: "T019 [US3] HarnessPort + PydanticAI-адаптер — src/dark_factory/adapters/harness/"
Task: "T020 [US3] Quality — независимая приёмка — src/dark_factory/quality/"
```

---

## Implementation Strategy

### MVP First (User Story 1)

1. Phase 1 Setup — выполнено (T001–T003).
2. Phase 2 Foundational — критично, блокирует всё (T004–T006).
3. Phase 3 US1 — walking skeleton (T007–T013).
4. **STOP и VALIDATE**: quickstart §1–§2 — CLI-стадия локально и в CI без LLM, идемпотентный повтор.

### Incremental Delivery

1. Foundational → US1 → demo (MVP: одна стадия без LLM).
2. US2 → согласованная спецификация с version-bound approval.
3. US3 → агентная реализация + review + rework.
4. US4 → автономный MR-контур и merge.
5. US5 → релиз в dev через GitOps + smoke.
6. US6 → контекст, трекер, Console, наблюдаемость.
7. Pilot → 10 задач e2e (SC-001…SC-008), затем P1–P3 расширения.

### Parallel Team Strategy

1. Foundational — вместе.
2. Далее потоки: Core/Agents (US2+US3) ∥ Delivery (US4+US5) ∥ Console/API (US6), с учётом interleaved-рёбер из раздела выше.

---

## Notes

- `[P]` — разные файлы, допускает параллельный запуск с другими задачами волны; не отменяет перечисленные зависимости задачи.
- `[Story]` — трассировка к user story `spec.md`; `[docs T-0NN]` — к реестру `docs/plan.md`. Якорь отсутствует у T007–T013 (implementation slice US1) и T056 (финальная синхронизация) — у них нет отдельных задач в реестре.
- Каждая задача независимо завершаема и проверяема; договорённости — в `contracts/` и `data-model.md`.
- Тесты — часть DoD задачи (не отдельные задачи): unit + contract + integration, включая crash-тесты effect ledger и exhaustiveness таблицы переходов.
- Коммиты — Conventional Commits на английском, только по явной просьбе пользователя (конституция V).
- Останавливаться на checkpoint'ах для независимой проверки истории.
- Избегать: расплывчатых задач, конфликтов по одному файлу, межстадийных зависимостей, ломающих независимость (кроме явно перечисленных interleaved-рёбер).
