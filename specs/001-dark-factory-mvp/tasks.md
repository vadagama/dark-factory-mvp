---

description: "Task list for Dark Factory MVP implementation"
---

# Tasks: Dark Factory MVP

**Feature**: `001-dark-factory-mvp` | **Spec**: [`spec.md`](./spec.md) | **Plan**: [`plan.md`](./plan.md)

**Input**: Design documents from `/specs/001-dark-factory-mvp/`

**Prerequisites**: `plan.md` (tech stack, structure), `spec.md` (user stories P1–P6), `research.md`, `data-model.md`, `contracts/`, `quickstart.md`, `.specify/memory/constitution.md` v3.0.0

**Тесты**: Отдельные тест-задачи не генерируются — спецификация не запрашивает TDD. Тесты входят в DoD каждой задачи (`ruff` + `mypy --strict` + `pytest`, см. `AGENTS.md`, «Definition of Done») и исполняются как часть реализации (unit, contract, integration, crash-тесты).

**Организация**: Задачи сгруппированы по user stories из `spec.md` для независимой реализации и проверки. Якорь `[docs T-0NN]` — исторический номер задачи из реестра `docs/plan.md` (реестр удалён 2026-09-17; номера сохранены как provenance в ADR, `docs/tech-dept.md` и коде); ID здесь (`T0NN`) — порядок исполнения в этом файле и не совпадает с историческим `T-0NN`. Единственный актуальный трекер — этот файл.

**Traceability**: `T0NN` (этот файл) ↔ исторический `T-0NN` (реестр `docs/plan.md`, удалён). Ни одна задача исторического реестра P0–P3 не выпадает.

## Формат: `[ID] [P?] [Story] Описание`

- **[P]**: можно выполнять параллельно с другими задачами той же волны (разные файлы); перечисленные в задаче зависимости всё равно должны быть выполнены до её старта
- **[Story]**: `[US1]`…`[US6]` — принадлежность к user story `spec.md`
- Каждая задача содержит конкретный путь к файлу и ссылку на контракт/ADR

## Path Conventions

- Модульный монолит (src-layout, ADR-002): код — `src/dark_factory/<модуль>/`, тесты — `tests/{unit,contract,integration}/`.
- Миграции — `migrations/`; Helm — `charts/dark-factory/`; CI — `.github/workflows/`; деплой — `deploy/`; пакеты — `packs/`.
- Документы фичи — `specs/001-dark-factory-mvp/`.

**Состояние на старт**: `T001`–`T003` (`docs T-001`…`T-003`) уже выполнены (✅ 2026-09-13), остальные — в работе.

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Решения, каркас репозитория и доменные контракты. Выполнено до этой итерации.

- [x] T001 [docs T-001] Принять и зафиксировать ADR-пакет по открытым архитектурным вопросам (ADR-002…ADR-019), включая стек, PostgreSQL state store, merge policy, трекер, UI pack, границы репозиториев — `docs/adr/`
- [x] T002 [docs T-002] Scaffolding репозитория и базовый CI: пакет `dark_factory` (src-layout, 8 модулей HLD), `pyproject.toml` (uv, ruff, mypy strict, pytest), тест границ импортов (ADR-015 §3), pipeline lint+typecheck+test — `pyproject.toml`, `src/dark_factory/`, `tests/`, `.github/workflows/ci.yml`
- [x] T003 [docs T-003] Доменная модель изменения: `Change`, `ChangeRun`, `StageRun`, immutable `StageResult`, `NextAction` (закрытое объединение из 8 вариантов), `ArtifactRef`, `Evidence`, `ChangeRequestRef`/`RepositoryRef`, `Decision`, `Finding`, `GateResult`, `Usage`, `BudgetSnapshot`, `RunManifest`/`RunRecord` + `schema_version`; таблицы переходов run/stage (`apply_status`), инварианты завершения (ADR-009 §9), key-функции ADR-006 §3, JSON/YAML round-trip — `src/dark_factory/changes/`

**Checkpoint**: Каркас и контракты готовы — можно начинать Foundational.

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Общее ядро, блокирующее все user stories: схема состояния, детерминированный Flow, порты и фейки.

**⚠️ CRITICAL**: Ни одна user story не начинается до завершения этой фазы.

- [x] T004 [docs T-006] Схема состояния PostgreSQL и Alembic-миграции: таблицы `execution`, `stage`, `attempt` со `state_revision`; `execution_leases` (`resource_type`, `resource_id`, `owner_id`, `fencing_token` монотонный, `acquired_at`, `expires_at`, `heartbeat_at`); `outbox` (envelope: `eventId`, `eventType`, `eventVersion`, `occurredAt`, `changeId`, `runId`, `stage`, `aggregateId`, `aggregateVersion`, `correlationId`, `causationId`, `artifactRefs`, `payload`) и `event_delivery` (`event_id`, `consumer_id`, `status`, `attempts`, `next_attempt_at`, `last_error`); durable effect ledger (`effect_key` уникален, статусы `planned/in_progress/succeeded/unknown`, `external_ref`); `usage`-агрегаты. Инварианты покрыть тестами: уникальность `operation_key`/`effect_key`, уникальность пары (`event_id`, `consumer_id`), монотонность `fencing_token`, атомарность «изменение состояния + запись в outbox»; crash-тесты `crash-before-call` и `crash-after-call-before-commit` не создают второй внешний эффект (ADR-004, ADR-006 §3, ADR-016) — `migrations/`, `src/dark_factory/orchestration/state/`, `tests/integration/`
- [x] T005 [docs T-004] Factory Flow: стадии (`specification → planning → construction → review_verification → release`), 7 гейтов MVP (`specification`, `planning`, `code`, `ui`, `review`, `verification`, `release`), маршруты `quick`/`standard`, TaskGraph (pydantic-graph) только внутри стадии, меж-job продолжение через `StageResult`/`NextAction`, лимиты rework/бюджета на уровне Flow, стоп-условия → `Blocked`. DoD: таблица переходов покрыта целиком; обработчики всех 8 вариантов `NextAction` проходят exhaustiveness (`assert_never`); переход вне таблицы невозможен в рантайме; мёртвых рёбер нет (ADR-005) — `src/dark_factory/orchestration/`, `src/dark_factory/flows/`, `src/dark_factory/rules/`
- [x] T006 [docs T-005] Порты (`Protocol`) и in-memory фейки: `RepositoryPort`/`MergeRequestPort`/`PipelinePort` (единый `ChangeRequestRef`, ADR-019), `TrackerPort`, `HarnessPort`, `ArtifactStorePort`, `TelemetryPort`, `WorkflowEnginePort` (`start`/`resume`/`cancel`/`get_status` с `idempotency_key` и `expected_revision`), `ReconciliationService`, `EventPublisherPort`. Все mutating-методы принимают `idempotency_key`/`effect_key` и возвращают `external_ref`. DoD: contract-тесты каждого порта на фейках; ни один модуль ядра не импортирует внешний SDK (ADR-015 §3); `KnowledgePort`/`ExecutionPort` (T015), `CIPort` (T024), `SDDPort` (T016) вводятся в своих задачах — не авансом — `src/dark_factory/ports/`, `src/dark_factory/adapters/`, `tests/contract/`, `tests/test_import_boundaries.py`

**Checkpoint**: Схема, Flow и порты готовы — user stories можно начинать параллельно.

---

## Phase 3: User Story 1 - Исполнение одной стадии фабрики (walking skeleton) (Priority: P1) 🎯 MVP

**Goal**: Оператор запускает одну стадию (без LLM) локально через CLI и в CI, получает типизированный `StageResult` и запись запуска; процесс не требует постоянно живущего сервиса.

**Independent Test**: `uv run factory doctor --json` и `uv run factory stage run --change ./fixtures/chg_smoke.yaml --stage construction --json` (quickstart §2) дают валидный `StageResult` (`schema_version: 1`); повтор с той же `operation_key` не создаёт второй внешний эффект; отключённый LLM-эндпоинт не мешает детерминированным шагам.

### Реализация для User Story 1 (слой `cli/` поверх Foundational)

- [x] T007 [P] [US1] Factory Runner CLI: точка входа и разбор команд `stage run`, `stage resume`, `run status`, `reconcile`, `outbox dispatch`, `doctor` по контракту `contracts/cli.md` (опции `--change`, `--stage`, `--route`, `--input-revision`, `--run-id`, `--json`, `--evidence-dir`, `--non-interactive`) — `src/dark_factory/cli/__main__.py`, `src/dark_factory/cli/main.py`
- [x] T008 [US1] `factory doctor`: проверка окружения и конфигурации, отсутствие секретов в выводе, exit-код 2 при невалидной конфигурации — `src/dark_factory/cli/doctor.py`
- [x] T009 [US1] `factory stage run`: фиксация снапшота входа до агентной работы (FR-001), вычисление `input_revision` → `operation_key` (ADR-006 §3), исполнение стадии, сериализация immutable `StageResult` (`schema_version: Literal[1]`, `status` только `waiting|succeeded|failed|blocked`), маппинг exit-кодов 0/10/20/1/2 — `src/dark_factory/cli/stage.py`
- [x] T010 [US1] Детерминированные шаги стадии без LLM: сборка контекста, машинные проверки, агрегация, решение о доработке (детерминированный путь не обращается к harness) — `src/dark_factory/orchestration/stages/`
- [x] T011 [US1] Персистенция записи запуска: `RunRecord` (`schema_version` + `manifest` + `change` + `run` + `stage_results` + `decisions`) в `--evidence-dir`/артефакт; immutable refs, без `latest` (ADR-015 §5) — `src/dark_factory/cli/run_records.py`
- [x] T012 [US1] Идемпотентность запуска: повтор `stage run` с той же `operation_key` не создаёт второй внешний эффект; `find_existing` перед созданием, сверка эффекта перед повтором (FR-017, ADR-006 §3) — `src/dark_factory/orchestration/idempotency.py`, `tests/integration/`
- [x] T013 [US1] Fixture и паритет локально/CI: `fixtures/chg_smoke.yaml`; job, запускающий тот же релиз ядра тем же CLI (US1 scenario 2), `--non-interactive` в CI; `doctor` + `stage run` в pipeline — `fixtures/chg_smoke.yaml`, `.github/workflows/ci.yml`

**Checkpoint**: US1 полностью функциональна и проверяется независимо (quickstart §2).

---

## Phase 4: User Story 2 - От задачи к согласованной спецификации (Priority: P2)

**Goal**: Задача нормализуется в снапшот, Product публикует MR с proposal'ом изменения; реализация разрешена только для согласованной ревизии спецификации.

**Independent Test**: Подать задачу → получить MR с ChangeSet'ом (`.factory/changes/<year>/<id>/`) → согласовать конкретную ревизию (SHA) → убедиться, что `construction` без approval не стартует, а смена ревизии инвалидирует approval.

- [x] T014 [US2] [docs T-011] Профили ядра Product/Develop/Quality и минимальные skills (`intake`, `requirements-refinement`, `spec-авторинг`, `implementation`, `implementation-rework`, `code-review`, `acceptance-verification`, `change-request`); версионированные манифесты (входы/выходы, tools, ограничения, stop-conditions) + контракт `AgentProfile → TaskEnvelope → AgentResult` (ADR-007) — `src/dark_factory/agents/profiles/`, `src/dark_factory/agents/skills/`. Зависит от T015 (docs T-012) и T019 (docs T-010 — harness, US3, interleaved: T019 до T014).
- [x] T015 [P] [US2] [docs T-012] `ContextBundle`: сбор источников (`repo`, `specs/` до T-020 → `.factory/` после, конституция, ADR, engineering pack) с provenance, revision/hash; версионирование набора на запуск; изолированный worktree. Ввести `KnowledgePort` и `ExecutionPort` (contract-тесты на фейках); bundle воспроизводим — одинаковый hash на одинаковых входах — `src/dark_factory/context/`, `src/dark_factory/ports/`
- [x] T016 [US2] [docs T-020] Контрольная точка Native SDD Core (ADR-020, `docs/sdd-native-core.md`): Product Baseline `.factory/` (`product/` + `changes/`), ChangeSet с манифестом `change.yaml` (workflow profile, risk_class) и дельтой `spec/delta.yaml` (`add/modify/supersede/retire`), reconciliation к baseline; `SDDPort` с `NativeChangeSetAdapter` (основной), `SpecKitAdapter` (bootstrap-импорт legacy `specs/`) и `OpenSpecAdapter` (compatibility, ADR-020 п.8); строгость по типу задачи (fix — без SDD-артефактов); `.specify/` и `specs/` — read-only bootstrap evidence. Зависит от T005, T014 — `src/dark_factory/context/sdd/`, `.factory/` (реализовано: модуль `context/sdd/` — Pydantic v2 модели артефактов change/v1, delta, task-graph/v1, requirement/v1 frontmatter, verification/evidence/reconciliation с YAML round-trip; таблица переходов lifecycle draft→…→closed с единой точкой валидации; политика строгости по profile × risk_class (`bugfix-r0` — пустой набор, R2+ — обязательный reconciliation); `SDDPort` в `ports/protocols.py` по контракту ports.md с реэкспортом; `NativeChangeSetAdapter` (create/read/apply_delta, guard ожидаемого baseline revision — детекция параллельных изменений), `SpecKitAdapter` (read-only импорт legacy `specs/` с делегацией нативному адаптеру), `OpenSpecAdapter` (конвертер ADDED/MODIFIED/REMOVED ↔ add/modify/retire); начальный Product Baseline `.factory/` (factory.yaml + product/, revision — детерминированный sha256 content-hash); нормализованный ChangeSet-контракт с 5 осями как данные (гейт — T021); unit + контрактные тесты SDDPort на трёх адаптерах. ruff + ruff format + mypy strict + pytest — зелёные (586 passed + 13 skipped без БД))
- [x] T017 [US2] [docs T-021] Specification gate: детерминированные проверки (наличие AC, трассировка AC → сценарий → задача, противоречия scope/out-of-scope, связность с конституцией), blocking/non-blocking; результат фиксируется как `GateDecision` (policy и версия, revision ChangeSet, результат, evidence, объяснение, агент/человек, разрешённый override — ADR-020); поле `risk_class` из Implementation Contract (T023) в нормализованном контракте ChangeSet. Зависит от T016 (T-020), T023 (T-016, US3 — interleaved) — `src/dark_factory/quality/gates/` (реализовано: `evaluate_specification_gate` поверх `normalize()` — находки 5 осей как данные, гейт взвешивает; обязательные артефакты и R2+-reconciliation пересчитываются по эффективному риск-классу из Implementation Contract (повышение — информационная находка, понижение без политики — блокирующая); AC-наличие и связка AC→сценарий — каноническая проверка `acceptance-scenario` в verification plan, трассировка →задача — coverage-ось (`requirement_without_task`); противоречия scope in/out без учёта регистра; классификация blocking/non-blocking — данные `SpecGatePolicy` (12 blocking-кодов; `risk_class_raised` и `missing_evidence_for_accepted_change` — non-blocking); `GateDecision` (`dark-factory.dev/gate-decision/v1`: policy+версия, changeset_id+revision, эффективный risk_class, результат passed/failed, находки с blocking-меткой, evidence-ссылки, объяснение, `decided_by=policy`, human-only override через `apply_override` — только для failed); persist в `gates/specification.yaml`; качество не зависит от orchestration (порядок риск-классов продублирован локально, граница импортов важнее DRY); 16 тестов. ruff + format + mypy strict + pytest — зелёные (602 passed + 13 skipped без БД), CI на MR зелёный)
- [x] T018 [US2] [docs T-022] Product Baseline/шаблоны ChangeSet для продуктовых репозиториев (`.factory/`: `factory.yaml`, `product/`, `changes/`; frontmatter-профили артефактов): правила минимальных изменений и evidence-based validation; проверка через `NativeChangeSetAdapter` (ADR-020 п.8). Зависит от T016 (T-020), T041 (T-070) — `packs/` (реализовано: пак `packs/product-baseline/` — `baseline/` (factory.yaml + product/ с примерами нод requirement/scenario/adr/capability в минимальном frontmatter §7-8, все active) и `changeset/` (канонический скелет ChangeSet R1/product-feature/draft: 6 обязательных слотов без reconciliation, delta add с AC в body, task-graph с трассировкой satisfies, verification plan с unit-test + acceptance-scenario, пустой evidence-индекс); placeholder-ревизия (строка из 64 нулей) в change.yaml и spec/delta.yaml заменяется на актуальную `current_revision()` при применении; `rules.md` — правила минимальных изменений (принцип II конституции: дельта add/modify/supersede/retire вместо копии спецификации, baseline только active/superseded/retired, прямые правки baseline мимо ChangeSet запрещены) и evidence-based validation (принцип III: фактические проверки, evidence в evidence/index.yaml, наличие артефакта ≠ пройденный гейт — GateDecision в gates/specification.yaml); README с порядком применения и правилом «пустые каталоги не создаются»; проверка через `NativeChangeSetAdapter` — `tests/test_packs_product_baseline.py`: baseline-шаблон валиден (FactoryManifest + frontmatter всех нод), changeset-шаблон соответствует строгим схемам (CHANGE_ID_PATTERN, ровно 6 слотов, консистентность delta↔manifest), e2e: копия baseline → read_change → спецификационный гейт PASSED → read_requirements → apply_delta (новая ревизия, REQ в product/ со status active, reconciliation/result.yaml) → повторный apply_delta кидает BaselineMismatchError; применение пака к пилотному репо — за T041 (T-070). ruff + format + mypy strict + pytest — зелёные (605 passed + 13 skipped без БД), CI на MR повторяет проверки)

**Checkpoint**: US2 проверяется независимо — реализация блокируется без version-bound approval.

---

## Phase 5: User Story 3 - Агентная реализация с независимым review и ограниченным rework (Priority: P3)

**Goal**: На согласованной спецификации — реализация в изолированном workspace, независимый review отдельным контекстом, ограниченный rework в том же MR, машинные проверки на итоговом SHA.

**Independent Test**: На согласованной спецификации одного небольшого изменения пройти цикл реализация → review → rework → зелёный CI; проверить лимит раундов (≤3) и параллелизм ≤2.

- [x] T019 [P] [US3] [docs T-010] `HarnessPort` + PydanticAI-адаптер: model/tools/structured output, учёт usage (токены, стоимость), ограничения инструментов по профилю, LLM endpoint через существующий LiteLLM (ADR-002 §2, R-17) — `src/dark_factory/adapters/harness/`
- [x] T020 [P] [US3] [docs T-013] Quality — независимая приёмка: контекст строится заново из закреплённой спецификации, актуального diff и evidence (не наследует Develop); `Finding` (severity/category/evidence/confidence); `GateResult` pass/fail/blocked для конкретного SHA; blocking/non-blocking; блокирующий finding останавливает merge; человеческие замечания MR включаются в общий набор `Finding` как данные, без влияния на права и гейты (FR-007) — `src/dark_factory/quality/` (реализовано: модуль `quality/acceptance.py` — `build_acceptance_context` собирает свежий `ContextBundle` из `SpecMaterial`/`DiffMaterial`/`EvidenceMaterial` с sha256-provenance (дубликаты evidence — ValueError; `SourceKind.EVIDENCE` добавлен в context/bundle, hash воспроизводим, `retrieved_at` не входит); `human_comment_findings` нормализует комментарии MR в `Finding(origin=human, severity=info, status=open)` — текст не интерпретируется, `required_action` пуст, полный текст/автор — в evidence-артефакте комментария; `classify_findings` → blocking (open + severity в политике, default BLOCKER) / non_blocking / stale (чужой `reviewed_sha` — новый SHA аннулирует прежний review); `evaluate_review_gate` → `GateResult(REVIEW, passed|failed, sha, summary, evidence_ids)`; поле `confidence` (0.0–1.0, optional) в `Finding` (changes/findings.py) — документирует уверенность источника, вес не меняет; блокирующий finding останавливает merge через существующую политику flow (failed review-гейт в `MergeAction` → Blocked, доказано интеграционным тестом `test_blocking_finding_stops_merge`; контрольные тесты: merge проходит при passed-гейте и при только-human findings). ruff + format + mypy strict + pytest — зелёные (622 passed + 13 skipped без БД))
- [x] T021 [US3] [docs T-014] Bounded rework loop: `review → rework → re-review` с лимитом 3; новый attempt не перезаписывает прежнее состояние; новый SHA аннулирует прежний допуск; исчерпание лимита/повторяющаяся ошибка/конфликт требований → `Blocked` с диагностикой (условия эскалации — подмножество ADR-018 §5). Зависит от T005, T020 — `src/dark_factory/orchestration/` (реализовано: модуль `orchestration/rework.py` — чистая детерминированная loop-политика уровня над переходами flow (flow.py, rules/, changes/, quality/ не изменены; зависимости — только `changes.*`, `rules.limits`, `orchestration.policy.escalation`); `FindingSignature` — frozen-hashable идентичность блокирующего finding (origin, category, file, line) через `finding_signature()`: id/role/reviewed_sha не входят — id перегенерируется каждым проходом review, SHA меняется каждый раунд; `ReviewPass` — frozen-запись завершённого прохода review (round — номер раунда цикла: начальный review = 1, re-review после rework-раунда k = k+1; sha; blocking — кортеж сигнатур; escalations) в append-only истории: новый attempt добавляет запись, прежние неизменяемы (FR-008); `plan_rework(budget, passes)` → `ReworkDecision(outcome=rework|blocked, round, max_rounds, reason, signatures)` — чистая функция (budget/passes не мутируются; пустая история и последний проход без блокирующих — ValueError); детерминированный порядок stop-условий, первое срабатывание → Blocked (подмножество ADR-018 §5): эскалации последнего прохода (`escalation_stop_reason` — эскалация не расходует раунд, конфликт требований REQUIREMENTS_DEFICIENT останавливается здесь) → лимит rework (`rework_violation` с requested_round = used+1 — счётчик бюджета авторитетен, FR-016) → тот же SHA у двух последних проходов («rework не произвёл новый commit — прежний допуск на SHA остаётся, re-review невозможен»; механика «новый SHA аннулирует прежний допуск»: проход без нового SHA ничего не отменяет) → повторяющаяся ошибка (множество блокирующих сигнатур не изменилось после раунда) → иначе rework round = used+1, reason перечисляет блокирующие находки (детерминированная сортировка (origin, category, file, line)); решения о прошлых проходах не переиспользуются как допуск — вердикт прохода действителен только для своего SHA, повтор — stop-условие, а не допуск (ADR-018 §5: исправимые findings возвращаются в цикл, повтор/исчерпание — эскалация Blocked); классификация blocking остаётся у вызывающего (T020, quality не импортируется — направление зависимости сохранено); wiring в stages/executor — за следующими задачами; 17 тестов в `tests/test_orchestration_rework.py`. ruff + format + mypy strict + pytest — зелёные (639 passed + 13 skipped без БД))
- [x] T022 [US3] [docs T-015] TaskGraph внутри стадии: branch/join для ≤2 независимых подзадач, агрегация через reducers, конкурентная запись в state через типизированные outputs. Зависит от T005 — `src/dark_factory/orchestration/` (реализовано: модуль `orchestration/taskgraph.py` — типизированный фасад над pydantic-graph 2.43 (GraphBuilder fork/join, ADR-005): `TaskGraph[InputT, OutputT, AggregateT](name, reducer, initial_factory)` + `add_branch(Branch(name, call))` + `await run(inputs) -> AggregateT`; ≤2 веток — третья отклоняется на этапе конструирования (`TooManyBranchesError`, FR-005), дубликат имени/0 веток — ValueError; ветка — async-подзадача, получает только вход (иммутабельный по контракту) и возвращает frozen-выход — API не имеет разделяемого state, мутационное разделение невыразимо, агрегат персистит вызывающий ровно один раз; детерминизм: выход помечается индексом объявления, join буферизует в порядке завершения, финальный шаг проверяет полноту набора индексов (инвариант «join не теряет результаты» → RuntimeError) и сворачивает reducer в порядке объявления — порядок агрегата от порядка завершения не зависит; политика отказов — fail-fast: первая упавшая ветка прерывает run исходным исключением (не ExceptionGroup), соседняя отменяется, агрегат не создаётся; `initial_factory` — свежий агрегат на каждый run. pydantic-graph объявлен явной зависимостью (`pydantic-graph>=2.43,<3`, резолюция не изменилась — pydantic-ai-slim 2.43.0 pins ==2.43.0). Ограничение: mypy 2.3.1 не резолвит TypeVar'ы pydantic-graph (typing_extensions/infer_variance на py3.12) — классы библиотеки используются без параметризации с точечными type: ignore/cast, контракт типов несут сигнатуры фасада (кандидат в tech-debt: обновление typing_extensions вернёт проверяемость дженериков pydantic-graph). Wiring в stages/executor — за следующими задачами; 16 тестов в `tests/test_orchestration_taskgraph.py`. ruff + format + mypy strict + pytest — зелёные (655 passed + 13 skipped без БД))
- [x] T023 [US3] [docs T-016] Implementation Contract и границы автономной реализации (ADR-018): (1) схема контракта (scope, acceptance criteria, архитектурные ограничения, UI evidence, риск-класс R0–R4, бюджет, правила эскалации); (2) классификатор Known/Bounded choice/New path; (3) машинно проверяемые условия эскалации (публичный API, схема данных, IAM, архитектурная граница, превышение бюджета, необратимая операция); (4) режимы участия человека по фазам → `human_gates` flow-профиля; (5) минимальный контракт риск-класса (LLM может повысить, понизить — только политика). DoD: изменение без утверждённого контракта не проходит в реализацию. Зависит от T005, T014 — `src/dark_factory/orchestration/policy/`, `src/dark_factory/agents/profiles/` (реализовано: схема контракта и `EscalationViolation` в `changes/`; 10 проверок эскалации, классификатор, риск-монотонность и таблица участия в `orchestration/policy/`; `HUMAN_GATES` в `flows/routes.py`; гейт входа в construction и бюджет автономии — в `flow.apply_result`, `agents/profiles/` не менялись)

**Checkpoint**: US3 проверяется независимо — машинные проверки зелёные на итоговом SHA, лимиты срабатывают детерминированно.

---

## Phase 6: User Story 4 - Автономный MR-контур и merge по правилам (Priority: P4)

**Goal**: При включённом расписании сверки фабрика сама продолжает активные инициативы (CI/MR/замечания), запускает разрешённые попытки; merge — доверенный финализатор по правилам.

**Independent Test**: Включить reconcile и merge policy; провести инициативу от реализации до merge без ручного запуска промежуточных шагов; изменения вне разрешённого класса требуют человека; повтор webhook не создаёт дубль MR.

- [x] T024 [US4] [docs T-030] GitHub-адаптер (первый провайдер MVP): `GitHubAdapter` реализует `RepositoryPort`/`MergeRequestPort`/`PipelinePort` и `CIPort`; ветки/worktree, push, CR (`ChangeRequestRef`), diff, комментарии/review, Checks API, rebase, merge через finalizer; GitHub App с короткоживущими installation tokens; webhooks — ускоритель. DoD: единая контрактная сюита зелёная + интеграционный тест против тестового репозитория (ADR-019). Зависит от T006 — `src/dark_factory/adapters/scm/github/` (реализовано: `CIPort` в `ports/protocols.py` по контракту ports.md + `StageJobRequest`/`stage_gate()` в `ports/common.py`; `GitHubAdapter` в `adapters/scm/github/` — `RepositoryPort` (get_revision, ensure_branch c идемпотентностью по ключу — семантика фейка 1:1), `MergeRequestPort` (open с дедупликацией по head-ветке, find_existing по change-маркеру в body переживает рестарт, add_comment идемпотентен через невидимый маркер-комментарий, merge — сверка head.sha с expected_sha → `HeadMismatchError`, squash, replay уже слитого — no-op), `PipelinePort` (check-runs/actions-runs → `PipelineStatus`, нет запусков → queued/None как у фейка), `CIPort` (workflow_dispatch, детерминированный `job_ref` `github:<slug>:<stage>:<run_id>`, маппинг conclusion → `GateStatus` fail-closed, артефакты → `ArtifactRef`); GitHub App auth: RS256 JWT (PyJWT) → короткоживущий installation token с кэшем по сроку и refresh по 401, токен-провайдер инъецируемый для тестов; фейк `FakeCI` (fakes/ci.py); контрактная сюита параметризована fake|github через stateful эмулятор GitHub API на `httpx2.MockTransport` (tests/contract/github_api.py) — тест-модули сюиты не менялись; новые suite `tests/contract/test_ci_port.py` (9×2), юнит `tests/test_github_auth.py` (15), integration `tests/integration/test_github_adapter.py` (skip без `DARK_FACTORY_TEST_GITHUB_*`); зависимости `httpx2>=2.12` (уже транзитивно в графе через openai/genai-prices — единый HTTP-клиент) и `pyjwt[crypto]>=2.9`; webhooks/push/diff/rebase — вне контракта портов, отложены (задокументировано в docstring модулей). ruff + format + mypy strict + pytest — зелёные (693 passed + 14 skipped), CI на MR повторяет проверки)
- [x] T025 [P] [US4] [docs T-031] CI-шаблоны фабрики (agent jobs): один job = одна стадия Flow, временный pod, сохранение `StageResult`/`NextAction`/usage в artifacts + run record, детерминированные jobs (build/test/lint/security), повторный проход гейтов на итоговом SHA. Зависит от T005, T024, T030 (T-041, US5 — interleaved) — `.github/workflows/`, `src/dark_factory/cli/`, `tests/` (реализовано: reusable workflow `.github/workflows/factory-stage.yml` (`workflow_call`, one job = одна стадия Flow: checkout на итоговый SHA — FR-009/SC-004, `factory doctor`, `factory stage run --json --non-interactive --evidence-dir factory-evidence` с shell-гейтом exit-кодов cli.md — 0/10 job-continuing, waiting ≠ ошибка; артефакт stage_result.json + factory-evidence/ (снапшот, run_record.json) с retention-days и `if: always()` — evidence сохраняется и при blocked/failed; outputs status/next_action/run_id/change_id/usage (токены/стоимость); permissions `contents: read`, без секретов — FR-023; MVP-изоляция — GitHub-hosted эфемерные раннеры, T030 (T-041) сменит runs-on на self-hosted K8s pod); job-script `src/dark_factory/cli/ci_job.py` — валидация StageResult зеркально us1-parity (schema_version=1, результатный статус, непустой next_action.type), рендер outputs `$GITHUB_OUTPUT`, `::error::`-аннотация с коротким stop-reason без секретов (ADR-009), exit 0 для waiting (не ошибка) / 20 blocked / 1 failed / 2 invalid input; `ci.yml` — job `checks` разбит на детерминированные lint/typecheck/test/build/security (postgres-сервис и pytest без изменений; security — pip-audit по locked-набору `uv export --frozen --no-emit-project --no-dev` + `uvx pip-audit`, без новых зависимостей), все checkout на итоговый SHA, `factory-us1-parity` сохранён, dogfood `factory-stage-smoke` вызывает шаблон на `./fixtures/chg_smoke.yaml` (construction) на каждом MR; 53 теста в `tests/test_cli_ci_job.py` (5 exit-кодов → outcome, валидация, outputs, аннотации, waiting → 0). ruff + format + mypy strict + pytest — зелёные (746 passed + 14 skipped без БД/GitHub-интеграций), YAML обоих workflow провалидирован, сквозной прогон stage run → ci_job → outputs локально воспроизведён
- [x] T026 [US4] [docs T-032] Merge policy MVP: protected branch, обязательные human approvals, обязательные checks на итоговом SHA, squash merge, запрет merge при непройденных гейтах; проверка неизменности ожидаемого SHA перед merge (FR-011); publisher/finalizer отделены от sandbox-исполнения агентов. Зависит от T024, T020 — `src/dark_factory/orchestration/policy/`, `src/dark_factory/rules/` (реализовано: `orchestration/policy/merge.py` — детерминированная merge-политика без I/O: `MergePolicy` (auto-merge риск-классы — по умолчанию пусто = ручной режим FR-010, squash-only T-032, гейт merge-авторизации REVIEW = `HUMAN_GATES`), `MergeRequestContext` (executor human|trusted_finalizer|agent, риск-класс, expected/head SHA, human approvals, gate results), `MergeDecision` (blocked / manual_merge_required / human_merge_authorized / finalizer_merge_allowed) и `evaluate_merge` с фиксированным порядком проверок — первая причина побеждает: ① executor=agent → blocked (FR-004/FR-023 — у agent jobs нет merge-полномочий, DoD); ② SHA неизвестны или expected ≠ head → blocked (FR-011); ③ обязательные гейты не удовлетворены результатами на итоговом SHA (результат на старом/неизвестном SHA — устаревший, последний по гейту выигрывает — зеркально `rules.gates`, ADR-009 п.7) → blocked; ④ нет human-решения на гейте REVIEW, привязанного к итоговому SHA (`Decision.commit_sha` — version-bound approval, контракт API T-035), либо последнее — не APPROVED → manual (новый SHA инвалидирует прежний допуск, ADR-009 п.7); ⑤ human → authorized; ⑥ trusted_finalizer + риск-класс явно в разрешённых → allowed с merge_method=squash — финализаторный путь тоже требует SHA-привязанного human approval: human release gate ADR-011 п.2 держится и на auto-merge пути до T-085; ⑦ иначе manual (FR-010). Врезка в flow: `apply_result` получил keyword-only `merge_context` (сигнатура обратно совместима); обработчик MergeAction — сперва прежние stage-гейты/эскалации (причины остановок сохранены), затем политика: blocked → `_stop` (Blocked), manual → run/stage в WAITING c `WaitForInputAction("waiting for human merge (ADR-011 p.2): …")` — повторное применение результата с approval продвигает, authorized → advance to release; `merge_context=None` = manual (fail-safe: на отсутствующих данных машина не мержит); контекст якорится к run/result через `dataclasses.replace` (route/stage/gate_results авторитетны — вызывающий не может сформировать набор гейтов); шов `MERGE_POLICY` в flow — точка конфигурации T-085. `rules/merge_protection.py` — требования branch protection/rulesets как данные без сети: `MergeProtectionPolicy` (protected branch, ≥1 обязательный human approval, обязательные checks на итоговом SHA, squash-only, dismiss stale approvals — провайдерский аналог инвалидации допусков новым SHA) + `protection_violations(ObservedBranchProtection)` — детерминированный список нарушений в фиксированном порядке (наблюдения подаст адаптер T-030, исполнение — на стороне провайдера). `changes/findings.py`: `Decision.commit_sha: str | None` (None = unbound — никогда не авторизует merge на конкретный SHA). Тесты: 21 политика (все ветки, включая «approval на старом SHA не авторизует новый»), 11 branch protection, врезка в flow (merge без approval → WAITING, с approval → release, SHA mismatch → Blocked, agent executor → Blocked); существующие merge-тесты обновлены на авторизованный контекст; `test_blocking_finding_stops_merge` сохранён без изменений. ruff + format + mypy strict + pytest — зелёные (789 passed + 14 skipped без БД/GitHub-интеграций))
- [x] T027 [US4] [docs T-063] Reconcile и recovery: идемпотентный CronJob (2–5 мин, `concurrencyPolicy: Forbid`), восстановление по PostgreSQL/Git/MR/StageResult, правила (истёкшая блокировка, дубликаты запусков, merged MR без run, approved+passed без merge, branch behind, повторяющаяся ошибка → эскалация); webhook — только ускоритель; разрешение рассинхрона «CI ↔ PostgreSQL» в пользу PostgreSQL после сверки. Зависит от T025, T024 — `src/dark_factory/orchestration/reconcile/` (реализовано: пакет `orchestration/reconcile/` — `rules.py`: таблица «аномалия → действие» как данные (`ANOMALY_RULES`, 6 правил в фиксированном порядке, первое срабатывание побеждает; `plan_action` — чистая функция без I/O и часов; `resolve_drift` — сверка CI ↔ PostgreSQL всегда в пользу PostgreSQL, ADR-006 п.9); `models.py`: frozen-журнал прохода `AnomalyKind`/`ReconcileAction`/`RunReconciliation`/`ReconcileReport`; `service.py`: `GlobalReconciler` — один идемпотентный проход в одной транзакции: глобальный PG-lease `("reconciler", "global")` с fencing token (K8s `Forbid` — только оптимизация; параметры CronJob 2–5 мин / `activeDeadlineSeconds=300` — константы `RECONCILE_INTERVAL_MINUTES`/`ACTIVE_DEADLINE_SECONDS`, манифест — T040/T042), скан активных статусов, опциональные швы наблюдения (WorkflowEnginePort, `ReconcileObserver` для Git/MR/StageResult), планирование и применение. Правила: ① повторяющаяся ошибка (два последних attempt одной стадии FAILED) → ESCALATE в Blocked; ② истёкшая блокировка → LEASE_TAKEOVER через семантику LeaseRepository (свежий монотонный fencing token; живой чужой lease → applied=False с note); ③ дубликаты запусков (активные run одного change; канонический — лексикографически минимальный id) → SUPERSEDE не-канонического; ④ merged MR без завершённого run → RECORD_MERGE (→ SUCCEEDED, факт merge не теряется); ⑤ approved+passed без merge → решение существующей merge-политики (T-026) для executor=trusted_finalizer: PLAN_MERGE (squash) / WAIT_FOR_HUMAN (ручной режим, MVP-дефолт) / ESCALATE (нарушение политики; авторизация merge по-прежнему SHA-привязана); ⑥ branch behind → UPDATE_BRANCH для provider wiring. Reconciler исполняет только state-мутации (takeover/supersede/record merge/escalate) с двойной защитой (fencing token + ожидаемая ревизия, проверка таблицы переходов; домен-невалидные — journal-only), агентные задачи не исполняет; PLAN_MERGE/WAIT_FOR_HUMAN/UPDATE_BRANCH — report-only для своих исполнителей; идемпотентность прохода и webhook-независимость зафиксированы тестами и докстрингами. `PostgresReconciliationService` — адаптер порта `ReconciliationService` (контракт портов/моделей не менялся). CLI: `factory reconcile [--json]` (`cli/reconcile.py`) — один проход, отчёт text/JSON, exit 0 (включая `lease_acquired=False` — норма при параллельном запуске) / 2 (БД недоступна; URL и текст исключений не эхо — ADR-009), URL из `DATABASE_URL` (как у doctor), liveness-probe. Тесты: 33 юнит (таблица, приоритеты правил, негативы, детерминизм/сходимость, drift, observe, порт) + 6 CLI + 13 интеграционных против PostgreSQL (takeover с инкрементом token, supersede, record merge, эскалация, идемпотентность второго прохода, глобальный lease, живой чужой lease, drift в пользу PostgreSQL, report-only) — `tests/test_orchestration_reconcile.py`, `tests/test_cli_reconcile.py`, `tests/integration/test_reconcile.py`; стабы `test_cli_parser.py` обновлены. ruff + format + mypy strict + pytest — зелёные (826 passed + 27 skipped без БД; 852 passed + 1 skipped с PostgreSQL), сквозной smoke `factory reconcile --json` против тестовой БД воспроизведён)
- [x] T028 [US4] [docs T-064] Outbox Dispatcher CronJob: единственный владелец доставки; резервирование через `FOR UPDATE SKIP LOCKED`/короткий lease; запуск внутренних обработчиков или CI pipeline; фиксация в `event_delivery`, exponential backoff, `dead` при исчерпании; порядок только в рамках `aggregateId`/`changeId`/`runId` по `sequence`; очистка outbox — только после терминального статуса всех обязательных потребителей и retention. DoD: at-least-once тест (повтор `eventId` не создаёт второй эффект), dead-letter и ручной replay. Зависит от T004 (T-006), T029 (T-040, US5 — interleaved) — `src/dark_factory/orchestration/events/`, `deploy/` (реализовано: пакет `orchestration/events/` — `rules.py`: чистые детерминированные правила без I/O и часов (`now` — явный параметр): backoff `backoff_delay_seconds` (константы `MAX_ATTEMPTS=5`, base 30 с, ×4, cap 3600 с — расписание 30с→2м→8м→32м→dead, ~1 ч до парковки), `is_dead`, `is_undelivered`, `min_undelivered_sequence`, `ordering_eligible` (строгое равенство: не минимальный недоставленный sequence → defer; `dead` блокирует stream до replay/skip, `delivered`/`waived` разблокируют), `is_cleanup_eligible` (все доставки in {delivered, waived} И occurred_at + `RETENTION_DAYS=30` ≤ now; dead запрещает удаление; связь с retention аудита ADR-009 §9 задокументирована; архивация не реализована — задокументировано); `models.py`: frozen-журнал прохода `DeliveryOutcomeKind`/`DeliveryOutcome`/`DispatchReport` + `sort_outcomes` (детерминированный порядок по (event_id, consumer_id)); `handlers.py`: consumer-side SPI `DeliveryHandler` (Protocol, без изменений в `ports/`), `NoOpHandler` (MVP-заглушка — реальные потребители T-063/T-090), `HandlerRegistry` (неизвестный потребитель → NoOp — проход не ломается; CI-pipeline-хук не реализован (YAGNI), реестр — точка расширения); `repository.py`: `DeliveryRepository` рядом с пакетом events (не в `state/repositories.py` — связность доставки, существующие репозитории не тронуты): `reserve_batch` — SELECT … JOIN с `FOR UPDATE OF event_delivery SKIP LOCKED` + lease `LEASE_SECONDS=120` (запись `next_attempt_at`), ordering-gate внутри транзакции резервирования (gap → defer, строка буквально не тронута), `commit_outcome` с guard'ом `status IN (pending, failed)` (не перезаписывает терминальные/операторные состояния при гонке), `replay` (dead/failed → pending, attempts=0, ошибки и backoff сброшены), `skip` (pending/failed/dead → waived, админское решение ADR-016 п.6); `cleanup.py`: удаление только cleanup-eligible событий (FK CASCADE уносит доставки; кандидаты — только старше cutoff); `dispatcher.py`: `OutboxDispatcher.run_pass` — один идемпотентный проход: короткая транзакция резервирования → вызов обработчика строго вне транзакций → отдельная короткая транзакция фиксации исхода (успех → delivered attempts+1; ошибка → attempts+1, last_error ≤1024, backoff → next_attempt_at, исчерпание → dead); crash между вызовом и фиксацией → повторная доставка после lease (at-least-once целевое поведение); опциональный шаг очистки отдельной транзакцией. Решение по stream: `aggregate_id` (уникальный ключ (aggregate_id, sequence) уже в схеме; change/run делят sequence-пространство агрегата) — задокументировано в докстрингах; глобальный порядок не гарантируется. CLI (`cli/outbox.py` по образцу reconcile): `factory outbox dispatch [--json] [--limit N] [--cleanup]` (имя команды — по контракту cli.md `outbox dispatch [--once]`, `--once` сохранён как no-op совместимости — проход всегда один), `factory outbox replay --event-id ID [--consumer ID]`, `factory outbox skip --event-id ID --consumer ID`; exit 0 (включая «делать нечего»), 1 (replay/skip — нет подходящей пары), 2 (БД недоступна/невалидный ввод; URL и текст исключений не эхо — ADR-009); wiring в `cli/main.py`, liveness-probe как у reconcile. Деплой: `deploy/events/outbox-dispatcher-cronjob.yaml` — CronJob ns `factory`, каждые 2 мин (`timeZone: Etc/UTC`), `concurrencyPolicy: Forbid` (только оптимизация — корректность даёт SKIP LOCKED), `activeDeadlineSeconds: 300`, `backoffLimit: 0`, `restartPolicy: OnFailure`, non-root securityContext (runAsUser/Group 65532, seccomp RuntimeDefault, readOnlyRootFilesystem, drop ALL), resources 50m/64Mi → 200m/128Mi, SA `factory-api` из bootstrap (automount=false; выделенный SA — T030/T042), команда `factory outbox dispatch --cleanup`; образ и Secret — плейсхолдеры с явными комментариями (реальный образ — T033/T042; Secret `factory-outbox-dispatcher-database` создаётся вне git, пример в README); `deploy/events/README.md` на русском (назначение, механика, расписание, ручные операции, ограничения). Тесты: 19 юнит (таблица backoff, dead-правило, ordering-eligibility (gap/dead блокируют, delivered/waived разблокируют), cleanup-матрица, детерминизм sort_outcomes, реестр/NoOp) — `tests/test_orchestration_events.py`; 6 CLI (render text/JSON, невалидный URL без эха, недостижимый store, --limit 0, wiring трёх команд) — `tests/test_cli_outbox.py`; стабы `test_cli_parser.py` заменены на парсинг реальных опций (dispatch/replay/skip, missing-required, help — файл теперь 52 теста); 12 интеграционных против PostgreSQL (at-least-once: crash между handler и фиксацией → повторная доставка, дедупликация по event_id даёт ровно один эффект; два перекрывающихся прохода не резервируют одну delivery (SKIP LOCKED) + lease; happy path; failure → backoff 30 с, undue-проход не берёт; исчерпание 5 попыток → dead; replay dead → delivered; gap: seq=2 не доставляется до delivered seq=1, 4 прохода, порядок вызовов; skip dead → waived разблокирует, повторный skip → exit 1; cleanup-матрица (eligible удаляется + FK-каскад, retention/dead/не-все-потребители удерживают), cleanup opt-in; CLI dispatch --json) — `tests/integration/test_events_dispatcher.py`. Валидация: ruff check + ruff format --check + mypy strict — чисто; pytest без БД 857 passed + 39 skipped (интеграции), с PostgreSQL 16 (эфемерный docker-контейнер) 895 passed + 1 skipped; манифест — kubeconform -strict 1/1 valid; сквозной smoke `factory outbox dispatch --json` / `replay` / `skip` / `--limit 0` против тестовой БД — exit 0/1/1/2 без эха URL)

**Checkpoint**: US4 проверяется независимо — дубликаты не создаются, merge вне разрешённого класса требует человека.

---

## Phase 7: User Story 5 - Релиз в dev через GitOps (Priority: P5)

**Goal**: После merge — immutable digest, GitOps-изменение, применение в dev, smoke; статус «выпущено» только при успешном smoke.

**Independent Test**: После merge пилотного изменения проверить цепочку сборка → GitOps MR → deploy → smoke → «выпущено»; провал smoke не даёт статус «выпущено».

- [x] T029 [P] [US5] [docs T-040] Bootstrap локального Kubernetes: Docker Desktop K8s, namespaces (`argocd`, `factory`, `ci`, `factory-runs`, `apps-dev`), quotas, service accounts, baseline NetworkPolicy deny-by-default + негативные egress-тесты; PostgreSQL фабрики (PVC, backup, автоматизированный restore-тест, MVP RPO/RTO) вместе с БД Plane логически раздельно; проверка доступности и учётных данных GitHub App; capacity smoke полного контура (idle + один e2e job). DoD: скрипт воспроизводим на чистом Docker Desktop (ADR-010) — `deploy/bootstrap/` (реализовано: идемпотентный `bootstrap.sh` (флаги --context/--only-license-check/--strict-license/--skip-*) — prereq + детект выключенного K8s в Docker Desktop (settings-store.json KubernetesEnabled=false → точная инструкция + exit 1, настройки пользователя не меняет), best-effort license-check по ADR-010 (warn-only, --strict-license — fail); namespaces ×5 + ResourceQuota/LimitRange ×5 (потолки ≈12.5Gi при concurrency=1, обоснование в README) + 5 service accounts (automountServiceAccountToken=false; RBAC — T030); NetworkPolicy deny-by-default ×19 (deny-all/allow-dns/allow-intra-ns во всех ns, allow-egress-https в factory/factory-runs/ci/argocd с обоснованием allow-листа в манифесте; сужение — T030); PostgreSQL 16.4 StatefulSet (PVC 5Gi, non-root, probes, Secret генерируется openssl при первом запуске и не попадает в git) с двумя логически раздельными БД factory/plane (отдельные роли-владельцы, init-скрипт); backup CronJob 02:30 UTC (pg_dump -Fc обеих БД на PVC 2Gi, retention 7 дней, RPO=24h, RTO≈15min — ручная процедура в README); автоматический restore-test (последний дамп → restore_test_<ts>, сверка таблиц/записей с source, DROP, SKIP без дампов); негативные egress-тесты (DNS ok, 443 ok, HTTP 80/TCP 25 блокируются, factory→postgres ok, cleanup подов); capacity smoke (allocatable узла, kubectl top при наличии metrics-server, e2e-job busybox с cgroup v2-замером peak RAM/CPU, вердикт против профиля 10–12GB); check-github-app.sh (env DARK_FACTORY_GITHUB_APP_ID/_PRIVATE_KEY/_INSTALLATION_ID/_API_URL → RS256 JWT на bash+openssl → installation access token, печать только фактов без секретов; без env — SKIP; --self-test); teardown.sh с подтверждением. README на русском: требования (включение K8s, ресурсы Docker Desktop 10–12GB), флаги, RPO/RTO, ограничения. Валидация: bash -n ×7 и shellcheck -x ×7 (docker koalaman/shellcheck:stable) — зелёные; kubeconform -strict — 49/49 ресурсов валидны; check-github-app.sh --self-test — зелёный; живой сценарий bootstrap.sh --only-license-check против выключенного K8s — корректный детект + инструкция + exit 1. Ограничение: живой кластерный прогон не выполнялся — Kubernetes в Docker Desktop был выключен; первый полный прогон: включить K8s → ./bootstrap.sh). Последующий фикс (fix/t-029-egress-probe, после первого живого прогона): проба https443 вела к ложному FAIL — `api.github.com` отвечает 403 на неаутентифицированный curl из этого контура (сам egress 443 исправен); цель заменена на `example.com` (та же, что и в проверке 80 — сравнение портов на одном хосте), критерий — любой завершённый HTTP-обмен (rc=0), `api.github.com` — информационное поле `github_code`; обнаружено, что docker-desktop K8s не исполняет NetworkPolicy (порт 80 доступен при deny-all) — добавлен канареечный детект enforcement (throwaway-ns, две канарейки, одна под deny-all-egress): блокирующие проверки 80/25 при неисполнении печатаются SKIPPED+warn вместо FAIL, при исполнении — обязательны (ловят реальные дыры в allow-политиках); ограничение задокументировано в README bootstrap и `docs/tech-dept.md` (TD-001, погасить в T-091); живой прогон после фикса: egress-тесты зелёные (policy_enforcement=off), restore-тест зелёный; capacity-smoke честно требует ≥10GiB — на VM 7.75GiB падает по дизайну (нужно поднять ресурсы Docker Desktop — решение за человеком)
- [x] T030 [P] [US5] [docs T-041] CI-раннеры (K8s executor) + безопасность job pods: self-hosted GitHub Actions runners в `ci`, pods только в `factory-runs` (non-root, read-only rootfs, без privileged/Docker socket/hostPath, `automountServiceAccountToken=false`, лимиты, active deadline, TTL cleanup, отдельные SA, закреплённые digests). DoD: pod создаётся/удаляется по TTL; agent job не имеет merge/deploy credentials. Зависит от T029 — `deploy/ci/` (реализовано: двухуровневая архитектура — ephemeral GitHub Actions runner (Deployment `factory-runner`, 1 реплика, Recreate, concurrency=1 по ADR-010) в ns `ci`, agent-стадии — K8s Job ТОЛЬКО в `factory-runs` (ADR-006: один эфемерный pod = одна стадия); без ARC — осознанное решение (listener и work-поды ARC в одном ns не выражают разделение `ci`/`factory-runs`, обоснование в README): `install.sh` (идемпотентный apply, без секретов, падает с подсказкой при отсутствии bootstrap), `verify.sh` (RBAC-матрица через impersonation + probe-job с 9 негативными/позитивными проверками изнутри пода: no_sa_token/env_clean/api_blocked/api_ip_blocked + TTL-удаление), `teardown.sh` (возврат baseline T029); `scripts/entrypoint.sh` (preflight SSAR → минт registration token по GitHub App: RS256 JWT → installation token → registration-token endpoint, тот же пайплайн что check-github-app.sh T029 → `config.sh --ephemeral` → `run.sh`; registration token живёт 1 ч и никогда не хранится, минтуется на каждый старт пода), `run-agent-job.sh` (рендер шаблона → создание Job через K8s REST API curl+jq (kubectl в образе нет) → polling → извлечение StageResult по маркерам из лога → маппинг exit-кодов 0/10/20), `common.sh` (самодостаточная копия хелперов bootstrap). RBAC (`manifests/rbac.yaml`): Role `factory-agent-jobs` в `factory-runs` — jobs CRUD, pods get/list/watch, pods/log get; БЕЗ pods/exec, БЕЗ secrets, БЕЗ доступа к control-plane namespaces; cross-ns RoleBinding SA `factory-ci` (из `ci`) → `factory-runs`; SA `factory-agent` — без RBAC и без токена (pod-level `automountServiceAccountToken: false`). securityContext agent-подов (`agent-job-template.yaml`): runAsNonRoot uid/gid 65532, readOnlyRootFilesystem, privileged=false, allowPrivilegeEscalation=false, drop ALL, seccomp RuntimeDefault, без docker socket/hostPath; writable — emptyDir `/tmp` + `/workspace` 2Gi; лимиты 250m/512Mi → 1 CPU/1Gi; TTL `ttlSecondsAfterFinished: 3600` (DoD «удаляется по TTL»), `activeDeadlineSeconds: 3600`, backoffLimit 0. NetworkPolicy (`manifests/networkpolicies.yaml`): широкое bootstrap-правило allow-egress-https в `factory-runs` ЗАМЕНЯЕТСЯ на «публичный 443» (ipBlock 0.0.0.0/0 с except приватных диапазонов: pod/service CIDR, host-сеть Docker Desktop с apiserver:6443, link-local; то же имя объекта — политики аддитивны, «вычесть» allow отдельной политикой нельзя); install.sh пере-применяет после bootstrap, teardown.sh возвращает baseline. Регистрация: GitHub App permission только Administration:write — недостаточно для merge (contents:write), deploy или repo secrets; Secret `factory-github-app` в `ci` создаётся вне git (envFrom optional: true — без секрета под стартует и печатает точную команду remediation). Pinned digests (проверены imagetools inspect): `ghcr.io/actions/actions-runner@sha256:551dc313…` (v2.327.1), `busybox@sha256:9db7b599…` (1.37); образ фабрики — плейсхолдер `__IMAGE__` до T033. README на русском (архитектура, инвентарь credentials, безопасность, установка, ограничения); ADR не потребовался — решение покрыто ADR-006/010/019, обоснование в README. Валидация: bash -n ×6 и shellcheck ×6 — зелёные; kubeconform -strict — 7/7 ресурсов (включая all-digits регресс рендера шаблона, вскрывший латентный баг незакавыченных string-плейсхолдеров `__GIT_SHA__`/`__RUN_ID__` — пофикшено в шаблонах); ruff check + ruff format --check + mypy strict + pytest без БД (857 passed, 39 skipped) — зелёные. Ограничение: живой кластерный прогон не выполнялся — Kubernetes в Docker Desktop выключен; первый прогон: bootstrap.sh → install.sh → verify.sh. runs-on swap в factory-stage.yml, транспорт evidence-каталога и приватные репо (contents:read токен) — T031; образ с immutable digest — T033)
- [x] T031 [US5] [docs T-042] Helm chart dark-factory: Console/API с зависимостью PostgreSQL фабрики, values-local, ресурсы по профилю MacBook, probes, локальный ingress. DoD: helm lint + тестовый deploy в namespace `factory`. Зависит от T029 (T-040), T035 (T-050, US6 — interleaved) — `charts/dark-factory/` (реализовано: chart `charts/dark-factory/` (apiVersion v2, 0.1.0) ставит API T035 (`factory api serve --host 0.0.0.0`) в ns `factory`: Deployment (1 реплика, Recreate — квота допускает ровно один API-под рядом с PG) + Service ClusterIP + условный Ingress (networking.k8s.io/v1, в values-local включён, className nginx, host factory.localhost — без контроллера инертен, штатный доступ port-forward); PostgreSQL не бандлится — используется bootstrap-овский `factory-postgres` (T029, «зависимость PostgreSQL фабрики»); `DATABASE_URL` из existingSecret `factory-api-database` (паттерн T028, секрет вне git, в README предупреждение о percent-кодировании base64-пароля с `+/=`), опциональный existingSecret для `DARK_FACTORY_API_TOKENS` (пусто = mutating fail-closed, ADR-009 §7); pre-install/pre-upgrade hook-Job миграций (`alembic upgrade head`, weight 0, before-hook-creation,hook-succeeded, deadline 300 с, backoffLimit 0, restartPolicy Never — fail-fast) — требование к образу T033 (alembic + migrations/ в образе) зафиксировано; probes без изменения кода API (T035 слит): liveness `GET /openapi.json` (200 без БД — процесс не рестартует при простое state store), readiness `GET /api/v1/runs` (200 только при живой БД — снимает с трафика и возвращает автоматически); безопасность по паттерну T028: SA `factory-api` bootstrap (переиспользование, serviceAccount.create=false), automountServiceAccountToken false, runAsNonRoot 65532, readOnlyRootFilesystem, drop ALL, seccomp RuntimeDefault, emptyDir /tmp; ресурсы requests 100m/128Mi, limits 500m/512Mi (пик контура 400m/448Mi req / 1.7 CPU / 1.64Gi lim ≤ квота 1 CPU/2Gi / 2 CPU/4Gi — расчёт в README); labels по конвенции репозитория (managed-by: dark-factory); образ-плейсхолдер `ghcr.io/vadagama/dark-factory:bootstrap` до T033. Валидация: helm lint (дефолт и values-local) — 0 failed; helm template обоих профилей — чисто; pytest chart-тестов `tests/test_chart_dark_factory.py` — 11 passed (рендер через helm template: probes, securityContext, ресурсы, отсутствие `:latest`, отсутствие Secret-ов в чарте, hook-аннотации, Ingress только в values-local; скип без helm); ruff + format + mypy strict — чисто; ruff/mypy/pytest по всему репо — зелёные. Тестовый deploy в ns `factory` (DoD): bootstrap T029 применён к живому docker-desktop (первый полный прогон: ns, квоты, SA, NetworkPolicy, PostgreSQL ready; негативные egress-тесты в этом окружении упали — `https443=FAIL`/`http80_blocked=FAIL` — средовое отклонение T029, к chart не относится); throwaway-образ собран локально (не коммитился); `helm install --wait` — Install complete: hook-Job применил схему 0001+0002 к реальной PG и самоудалился (hook-succeeded), API 1/1 Running; функционально: `GET /openapi.json` 200, `GET /api/v1/runs` 200 `[]`, `POST /api/v1/changes` без токена 401 RFC 7807 (fail-closed); после проверки release удалён чисто (схема в PG и секрет остались — штатно)
- [x] T032 [US5] [docs T-043] Argo CD + GitOps-репозиторий: установка Argo CD (Helm), read-only GitOps-репо, Applications для `factory` и `apps-dev`; поток merge → GitOps MR с immutable digest → sync. DoD: GitOps MR меняет digest, Argo разворачивает в `apps-dev`; откат — revert коммита. Зависит от T029 — `deploy/argocd/`, `dark-factory-gitops` (реализовано: установка Argo CD chart `argo/argo-cd` 10.9.1 (Argo CD 3.5.3, pinned в install.sh + assert в тестах) в ns `argocd` через `values-argocd.yaml` — dex/notifications/commitServer выключены (applicationSet безусловен в chart 10.x, ресурсы ужаты), ресурсы компонентов вписаны в квоту ns argocd T029 (req 400m/800Mi, lim 750m/1500Mi — тест проверяет суммы), automount=false только redis (компоненты с доступом к API сохраняют токены), `global.networkPolicy.create: false` (владелец политик — bootstrap T029: egress 443 в argocd и intra-ns уже разрешены, новых NetworkPolicy не добавлено; TD-001 задокументирован), `server.insecure: true` (локальный контур); root-Applications — локальный chart `dark-factory-argocd-apps` (deploy/argocd/chart): Application `factory` → ns `factory`, source = этот репозиторий (path charts/dark-factory + values-local.yaml, т.к. платформенный chart и источник истины здесь; OCI-публикация — T033) и Application `apps-dev` → app-of-apps, source = dark-factory-gitops (path envs/dev, dest ns argocd — desired-state окружений в gitops-репо по ADR-015 п.1, добавление приложений в dev без изменений в этом репо); оба syncPolicy automated, prune=false, selfHeal=false (ADR-011: merge — человек, deploy в dev после merge — автоматически; selfHeal=false — ручные правки не затираются, дрифт виден; откат = revert GitOps-коммита → auto-sync возвращает); секрет `dark-factory-gitops-repo-creds` в ns argocd создаётся ТОЛЬКО вне git (remediation-подсказка с точной командой при отсутствии — паттерн T030); scripts/ идемпотентны по образцу deploy/ci/: install.sh (helm repo add + upgrade --install + apply Application'ов), verify.sh (компоненты подняты, Application'ы зарегистрированы Healthy/Synced или ожидаемо Progressing, repo-connection настроен), teardown.sh (возврат baseline T029: Application'ы каскадом, оба release, stale argocd-initial-admin-secret; ns/CRDs/repo-creds сохраняются; пароль admin пересоздаётся при переустановке — argocd-secret helm-ресурс, отражено в README); seed `dark-factory-gitops` подготовлен в deploy/argocd/gitops-seed/ (репо НЕ создавалось и не пушилось — решение человека): структура desired-state envs/dev (child-Application pilot-dev → ns apps-dev) + platform, mini-chart pilot с digest-плейсхолдером sha256:__PILOT_IMAGE_DIGEST__ (реальный digest — T033), пример GitOps-MR с изменением digest, README с правилами (только через MR, без секретов, immutable digests — запрет latest/floating, протокол версионирования ADR-015 п.5) и точными gh-командами bootstrap (не исполнялись); границы: автоматизация открытия GitOps-MR с digest — T033, smoke/evidence — T034. Валидация: bash -n ×4 и shellcheck ×4 (docker koalaman/shellcheck:stable) — зелёные; helm lint — 0 failed; helm template локального чарта ×2 + diff — рендер идемпотентен; kubeconform -strict (docker ghcr.io/yannh/kubeconform:v0.6.7 — тега :stable на ghcr нет, задокументировано): локальный чарт валиден (2 CRD skipped — ожидаемо), pilot-seed валиден, upstream-рендер с values 9/9 валиден; pytest tests/test_deploy_argocd.py — 17 passed (рендер-контракты по образцу T031: ns/source/target-ns/syncPolicy, запрет floating tags, отсутствие Secret'ов, квота, automount, структурный upstream-рендер — имена Deployment/StatefulSet вместо строковых ассертов: `dex-server` присутствует в рендере даже при dex.enabled=false через TLS-volume optional:true); ruff check + ruff format --check + mypy strict + pytest весь сюит (900 passed, 49 skipped) — зелёные; helm pull-проба теста пишет tgz во tempdir (не засоряет корень репо). Ограничение: живой кластерный прогон не выполнялся — Kubernetes в Docker Desktop выключен; первый прогон: bootstrap.sh → создать dark-factory-gitops и запушить seed (gh-команды в gitops-seed/README.md) → создать repo-creds секрет → install.sh → verify.sh; DoD-контур (GitOps-MR меняет digest → Argo разворачивает в apps-dev; откат revert-ом) закрывается на первом живом прогоне вместе с T033)
- [x] T033 [US5] [docs T-044] OCI build job (immutable digest): доверенный build для итогового SHA без privileged DinD, SBOM, SAST/SCA/secrets/image scan, публикация immutable digest, запрет `latest` для promotion. DoD: образ по digest; повторная сборка того же SHA даёт тот же digest (или задокументированное отклонение). Зависит от T025 — `.github/workflows/`, `deploy/ci/` (реализовано: trusted workflow `.github/workflows/factory-image.yml` — reusable (workflow_call+dispatch), вызов из `ci.yml` job'ом `factory-image` строго после всех детерминированных гейтов (`needs: [lint, typecheck, test, build, security, factory-us1-parity, factory-stage-smoke]`) и всегда на итоговом SHA (FR-009/SC-004, не merge-preview); trusted tier: GitHub-hosted runner (один job = одноразовая VM, privileged DinD не нужен), единственный job с `permissions: contents:read, packages:write` (таблица учётных данных в deploy/ci/README.md дополнена), fork-PR'ы собирают образ, но никогда не публикуют (`CAN_PUSH` = `head.repo.full_name == github.repository`); concurrency по SHA. Публикация: единственный immutable-тег `sha-<sha>` → `ghcr.io/vadagama/dark-factory`, `latest`/floating запрещены (тесты), promotion между окружениями — по digest через GitOps-MR (T032/T034), digest возвращается в outputs `digest`/`image_ref`/`tag` и в артефакт `image-digest.json` (schema v1). Evidence-цепочка: gitleaks v8.30.1 (pinned digest, `--no-git`, fail до сборки) → SAST bandit 1.9.4 (`uvx --python 3.12`) → SCA pip-audit 2.10.1 по `uv export --frozen` (проверяется заблокированный набор) → multi-arch build+push (`linux/amd64,linux/arm64` — ADR-010 п.4) → опциональный rebuild-check → trivy 0.68.1 по опубликованному digest'у обеих платформ (`--ignore-unfixed`, exit 1 на unfixed HIGH/CRITICAL — fail-closed; гейт релиза не потребляет evidence красного прогона; публикация до скана — осознанный порядок) → syft v1.51.1 SBOM (SPDX JSON, обе платформы); артефакты `factory-image-evidence-*`/`factory-image-sbom-*` с retention 30 дней, отчёт в `$GITHUB_STEP_SUMMARY`. Образ (`deploy/ci/image/Dockerfile`, dockerignore deny-all + whitelist): двухстадийный, `python:3.12-slim-bookworm` pinned одним digest'ом в обеих стадиях, uv 0.11.19 pinned digest'ом через `COPY --from`, зависимости только из `uv.lock` (`uv sync --frozen`, без .pyc — шьют mtime), wheel-билдер hatchling==1.32.0 через `UV_BUILD_CONSTRAINT`, git из snapshot.debian.org `20260824T000000Z` (дата из комментария debian.sources базового образа), `USER 65532:65532`, read-only-safe (`PYTHONDONTWRITEBYTECODE=1`, HOME=/tmp), `alembic.ini`+`migrations/` в /app (контракт миграционного hook-Job T031), label `revision=$GIT_SHA`, `CMD ["factory","--help"]`. Детерминизм (DoD): `SOURCE_DATE_EPOCH` = timestamp коммита (BuildKit нормализует `created`/`history` — не mtime и не содержимое); mtime-нормализация в конце каждого пишущего RUN (prune RO-bind-mount'ов buildkit) и нормализация mtime build-контекста до сборки (GNU touch в workflow; python3 `os.utime` локально — BSD touch не понимает `-d @epoch`); волатильное содержимое удалено: `/var/log` (dpkg/apt пишут Start-Date/End-Date внутрь логов), `/var/cache/ldconfig/aux-cache` (адреса загрузки библиотек, ASLR), `uv_cache.json` + его строка в `RECORD` (timestamp в содержимом, хэш — в RECORD); runtime-дерево собирается в builder'е (`/out`: .venv + alembic.ini + migrations, без pyproject/uv.lock/README/build-constraints) и уходит **одной** `COPY --from=builder /out /app` в несуществующий /app — несколько COPY перештамповывают запись родительского каталога wall-clock временем каждой копии (найдено диффом слоёв двух холодных билдов; корневая запись одно-копийной COPY наследует SDE-mtime источника — проверено экспериментом); `provenance: false, sbom: false` (attestations шьют параметры вызова и ломают стабильность digest'а; SBOM — CI-артефакт). Rebuild-check: в CI — input `rebuild-check=true` (второй холодный билд `--no-cache` того же SHA + сравнение digest'ов, расхождение валит ран; уникальный тег перезаписывается тем же содержимым), локально — `./deploy/ci/scripts/build-image.sh --rebuild-check` (dirty-check только по путям build-контекста, python3-нормализация mtime, одно-арх сравнение image ID — ограничение задокументировано). bandit-находки B404/B603 в `cli/run_records.py` подавлены точечными `# nosec` с обоснованием (git rev-parse фиксированным argv-кортежем, без shell, без недоверенного ввода — не глобальные excludes). README T033: архитектура trusted-уровня, рецепты детерминизма, политика immutable-публикации, обновлённые pinned-digests (uv/gitleaks/trivy/syft), сознательные отклонения (бамп пина = новый digest при том же SHA), ограничение приватного репо для анонимного клона (T030/T031). Валидация: ruff check + ruff format --check + mypy strict (215 файлов) + pytest (917 passed, 50 skipped) — зелёные; локальная сборка arm64; rebuild-check — два холодных билда дают одинаковый image ID; smoke под securityContext agent-подов (read-only rootfs, uid 65532, tmpfs /tmp, cap-drop ALL): `factory --help`, `alembic heads` → `0002_api_state (head)`, `git --version` → 2.39.5, в образе нет uv_cache.json, /var/log и aux-cache пусты, /app содержит только .venv+alembic.ini+migrations; gitleaks «no leaks found», pip-audit «No known vulnerabilities found», bandit exit 0 (2 nosec); 18 тестов Dockerfile/workflow — `tests/test_deploy_ci_image.py`. Ограничение: multi-arch push, trivy-скан опубликованного digest'а, SBOM-артефакты и CI rebuild-check подтверждаются первым прогоном workflow на PR (пуш в ghcr.io — права владельца; первый digest появится в артефакте `factory-image-evidence-*` и подставляется в `run-agent-job.sh --image` и GitOps-promotion); пока репозиторий приватный — анонимный clone из agent-поды ограничен (токен contents:read — T031))
- [x] T034 [US5] [docs T-045] Smoke + release evidence: проверка неизменности ожидаемого digest перед деплоем (FR-011); health + smoke после Argo sync; release evidence (digest, Argo status, smoke result) в run record; неуспешный smoke не переводит изменение в Released. DoD: e2e — успешный и неуспешный smoke дают корректные статусы и evidence. Зависит от T032, T033 — `src/dark_factory/quality/release/` (реализовано: пакет `quality/release/` в дисциплине чистого ядра `orchestration/policy/merge.py` — без I/O, часов и LLM: `decision.py` — `ReleaseObservation` (expected/observed digest, raw Argo sync/health, smoke) → `ReleaseDecision` (`status`, `reason`, `rollback_signal`) через `evaluate_release()` с фиксированным порядком проверок, первая причина побеждает: ① digest (FR-011) — expected или observed неизвестен либо ≠ → стоп, диагностика называет обе стороны; ② Argo sync (ADR-010) — только `Synced`; ③ Argo health — только `Healthy`; ④ smoke (FR-013, ADR-011 п.6) — пробы обязаны быть выполнены и пройти (пустой набор = NOT_RUN = не released; одна упавшая проба валит набор). Fail-closed при неполных данных (нет evidence — никогда не released, зеркально merge_context=None → manual T-026); каждая неудача несёт `ROLLBACK_SIGNAL` — сигнал Operation для отката: revert GitOps-коммита с digest'ом, Argo auto-sync возвращает прежний (ADR-010/ADR-011 п.6). `ArgoSyncStatus`/`ArgoHealthStatus.from_raw()` — case-insensitive, нераспознанное/отсутствующее → Unknown (не ошибка); `normalize_digest` — пустое = отсутствующее. `probes.py` — шов `SmokeProbe` (Protocol, по образцу ReconcileObserver), `run_smoke_probes()` (упавшая проба = данные, не прерывает остальные; URL/тело/текст исключений не эхоятся — ADR-009), MVP-пробы `HttpHealthProbe` (2xx = pass) и `HttpDigestProbe` (expected digest в header/теле ответа — связывает работающий под с promoted digest, FR-011/T033); таймауты/ретраи — фиксированные константы (3 попытки × 5 с, пауза 1 с), transport/attempts инъецируются для тестов. `evidence.py` — `build_release_evidence()` → секция `RunRecord.release`. Run record (`changes/run_records.py`): `SmokeProbeEvidence`/`ReleaseEvidence`, опциональное поле `RunRecord.release`; `schema_version` остался 1 — расширение аддитивно (старые записи парсятся, старые ридеры ключ игнорируют; обоснование в докстринге), `build_release_run_record()` (`cli/run_records.py`): released → run/stage SUCCEEDED, иначе FAILED; `stage_results=[]` (StageResult — контракт детерминированного исполнителя, не этой команды). CLI `factory release verify` (`cli/release.py`, wiring в `cli/main.py`): expected digest — флаг `--expected-digest` XOR `--digest-json` (артефакт T033 image-digest.json schema v1, читается только поле `digest`), `--application` (namespace/name, попадает в evidence), наблюдаемое состояние значениями `--observed-digest/--argo-sync/--argo-health` (живого Argo/K8s-ридера нет — осознанный YAGNI, читатель состояния за тем же швом — отдельная задача), smoke — `--smoke-url` (+ опц. `--smoke-digest-url/--smoke-digest-header`); без smoke-опций вердикт «smoke was not run» — fail-closed. Короткое замыкание: при провале digest/Argo-проверок (`pre_smoke_failure()`) пробы не отправляются — чужой деплой не зондировать (FR-011 «останавливать операцию»). Вся валидация до проб (fail fast, exit 2); exit-коды: 0=released, 1=release_failed (CI краснеет; при провале persist run record — exit 1 с печатью evidence, ничего не теряется молча), 2=invalid input; `--evidence-dir` + `--change` — только парой (снапшот фиксирует, что проверялось); рендер text/JSON. Тесты: 30 юнит решения (`tests/test_quality_release_decision.py` — все ветки порядка, детерминизм, fail-closed, from_raw, pre_smoke_failure), 17 проб (`tests/test_quality_release_probes.py` — MockTransport 2xx/5xx/транспортные ошибки, digest в header/теле, изоляция упавших проб, отсутствие эха), 27 CLI (`tests/test_cli_release.py` — exit-коды, invalid-ветки, e2e DoD: успешный и неуспешный smoke дают корректные статусы + evidence в run_record.json, rollback-сигнал, короткое замыкание проб, секреты в URL не эхо, wiring через `main()`), параметризации в `tests/test_cli_parser.py` и round-trip с release-секцией + обратная совместимость в `tests/test_cli_run_records.py`. ruff check + ruff format --check + mypy strict (220 файлов) — чисто; pytest — 1005 passed + 50 skipped (было 917 + 50). Ограничения: наблюдаемое состояние деплоя передаётся значениями флагов (вызывает CI job/операторский скрипт после Argo sync); digest-проба требует, чтобы пилотный эндпоинт отдавал digest (header/тело); интеграционный прогон против живого кластера — вместе с первым живым e2e T032/T033)

**Checkpoint**: US5 проверяется независимо — 100% «выпущено» имеют успешный smoke на зафиксированном digest.

---

## Phase 8: User Story 6 - Контекст, знания, трекер и наблюдаемость (Priority: P6)

**Goal**: Контекст из закреплённых источников с фиксацией версий/provenance; синхронизация с трекером через сменный адаптер; Console показывает стадии, блокеры, стоимость, гейты, ссылки на evidence; полная трассируемость.

**Independent Test**: Для одной инициативы собрать `ContextBundle` (проверить фиксацию версий), синхронизировать статусы с Plane (без дублей), открыть Console (стадия, блокеры, стоимость, гейты, ссылки); отключение трекера не останавливает конвейер.

- [x] T035 [P] [US6] [docs T-050] FastAPI API: `GET /runs`, `/runs/{id}`, `/runs/{id}/stage-results`, `/runs/{id}/trace`, `/changes`, `/changes/{id}`, `/changes/{id}/trace`, `/changes/{id}/approvals`, `/runs/{id}/evidence`, `/runs/{id}/gates`, `/runs/{id}/findings`; token AuthN (единый оператор + service-токены CI/CLI), version-bound approval `{actor, role, contract_hash/commit_sha, timestamp, decision}`, audit mutating-операций, `Idempotency-Key`, `409` при `state_revision` mismatch, RFC 7807-подобные ошибки. DoD: негативные AuthN-тесты, OpenAPI-схема. Зависит от T003, T006 — `src/dark_factory/api/` (реализовано: пакет `api/` — `app.py` (create_app, префикс `/api/v1`, RFC 7807-хендлеры 401/403/404/409/422/500 без утечки внутренностей), `auth.py` (Bearer-токены из `DARK_FACTORY_API_TOKENS` (JSON `{token, actor, role, scopes}`), хранение только SHA-256-дайджестов, сравнение `hmac.compare_digest`, fail-closed при пустом store, роли operator/service, scopes `changes:write`/`approvals:write`, approvals дополнительно требуют роль operator — агенты не согласовывают), `dto.py`, `aggregates.py` (trace/gates «последний результат по гейту выигрывает»/findings/evidence с дедупликацией по id), `routes_runs.py` (8 GET: runs+фильтры, карточка с usage-агрегатом и открытыми блокерами, stage-results, trace SC-007, evidence (+/{id}), gates, findings с фильтрами severity/status), `routes_changes.py` (POST /changes — intake FR-001 с дедупликацией по id и external_ref FR-017, GET /changes, карточка, change-trace, approvals GET/POST — version-bound `Decision.commit_sha=subject_revision`, опциональный `expected_state_revision` → 409 до записи, bump revision; состояние+audit в одной транзакции). Миграция `0002_api_state` (обратима): таблицы `change` (unique partial external_ref), `decision` (unique partial idempotency_key), `stage_result` (PK=attempt_id, payload JSONB, ON CONFLICT DO NOTHING — данные попыток не перезаписываются, FR-014), `audit_log`; ORM-модели в `state/models.py`, репозитории `state/stage_results.py` + `state/change_store.py` (идемпотентные pg_insert). CLI `factory api serve` (`cli/api.py`: DATABASE_URL без эха, exit 2 при отсутствии; uvicorn). Зависимости +fastapi +uvicorn. Тесты: 10 no-DB (401/403-матрица, OpenAPI-пути, RFC 7807, парсинг CLI) + 10 интеграционных с PG (intake-идемпотентность, replay по Idempotency-Key, 409, version-bound approvals, гейты/findings/evidence/trace, usage-агрегат); `_TABLES` в conftest дополнен. Ограничения: CLI/движок пока не пишут StageResult в PostgreSQL (запись — публичный репозиторий `StageResultRepository`, durable wiring — следующая задача); GET-эндпоинты без токена (локальный контур, ADR-009 §7); pause/retry/cancel — вне чеклиста T035 (нужен durable wiring движка). ruff + format + mypy strict — чисто; pytest без БД 872 passed + 49 skipped; с PostgreSQL 16.4 (эфемерный docker) 920 passed + 1 skipped)
- [x] T036 [US6] [docs T-051] Console MVP (5 экранов): список изменений, карточка изменения с evidence и цепочкой стадий, гейты/approvals, бюджеты/лимиты, настройки/профили; режимы «с согласованиями» и «автономно до MR». DoD: vitest + Playwright smoke 5 сценариев; сборка chart'ом (T031). Зависит от T035 — `console/` (реализовано по принятому ADR-021, расположение — отдельный сабпроект `console/` в корне (не `src/dark_factory/console/`); Python-код фабрики не тронут. Каркас: Vite 8 + React 19 + TypeScript strict + npm (точные версии в package-lock.json), .nvmrc Node 22.23.2, eslint flat + prettier, vite-прокси `/api` → `VITE_API_TARGET` (default 127.0.0.1:8000); стилистика — Radix primitives (dialog, label) + plain CSS дизайн-токены без Tailwind (осознанное упрощение shadcn-слоя, задокументировано в console/README.md), роутинг react-router v7 (declarative + useAsync вместо loaders). API-клиент `console/src/api/`: тонкий типизированный fetch (без react-query/axios), TS-типы зеркалируют pydantic-модели T035 (Decimal-cost — строкой), Bearer только на POST + Idempotency-Key (UUID v4), RFC 7807-парсинг, 401/403/409-семантика. Токен оператора — только localStorage (SC-008), маскированный ввод/отображение (виден суффикс 4 символов), никогда в логах/фикстурах/git. Экраны (5): (1) список изменений со статусами последнего прогона (derive из GET /runs без N+1) + intake-форма FR-001 (title, provider/slug, risk_class R0–R4, source=console, client-side chg_-id, POST с changes:write, fail-closed без токена); (2) карточка изменения — ChangeCard + прогоны (стадии, usage/стоимость, evidence-ссылки, открытые blocker-фединги) + цепочка стадий SC-007 из /changes/{id}/trace; (3) гейты/approvals — гейты из /runs/{id}/gates, blocker-фединги, история решений, форма version-bound approval (gate, approved/rejected/waived, обязателен subject_revision, expected_state_revision = 1 + decisions_count с автоматическим 409-retry: перечитать карточку и повторить один раз с тем же Idempotency-Key; approvals:write + роль operator); (4) бюджеты/лимиты — таблица лимитов из meta.json против usage прогонов (токены/стоимость/доля от лимита/ручные вмешательства/попытки); (5) настройки/профили — управление токеном (сохранение/удаление, предупреждение о localStorage), URL API (опционально), профили ролей из meta.json, информационный вывод режима фабрики «С согласованиями»/«Автономно до MR» (из снапшота: human-гейты + auto-merge риск-классы; интерактивное переключение вне скоупа — нет API, задокументировано). Meta-снапшот: `console/tools/export_meta.py` (uv run, детерминированный вывод из src/dark_factory/rules + agents/profiles + BudgetSnapshot + HUMAN_GATES/merge policy, фиксированная сортировка ключей, без source-SHA в данных) → коммиченный `console/src/generated/meta.json`; drift-тест `tests/test_console_meta_snapshot.py` (3 passed) регенерирует снапшот в tmp и сверяет — работает в pytest-CI без node. Тесты: vitest 40 passed (api-клиент: GET без токена, POST Bearer+UUIDv4-заголовок, 401/403/409/404 RFC 7807, не-JSON тело, сеть; useAsync; meta/id; компонентные — список+fail-closed intake, intake-пейлоад source=console, гейты+approvals+409-retry с тем же Idempotency-Key и отсутствием токена на GET, бюджеты, настройки с маскировкой) через мок global fetch с синтетическими данными; Playwright `console/e2e/` — 5 passed (по сценарию на экран против `vite preview` с page.route-фикстурами `e2e/fixtures/api.ts`, синтетические данные, без секретов; chromium headless, веб-сервер localhost:4173). CI (`.github/workflows/ci.yml`): jobs console-lint/typecheck/test/build (setup-node по console/.nvmrc, npm ci, working-directory console) + console-e2e (npx playwright install --with-deps chromium) — существующие python-джобы не тронуты; trusted-образ `.github/workflows/console-image.yml` по паттерну factory-image (workflow_call/dispatch, gitleaks, npm ci + build dist, docker buildx multi-arch, только immutable тег `sha-<sha>` ghcr.io/vadagama/dark-factory-console, без latest; CAN_PUSH = не fork; trivy fail-closed + syft SPDX evidence + image-digest.json + step summary; rebuild-check НЕ добавлен — по ADR-021) — вызывается из ci.yml с needs по всем python- и console-гейтам. Образ: `console/Dockerfile` (nginx-unprivileged 1.29.4 pinned по digest sha256:c38007…57423, проверен docker buildx imagetools inspect; COPY dist/, entrypoint-шаблон `console/nginx.conf.template`: listen 8080, SPA-fallback try_files, прокси `/api/` → `${API_UPSTREAM}` без trailing slash с сохранением префикса — envsubst подставляет только объявленные env, nginx-переменные $uri/$host не трогаются; локальная проверка: сборка образа, index/SPA-fallback/ассеты 200, рендер прокси корректен). Chart: `charts/dark-factory/` + console Deployment (плейсхолдер `ghcr.io/vadagama/dark-factory-console:bootstrap` — реальный digest после первого прогона console-image, паттерн T031; replicas 1, Recreate, securityContext runAsNonRoot 101/101, readOnlyRootFilesystem, drop ALL, seccomp RuntimeDefault, automount false, emptyDir /tmp и /var/cache/nginx, ресурсы 25m/32Mi → 100m/64Mi, probes GET / — процессные, env API_UPSTREAM = <release>-<chart>.<ns>.svc.cluster.local:8000, без valueFrom/секретов) + Service ClusterIP 80→http; Ingress не тронут (проксирование /api внутри nginx-образа, ADR-021); chart-тесты расширены до 17 passed (console Deployment/Service, securityContext 101, ресурсы, non-latest тег, API_UPSTREAM, процессные probes, отсутствие секретов в env консоли). AGENTS.md дополнен строкой console/ в «Структура репозитория». Валидация фактически запущена: cd console && npm ci && npm run lint (0 errors, 4 warning react-refresh на вспомогательные экспорты — by design) && npm run typecheck && npm run test (40 passed) && npm run build && npm run e2e (5 passed); uv run python console/tools/export_meta.py → git diff по meta.json пуст (идемпотентность генератора); uv run ruff check . && ruff format --check && mypy && pytest (весь сюит; drift-тест зелёный; без БД часть skipped — как обычно); helm lint (дефолт + values-local) и helm template обоих профилей — зелёные; YAML workflow провалидирован python yaml.safe_load. Ограничения: режим фабрики информационный (переключения нет — нет API); expected_state_revision выводится из карточки (1 + decisions_count, инференс T035-семантики) и остаётся редактируемым; приемка создаёт change без runs (запуск — CLI/движок); интерактивные pause/retry/cancel прогонов и live-обновление — вне скоупа MVP)
- [x] T037 [P] [US6] [docs T-033] `TrackerPort` + адаптер Plane (self-hosted, webhook+HMAC): получение задачи, публикация статусов/ссылок, запрос уточнений; webhook — ускоритель, подпись + timestamp window, дедупликация по event ID, ротация секрета; до готовности — NoOp-заглушка; недоступность трекера не блокирует CLI/Console (FR-020). DoD: e2e создание задачи → Change → статус отражён. Зависит от T006 — `src/dark_factory/adapters/tracker/` (реализовано: пакет `adapters/tracker/` — `config.py`: `PlaneConfig` (frozen; `from_env`/`missing_env_vars`; `api_key` и webhook-секреты с `repr=False` — ADR-009; неизвестный `DARK_FACTORY_PLANE_REPOSITORY_PROVIDER` → `ValueError`, а не молчаливый дефолт); env: `BASE_URL`, `API_KEY` (секрет), `WORKSPACE_SLUG`, `PROJECT_ID`, `REPOSITORY_SLUG`, опц. `REPOSITORY_PROVIDER` (дефолт github), `WEBHOOK_SECRET`, `WEBHOOK_SECRET_PREVIOUS`; `client.py`: `PlaneClient` (httpx2, статический `X-API-Key` — у Plane нет refresh-flow, инъецируемый transport, timeout 10 с — FR-020) и `PlaneAPIError(PortError)` с сообщением только method/path/status (тело и секреты не эхо — ADR-009); `adapter.py`: `PlaneTrackerAdapter` — `get_change`: GET issue detail, 404 → `None` (задачи нет), 5xx/сеть → `PlaneAPIError` (degraded выбирает вызывающий), маппинг issue → `Change` (id, name → title, `description_stripped`, source=TRACKER, external_ref, product из конфигурации, риск-класс из label `risk:R2` с дефолтом R1, ISO-8601 `created_at` → aware datetime); `publish_status`/`request_approval`: комментарий к задаче (`factory status: …` / `factory approval requested: …`) с невидимым маркером `<!-- dark-factory:idempotency:<key> -->` — идемпотентность по ключу через список комментариев (FR-017; конвенция source-control адаптера, ADR-019 п.4), handle percent-кодируется целиком (сегмент не выходит за `/issues/`); `noop.py`: `NoOpTracker` — заглушка контура без трекера (ADR-013 п.5): `get_change` → `None`, записи — no-op, не падает никогда (FR-020); `webhook.py`: `verify_webhook` (фиксированный порядок: нет подписи → `missing_signature`, нет/битый timestamp → `missing_timestamp`/`malformed_timestamp`, выход за окно ±300 с → `stale_timestamp`, HMAC-SHA256 по `<timestamp>.<body>` со сравнением `hmac.compare_digest` по всем активным секретам без early exit, пустой набор секретов → `invalid_signature`), `WebhookDeduplicator` (bounded OrderedDict по `X-Plane-Delivery`), `PlaneWebhookGuard` (ring на 2 секрета, `rotate` сдвигает current → previous, `verify` = подпись/окно, затем требование event id и дедупликация: только аутентифицированная доставка занимает id, окно нельзя отравить, отклонённая несёт event id/type для корреляции); webhook — ускоритель, не источник истины (ADR-013 п.2, ADR-016 п.8). Тесты: контрактная сюита `test_tracker_port.py` параметризована `fake | plane` (тест-модуль не менялся: задачу `chg-001`/`PLANE-42` сидит сам binding — fake через `seed`, plane через `PlaneApiEmulator.seed_change`, — поэтому реальный трекер, в отличие от фейка, не получает запись в никуда) + in-memory эмулятор `tests/contract/plane_api.py` (issues/comments, auth, journal wire-путей, `visible_comment`/`statuses_of`/`approvals_of`); 21 юнит `tests/test_plane_tracker_adapter.py` (маппинг и риск-label, 404 → None, 401/500 без эха секрета, кодирование handle, идемпотентность записи, `NoOpTracker`, config из env, `repr` без секретов, e2e задача → Change → статус); 22 юнита `tests/test_plane_webhook.py` (все негативы: missing/blank подпись, missing/malformed/stale/future timestamp, граница окна, сужение tolerance, чужой секрет, refresh timestamp под старой подписью, подмена тела, пустой ring, ротация с сохранением предыдущего секрета и вытеснением после второй, dedup и его вытеснение, отсутствие отравления окна); skip-gated интеграционный `tests/integration/test_plane_tracker.py` (replay-safe ключи). Валидация: ruff check + ruff format --check + mypy strict (231 файл) — чисто; pytest 1066 passed + 51 skipped без БД. Ограничение: живого Plane-инстанса нет — эмулятор + skip-тест; контракт webhook-прокси (подпись `<timestamp>.<body>`) зафиксирован в `webhook.py` и `docs/descriptions/ports.md`; wiring трекера в CLI/движок (публикация статусов из Flow) — следующая задача)
- [x] T038 [P] [US6] [docs T-060] `TelemetryPort` + OpenTelemetry-корреляция: трейсы `change → run → stage → agent call → tool call → CI job → deployment`, атрибуты usage/cost, экспорт в job logs/artifacts (внешний backend опционально). DoD: по trace восстанавливается полная цепочка запуска. Зависит от T006 — `src/dark_factory/adapters/telemetry/` (реализовано: пакет `adapters/telemetry/` поверх OpenTelemetry SDK (`opentelemetry-api`/`opentelemetry-sdk`, ADR-009 п.2; OTel добавлен в denylist границ импортов): `adapter.py` — `OtlpTelemetryAdapter(TelemetryPort)`: `span()` открывает OTel-span как текущий, возвращает вызывающему value-объект порта `Span` (провайдерские типы в core не просачиваются), корреляция держится на вложенности (`change → run → stage → agent → tool → CI job → deployment`); `record_usage` — короткоживущий span `factory.usage` (ребёнок текущего span'а) с атрибутами `usage.prompt_tokens/completion_tokens/total_tokens/cost`, запись не теряется и без активного span'а (ADR-009 п.3); `config.py` — `TelemetryConfig.from_env` (`DARK_FACTORY_TELEMETRY_SERVICE_NAME`/`_EXPORTER`/`_FILE`), fail-closed (неизвестный экспортёр и `file` без пути → `ValueError`); `exporters.py` — `ConsoleSpanExporter` (дефолт: job logs), `JsonLinesSpanExporter` (artifact: append, детерминированный порядок ключей, parent-links), `SafeSpanExporter` (сбой sink'а деградирует телеметрию, но не роняет пайплайн; диагностика без payload/значений и текста исключения — ADR-009 п.8). Контрактная сюита `tests/contract/test_telemetry_port.py` не менялась: параметризована `fake | otlp` через binding в conftest (журналы выводятся из инъектированного `InMemorySpanExporter`). Тесты: `tests/test_telemetry_otlp_adapter.py` — 19 (восстановление цепочки — DoD, ветвление/изоляция запусков, usage-корреляция, ERROR-статус и проброс исключения, конфиг fail-closed, JSON-lines артефакт, изоляция сбоя экспортёра без утечки секрета). Валидация: ruff check + ruff format --check + mypy strict (236 файлов) — чисто; pytest 1088 passed + 51 skipped без БД; contract-сюита телеметрии — 6 passed (fake 3 + otlp 3). Независимая проверка (quality): гейты воспроизведены, цепочка подтверждена адверсариально. Ограничения: внешний OTLP-backend не подключён (TD-003), wiring адаптера в стадии/Flow — следующая задача, cross-process propagation (W3C `traceparent`) не реализован, redaction атрибутов перед экспортом — TD-004)
- [x] T039 [US6] [docs T-061] Run records в `dark-factory-runs` — **P0-минимум** (блокирует T043): `RunManifest` + итоговый `StageResult` + evidence-ссылки с первого e2e-прогона; запись идемпотентна (lookup по `change_id`). Полный протокол (partitioning, single-writer, sanitization, подпись manifest), retention и аналитика — P1. Зависит от T003, T024 — `dark-factory-runs` (реализовано: пакет `src/dark_factory/execution/runs/` — `RunRecordStore`: детерминированный partitioning `<root>/runs/<YYYY>/<MM>/<change-slug>/<run-slug>` (UTC-год/месяц из `run.created_at`, не из часов публикатора), идемпотентная запись (повтор → `UNCHANGED`, файлы не переписываются), immutability (иное содержимое по тому же адресу → `RunRecordImmutabilityError`; исправление — новая запись), атомарная запись через staging-каталог; дерево по git-structure §8 — `manifest.yaml`, `snapshot.json` (самодостаточный `RunRecord`), `stages/<stage>-<attempt>.json`, `usage.json`, `decisions.md` (только при наличии решений), `evidence-index.json`, пустые секции не создаются. Screening `sanitize.py` — fail-closed шаблоны секретов (PEM key, `gh[pousr]_`, `Bearer`, JWT, AWS key, `token/password/secret=…`, credentials в URI) и лимит `MAX_RUN_RECORD_BYTES` 512 KiB; диагностика без значения (ADR-009). Цепочка evidence: необъявленные ссылки, конфликтующие дубликаты id, неиммутабельные URI (`latest`), `required` без `checksum` у успешного run (failed/blocked остаются публикуемыми) + инварианты ADR-009 §9; `verify_evidence_checksums` с инъецируемым резолвером. Path safety + сверка идентичности по `change_id` (slug-коллизия не подменяет запись). CLI `factory run publish --record <path> [--runs-root <dir>] [--json]` (`cli/runs.py`; корень — флаг или `DARK_FACTORY_RUNS_ROOT`): exit 0 created/unchanged, 1 отклонена (секрет/размер/цепочка/immutability), 2 невалидный вход; публикация вынесена из стадии (sandbox по ADR-018 → доверенный публикатор, как в T-026). Seed `deploy/runs/` (README + `schema/run-manifest.schema.json`, `schema/stage-result.schema.json` из контрактов, drift-тест); сам репозиторий не создавался — bootstrap-команды в README (решение человека). Тесты: `tests/test_execution_runs.py`, `tests/test_cli_runs.py`, `tests/test_deploy_runs_schema.py`, парсер. Валидация: ruff check + ruff format --check + mypy strict (246 файлов) — чисто; pytest 1157 passed + 51 skipped без БД; e2e вручную `stage run` → `run publish` (`created` → `unchanged`). Ограничение (P1, ADR-015 §4 — TD-005): коммит/push в `dark-factory-runs`, протокол single-writer/append, retention/архивация и подпись manifest; полная цепочка evidence для пилота подтверждается на прогонах T043)
- [x] T040 [US6] [docs T-062] Бюджет-координатор и allowance: лимиты токенов/времени/стоимости на запуск и роль, allowance-политики, исчерпание → Awaiting Decision с диагностикой, совокупный бюджет в `RunSnapshot`. Зависит от T005, T019 — `src/dark_factory/orchestration/budget/` (реализовано: пакет `orchestration/budget/` — `policy.py`: `BudgetLimits` (token/cost/deadline, `None` — не ограничено), `RoleAllowance`, `BudgetPolicy` (дубликат роли — ошибка конфигурации, порядок по `Role.value`), `DEFAULT_BUDGET_POLICY` без лимитов; эффективный лимит роли — tighter из run- и role-уровня (allowance только ужесточает, FR-018); `coordinator.py`: `check(role, *, now) → BudgetCheck` (`WITHIN_LIMITS`/`AWAITING_DECISION`, `diagnostics` — run-scope первым, затем role-scope, внутри — token, cost, deadline), `reserve(role, *, key, estimated, now)` (id `rsv_<role>_<key>`, идемпотентный реплей, отказ при awaiting_decision / незакрытой резервации роли / неизвестной оценке при заданном лимите), `settle(reservation_id, *, usage)` (неизвестный usage оставляет резервацию открытой — консерватизм FR-018; повтор идемпотентен; неизвестный id → `UnknownReservationError`), `aggregate(*, now) → BudgetAggregate` — совокупный бюджет прогона (тоталы, `roles`, эффективные лимиты, state). Пороги не дублируются: журнал сворачивается в `BudgetSnapshot` и оценивается `rules/limits.py::continuation_violations`; незакрытая резервация считается расходом, поэтому общий бюджет покрывает все роли, review и rework (FR-016/FR-018). Детерминизм: `now` — явный параметр, id — из ключа, без RNG/часов. Контракты: `RoleUsage` вынесен в `changes/usage.py` (сериализуемый контракт; `execution/runs/` не импортирует `orchestration`), `RunUsageSummary.roles` — аддитивное поле с дефолтом (`schema_version` = 1), `build_usage_summary(record, *, roles=())`. Интеграция: executor принимает опциональный `budget_check`; `awaiting_decision` → `StageStatus.BLOCKED` (`StopAction(StopOutcome.BLOCKED, reason=diagnostics)`) + один открытый blocker-`Finding` на сработавший лимит (id `budget:<scope>[:<role>]:<rule>`, `origin=CI`, диагностика в `required_action`) → `api.aggregates.open_blocker_count` показывает Awaiting Decision в Console без правок `console/`. Enum не менялся: `BudgetState.AWAITING_DECISION` — локальный вердикт координатора, отображаемый на существующий `BLOCKED` + `EscalationRule.AUTONOMY_BUDGET_EXHAUSTED` (ADR-018 §5 vs vision reconciled). По умолчанию поведение прежнее: без `budget_check`/при `DEFAULT_BUDGET_POLICY` результат побайтово тот же (регрессионный тест). Тесты: 35 новых (`tests/test_budget_policy.py`, `tests/test_budget_coordinator.py`, +1 в `test_execution_runs.py`) — все лимиты с точной диагностикой, порядок scope/правил, tighten-only, дефолтная политика, резервация как расход, идемпотентность, отказы, форма агрегата, integration (blocked attempt → открытый blocker → `open_blocker_count`; `apply_result` → `RunStatus.BLOCKED`), регрессия `within_limits` и приоритет переносимого snapshot. Валидация: ruff check + ruff format --check + mypy strict (252 файла) — чисто; pytest 1198 passed + 51 skipped без БД. Документация: `docs/descriptions/budget.md` + индекс/кросс-ссылки, TD-006/TD-007. Ограничения (P1): неизвестная *стоимость* не консервативна — TD-006; журнал в памяти и нет production-вызывающего (`cli/stage.py` без `budget_check`, `store.py` без `roles`), per-role allowances не отрисованы в Console — TD-007; wiring в стадии/CI и источник политики — следующая задача)

**Checkpoint**: US6 проверяется независимо — трекер отключаем, Console показывает полную картину, trace восстанавливается.

---

## Phase 9: Pilot & Metrics (Cross-Story Validation)

**Purpose**: Пилотный продукт и сквозная валидация P0-цепочки (SC-001…SC-008).

- [x] T041 [P] [docs T-070] Engineering pack пилотного продукта: blueprint (React + Small UIKit + FastAPI + PostgreSQL), CI-конфигурация продукта, Helm chart приложения, тестовые конвенции; pack версионируется. DoD: blueprint разворачивается в `apps-dev` через фабрику. Зависит от T032 (T-043) — `packs/web-app/` (реализовано: пак 0.1.0 — pack.yaml/CHANGELOG/rules.md (тестовые конвенции: pytest+PostgreSQL, vitest, Playwright smoke, связь с гейтами code/review/verification) + blueprint: backend FastAPI+async SQLAlchemy+Alembic (`/api/healthz`, unit+env-gated integration, uv.lock), frontend React+Vite+TS с workspace-заготовкой `@small/ui` (tokens+Button; UIKit — T-071), продуктовый CI (ruff/mypy/pytest + eslint/tsc/vitest; OCI-образы `sha-<sha>` multi-arch, publish только из main, gitleaks+trivy), Dockerfile'ы (pinned digest, non-root), Helm chart с in-chart PostgreSQL и digest-placeholder'ами, ресурсы в квоте apps-dev; валидация — 23 теста `tests/test_packs_web_app.py` + helm lint/template; живой деплой в apps-dev — T-072, TD-009…TD-012)
- [x] T042 [P] [docs T-071] Small UIKit минимум + UI-гейты: токены (DTCG), 10–12 компонентов + 5–7 паттернов, Storybook как живая спека; гейты UIKit-policy (ESLint), stylelint, axe/WCAG 2.2 AA, visual regression (Playwright); запрет прямых импортов вне `@small/ui`. Зависит от T041 — `packs/ui/` (реализовано: пак 0.1.0 — pack.yaml/README/CHANGELOG/`rules.md` + blueprint `blueprint/ui/`: 12 компонентов (Alert, Badge, Button, Card, Checkbox, Dialog, Input, Select, Spinner, Switch, Tabs, Textarea) и 5 паттернов (ConfirmDialog, EmptyState, FormField, StatusBadge, Toolbar) поверх Radix/shadcn, токены DTCG (`tokens/*.tokens.json` → `tokens.css`/`tokens.ts` через `build-tokens.mjs`, drift-тест), Storybook 10 как исполняемая спека (45 историй), публичная поверхность — только корень `@small/ui` (+ `styles.css`/`tokens.css`); гейты: UIKit-policy ESLint (`policy/eslint-small-ui.mjs` — запрет deep-импортов `@small/ui/...` и прямых `@radix-ui/*`; самотест `test:gates` — 9 фикстур, нарушения блокируются, чистый код проходит), stylelint (цвета только `var(--small-*)` вне генерата), axe/WCAG 2.2 AA (vitest a11y + программный контраст; неавтоматизируемое — checklist человеческой приёмки в `rules.md`, ADR-014 п.3/ADR-018 п.1), visual regression Playwright (45 историй против коммиченных бейзлайнов; детерминизм — `rules.md`: закреплённый образ `mcr.microsoft.com/playwright:v1.63.0-noble`, фикс viewport/locale/timezone/colorScheme/reducedMotion, анимации off, `maxDiffPixelRatio 0.01`, бейзлайны только через ревью). Интеграция в `packs/web-app` 0.2.0: копия кита в `frontend/packages/ui` (побайтовый паритет — `tests/test_packs_ui.py`), скрипты `ui:*`, ESLint-политика кита в коде приложения, джоба `frontend-ui-gates` в продуктовом CI (visual-гейт — только фабричный CI); фабричный CI — джобы `uikit-lint/typecheck/test/gates/storybook` + `uikit-visual` в закреплённом контейнере Playwright. Тесты: vitest кита 55 passed, гейты — 9 фикстур, манифест/паритет — `tests/test_packs_ui.py` (30 passed вместе с `test_packs_web_app.py`). Валидация: lint (eslint+stylelint)/typecheck/storybook:build/playwright (`--update-snapshots` + повторный) кита — зелёные; `npm ci` + гейты frontend blueprint — зелёные; ruff + format + mypy — чисто. Ограничения: каноничные visual-бейзлайны генерируются в закреплённом контейнере (оркестратор коммитит), digest-пиннинг тега контейнера — за оркестратором; WCAG-приёмка неавтоматизируемых критериев — человек; описание — `docs/descriptions/ui-kit.md`)
- [ ] T043 [docs T-072] E2E-пилот: 10 реальных задач (5 quick, 5 standard) по сквозному сценарию intake → SDD → реализация → MR → CI/review → merge → image → GitOps → dev → smoke; сбор метрик vision §7 и метрик FR-024 на уровне инициативы (стоимость с учётом попыток, принятые с первого прохода, число раундов rework, время до принятого MR, число ручных вмешательств), журнал отклонений. DoD: ≥7/10 e2e без ручных правок артефактов агента; отчёт по SC-001…SC-008. Зависит от T023, T025, T026, T031, T033, T034, T035, T039, T041, T057 — `dark-factory-runs`, `specs/001-dark-factory-mvp/quickstart.md`. **Уточнено (2026-09-18, решение оператора — вариант C):** объём инкремента 1 покрывает путь до merge (`review_verification`); промоушен/`release` (`image → GitOps → dev → smoke`) вынесен — модель промоушена не соответствует multi-image продукту, живой прогон `release` не заявляется (TD-031, журнал T043). **Уточнено (2026-09-18, решение оператора D9):** первый проход — стабилизация **одной формы standard-R2** в ручном режиме (K = 1 чистый прогон); матрица 5 quick + 5 standard и добор ≥7/10 — **следующая фаза**, её отсутствие называется в отчёте SC явно; пробел маршрутизации (`select_route` не подключён к созданию рана) зафиксирован как TD-032
- [x] T057 [docs T-092] Сквозной раннер: durable-драйвер запуска и composition root — межстадийный драйвер поверх `flow.apply_result` (решение и outbox-событие в одной транзакции, successor-стадии персистятся тем же решением), replay-first (повтор инертен), CLI `factory run advance`/`run status`; срезы S2–S4 — harness-исполнитель стадии, SCM/MR, гейты и merge, релиз. **Статус: S1 выполнен; S2 выполнен полностью** (retry-протокол, composition root и агентный исполнитель, entry point процесса, публикация commit/push — ADR-024, ADR-025); **S3 выполнен** (гейты по итоговому SHA, merge manual mode с human-решениями, bounded rework, разрешение waiting-чекпоинтов той же операцией через `FactsProvider`/`MergeRequestPort.observe` — ADR-024 §7); **S4 выполнен** (GitOps-промоушен digest `ReleaseStageExecutor` + MR промоушена, резолюция waiting-чекпоинта release-фактами той же попытки через `ReleaseFactsProvider`/`build_release_resolution`, release-опции `run advance` с pre-smoke-коротким замыканием проб, публикация run record после терминального advance — ADR-024 §7). TD-022, TD-023 и TD-024 погашены. Зависит от T004, T006, T010, T014, T016, T024, T025, T032, T039, T041 — `src/dark_factory/orchestration/runner.py`, `src/dark_factory/orchestration/state/run_store.py`, [ADR-024](../../docs/adr/ADR-024-durable-run-driver-and-composition-root.md), [ADR-025](../../docs/adr/ADR-025-process-entry-point-and-lazy-composition.md)
- [ ] T044 [docs T-073] Отклонения пилота → backlog и eval-кейсы: каждый сбой — eval-case (вход/ожидание/факт) и элемент backlog; базовый eval-набор для промптов и skills; прогон локально и в CI, отчёт по регрессиям. Зависит от T043 — `evals/`

---

## Phase 10: Polish & Cross-Cutting Concerns (Extensions P1–P3)

**Purpose**: Расширения после стабилизации пилота (не блокируют MVP); финальная синхронизация документации.

- [x] T045 [P] [docs T-080] Риск-классы R0–R4 и точки контроля: классификация, обязательные гейты и участие человека по классу, 4 точки контроля, «LLM повышает, понижает — только политика», маршруты architecture/foundation; машинное условие эскалации R2+. DoD: маршрутизация детерминирована, опасные изменения не проходят по короткому пути. Зависит от T005, T023, T040 — `src/dark_factory/orchestration/policy/` (реализовано: [ADR-023](../../docs/adr/ADR-023-risk-classes-and-control-points.md) + `changes/risk.py` (`RiskFacts`, `classify_risk`, `effective_risk_class`), `changes/enums.py` (`Route.ARCHITECTURE`/`FOUNDATION`, `ControlPoint`), `policy/risk.py` (`risk_facts`, `effective_change_risk_class`, `TRUSTED_CHANGE_PATHS`, `CONTROL_POINT_BINDING`, `required_control_points`, `missing_control_points`; `RISK_ASSESSMENT_MANUAL` удалён), `policy/escalation.py` (`risk_escalation_violation` — машинные обязательства класса вместо ручной оценки), `rules/gates.py` (`RISK_HUMAN_GATES`, `required_human_gates`; `HUMAN_GATES` в trusted-слое), `flows/routes.py` (полосы классов, `route_allows_risk`, `select_route`, `PROFILE_ROUTE`), `orchestration/flow.py` (полоса на advance/merge/release + обязательства R2+ из движка). Детерминизм: класс — чистая функция фактов, эффективный класс = `max(declared, classify_risk(facts), route_floor)`, агент может только повышать; `quick` — 6 гейтов без `ui`, остальные маршруты — все 7; 4 точки контроля (`problem`/`solution`/`ux`/`discovery_release`) выводятся из правила «гейт требуется и является человеческим». Миграция `0003_route_risk_classes` (обратимое расширение CHECK `execution.route`), перегенерированная схема `stage-result`, устранено дублирование порядка рисков в `quality/gates/specification.py`. Тесты: `tests/test_changes_risk.py`, `tests/test_rules_gates_risk.py` (новые), `tests/test_flow_policy.py`, `tests/test_flows_routes.py`, `tests/test_policy_{risk,escalation,merge}.py`, `tests/test_orchestration_stages.py`, `tests/test_rules_gates.py`. Валидация: ruff + format + mypy strict (262 файла) — чисто; pytest без БД 1387 passed + 51 skipped, с PostgreSQL 16 — 1435 passed + 3 skipped; адверсариальная независимая проверка (quality) после rework подтвердила все пункты DoD. Ограничения: TD-013 (часть фактов риска не наблюдаема), TD-014 (решения на стадийную границу подаёт вызывающий; раннер через `apply_result` — следующая задача))
- [x] T046 [P] [docs T-081] Роли Design и Architect + гейты UI/Planning: UX-flow/состояния/UIKit/accessibility и impact/контракты/данные/NFR/ADR-proposal. Зависит от T014, T042 — `src/dark_factory/agents/profiles/` (реализовано: гейты `ui` и `planning` уже были привязаны к маршрутам и риск-классам (ADR-023) — дополнен каталог профилей: `DESIGN_PROFILE` и `ARCHITECT_PROFILE` v1.0.0 в стиле существующих (ADR-007); `CORE_ROLES` расширен до пяти ролей в порядке каталога ADR-007 (product, design, architect, develop, quality), докстринги обновлены — остальные четыре роли (infrastructure, security, ci_cd, operation) остаются явной ошибкой до T047 (T-082); 4 скилла: `ux-flow` (user flows, screen inventory, состояния loading/empty/error, маппинг на UI-kit), `accessibility-review` (WCAG 2.2 AA, per-screen требования), `impact-analysis` (границы/контракты/данные/NFR → вердикт по маршрутам), `adr-proposal` (черновик ADR по шаблону — proposal для человека, не решение); `ArtifactKind` +3 аддитивных значения (`ux_spec`, `architecture_review`, `adr_proposal`); `STAGE_ROLE`/`STAGE_SKILL` не менялись — Design/Architect не владеют стадиями, профили — записи каталога для contract builder/meta; консольный мета-снапшот перегенерирован (`console/src/generated/meta.json`, профили сортируются по роли), витест-ассерт состава ролей обновлён; `docs/descriptions/agents.md` синхронизирован (таблицы профилей/скиллов, ArtifactKind, счётчики, error-матрица). Тесты: `test_agents_profiles.py` (CORE_ROLES = 5, не-core = infrastructure/security/ci_cd/operation), `test_agents_skills.py` (12 скиллов, kebab-case, привязка к профилям ролей). Валидация: ruff check + format --check (354 файла) + mypy strict (293 файла) — чисто; pytest — 1695 passed + 70 skipped (интеграции без БД — штатный baseline); console vitest — 57 passed)
- [x] T047 [P] [docs T-082] Роли Infrastructure, Security, CI/CD, Operation: инфраструктурные изменения (Helm/K8s), security-анализ (IAM/secrets/зависимости), pipeline/поставка, эксплуатация (smoke, rollback-анализ), release-гейт с Operation-вердиктом. Зависит от T014, T034 — `src/dark_factory/agents/profiles/`. **Частично:** ролей нет — `STAGE_ROLE` отображает стадию `release` на `ci_cd`, и стадия честно останавливается `blocked`, пока профиль не появится (`orchestration/stages/agent.py`); релиз-вердикт реализован как детерминированная политика (T034), без роли Operation. (реализовано: каталог профилей дополнен до полного состава ADR-007 — `INFRASTRUCTURE_PROFILE`, `SECURITY_PROFILE`, `CI_CD_PROFILE`, `OPERATION_PROFILE` v1.0.0; `CORE_ROLES` — все 9 ролей в порядке каталога ADR-007, совпадает с `Role`; 8 скиллов: `infra-design`/`infra-change` (проектирование и реализация IaC: Helm/K8s, окружения, сеть, IAM, plan/diff-валидация), `threat-model`/`security-analysis` (trust boundaries и secure design; IAM, secrets, зависимости — supply chain), `pipeline-delivery`/`task-git-cycle` (пайплайн как код с зеркалированием локальных проверок; ветка задачи, conventional commits, MR с evidence — merge остаётся решением человека, ADR-011), `smoke-verification`/`rollback-analysis` (smoke-свидетельства выпущенной ревизии и путь отката для вердикта Operation); `ArtifactKind` +4 аддитивных значения (`infrastructure_change`, `security_review`, `pipeline_config`, `ops_verdict`); Operation отвечает за вердикт гейта release, машина вердикта — детерминированная политика (T034), определение human/операционного вердикта — отдельное решение (ADR-023), новый гейт не моделировался; `STAGE_ROLE`/`STAGE_SKILL` не менялись — release остаётся не-агентной работой (ADR-024 §7 S4): профиль ci_cd теперь есть, и стадия по-прежнему честно останавливается `blocked` — на отсутствии skill-маппинга (тест остановки обновлён, поведение прежнее); консольный мета-снапшот перегенерирован (+4 профиля), витест-ассерт состава ролей обновлён (9 ролей); `docs/descriptions/agents.md` синхронизирован (таблицы профилей и скиллов, ArtifactKind, счётчики 9/20, error-матрица: ProfileNotFoundError для ролей каталога недостижим). Тесты: `test_agents_profiles.py` (CORE_ROLES = 9 = Role, у каждой роли полный манифест), `test_agents_skills.py` (20 скиллов, kebab-case, привязка к профилям ролей). Валидация: ruff check + format --check (358 файлов) + mypy strict (297 файлов) — чисто; pytest — 1758 passed + 70 skipped (интеграции без БД — штатно); console vitest — 57 passed)
- [ ] T048 [P] [docs T-084] Learning loop (минимальный контур): сигнал → RuleProposal/SkillProposal → проверка на eval-наборе (Pydantic Evals) → MR с независимым review; защита holdout-набора, сравнение с baseline. Зависит от T044, T039 — `src/dark_factory/learning/`
- [ ] T049 [P] [docs T-085] Auto-merge низкого риска (policy-controlled): по статистике T043 — политика для R0/R1 при зелёных гейтах, audit-log оснований, лёгкое отключение. Зависит от T045, T043 — `src/dark_factory/orchestration/policy/`
- [ ] T050 [P] [docs T-086] Preview namespace на MR: детерминированное создание/удаление (TTL, cleanup после merge/close). Зависит от T032 — `deploy/preview/`
- [ ] T051 [P] [docs T-090] Event-driven fast jobs (`@factory review` / `fix-ci`): короткие агентные операции по CI-событиям без полного Change Flow, те же политики безопасности и лимиты. Зависит от T025, T020 — `.github/workflows/`
- [ ] T052 [P] [docs T-087] Foundation: каталог блоков (owner, maturity, зависимости) и scorecards, управляемые обновления через migration MR. Зависит от T042, T048 — `src/dark_factory/foundation/`
- [ ] T053 [P] [docs T-088] Marketplace packs: формат CapabilityPack/FactoryPack (skills+agents+gates+policies+evaluations), SemVer, CI-валидация манифестов, публикация в registry, установка в проект. Зависит от T052 — `packs/`
- [ ] T054 [P] [docs T-089] Второй harness-адаптер: внешний CLI-агент как `HarnessPort`-адаптер; проверка смены исполнителя без изменения Flow. Зависит от T019 — `src/dark_factory/adapters/harness_alt/`
- [ ] T055 [P] [docs T-091] Миграция в shared/DC-контур: TLS/OIDC, Vault/external secrets, устойчивое хранилище артефактов, retention/backup, повышение concurrency после capacity-теста, разделение prod/non-prod Argo CD; пересмотр Temporal только при подтверждённых требованиях. Зависит от T031, T032, T043 — `charts/dark-factory/` (values-dc)
- [x] T058 Параметризация этапов CI: у каждого job `ci.yml` — выключатель `CI_SKIP_<JOB>` (repository variable; opt-out и fail-safe: этап выполняется, пока переменная не равна ровно `true`, опечатка гейт не убирает); image-этапы получают толерантный страж зависимостей (намеренно пропущенный гейт сборку не глушит, упавший или отменённый — глушит). Документация — `docs/instructions/manage-ci-stages.md` (управление плюс описание каждого этапа и его локальный эквивалент), `docs/adr/ADR-026-parameterizable-ci-stages.md`; drift-тест `tests/test_ci_stage_toggles.py`. DoD: переключатели у всех 20 этапов, инструкция и ADR согласованы с workflow, диагностика и тесты зелёные — `.github/workflows/ci.yml`, `docs/`, `tests/` (реализовано: `if: vars.CI_SKIP_<JOB> != 'true'` у всех 20 jobs, значения не читаются, когда переменная не задана (fail-safe: опечатка `True`/`yes`/`1` не выключает гейт); `factory-image`/`console-image` получили страж `!cancelled() && !contains(needs.*.result, 'failure') && !contains(needs.*.result, 'cancelled')` — скипнутый намеренно гейт сборку не глушит, упавший/отменённый глушит, `needs`-цепочка и итоговый SHA не изменены; ADR-026 зарегистрирован в `docs/adr/README.md`, `docs/development-workflow.md` содержит правило «переключатели только для разработки, перед merge — полный набор»; инструкция: быстрый старт (`gh variable set/delete/list`, веб-UI), правила безопасности (в т.ч. required status checks), таблицы всех 20 этапов с весом и локальным эквивалентом, разбор симптомов, ограничения. Валидация: `ruff check` + `ruff format --check` — чисто; `mypy .` strict — 280 файлов без замечаний; `pytest` — 1528 passed, 64 skipped; 9 новых тестов (`tests/test_ci_stage_toggles.py`) зелёные; `ci.yml` — валидный YAML, 20/20 jobs с переключателем; доступность `vars`, `needs`, `cancelled()` в `jobs.<job_id>.if` и замена ими неявного `success()` подтверждены документацией GitHub Actions (Contexts/Evaluate expressions). Ограничение: значения переменных живут в настройках GitHub, не в git; per-PR переключатели — возможная доработка (ADR-026, альтернативы))
- [x] T059 Управление этапами CI из консоли: каталог этапов в коде (`src/dark_factory/ci/stages.py`), порт `CiStageTogglePort`, адаптер `GitHubCiStageToggles` (repository variables REST) и эндпоинты `GET /api/v1/ci/stages` / `PUT /api/v1/ci/stages/{job}`; экран консоли `/ci`; опциональная обвязка chart; ADR-027. DoD: оператор включает/выключает этап из консоли, дрейф каталога с `ci.yml` ловится тестом — `src/dark_factory/ci/`, `src/dark_factory/api/routes_ci.py`, `console/src/pages/CiStagesPage.tsx`, `charts/dark-factory/` (реализовано: каталог 20 этапов как единый источник истины (job/title/group/summary/local_command/weight + `variable_for_job`, `is_skipped_value`; `title` = `name:` джобы, `variable` = переменная из её `if` — проверяется `tests/test_ci_stage_toggles.py`); `CiStageTogglePort.values()`/`set_value(variable, value|None)` — value-level и привязан к одному репозиторию при конструировании (запрос не перенаправит запись); `GitHubCiStageToggles` — Actions variables REST с пагинацией до конца списка, PATCH→POST при 404 (в REST нет upsert), DELETE (404 = уже отсутствует), тем же installation token и правом Variables: read/write; эндпоинты: GET отдаёт каталог + состояние (GET без токена, локальный контур ADR-009 п.7), `enabled=false` ⇔ значение `true` (регистр не различается — зеркало условия workflow), PUT `{"enabled": bool}` (строгий bool, `"yes"`/`1` → 422) пишет `true` или удаляет переменную, идемпотентно; авторизация — Bearer со scope `ci:write` И ролью operator (агенты не перенастраивают пайплайн, который их гейтит) → 401/403, неизвестный job → 404 (никогда не произвольная переменная), контур без конфигурации → `available=false` + reason у GET и 503 у PUT, ошибка провайдера → 502 с санитизированным текстом; композиция — `build_runtime` строит адаптер по `DARK_FACTORY_GITHUB_*` + новому `DARK_FACTORY_GITHUB_REPOSITORY_SLUG`, `runtime/entrypoint` для `api serve` собирает Runtime и передаёт биндинг аргументом (core-путь без seams остаётся `available=false`); chart — `ciToggles.existingSecret` (envFrom secretRef, `optional: true`) + `ciToggles.repositorySlug`, без них рендер не меняется; консоль — экран `/ci` (группы, вес, локальная команда, `button[role=switch]`, баннеры безопасности и «не сконфигурировано», кнопка «включить все этапы», 401/403/503/502-разбор), новых npm-зависимостей нет. Валидация: `ruff check` + `ruff format --check` — чисто; `mypy .` strict — 286 файлов без замечаний; `pytest` — 1572 passed, 64 skipped (было 1528; +44: каталог/дрейф, API, адаптер, chart-рендер и 4 теста точки входа); chart — `helm`-рендер 18 тестов, включая новый (envFrom + slug, и отсутствие обвязки по умолчанию); консоль — ESLint 0 ошибок, `tsc --noEmit`, 57 vitest, `vite build`, 6 Playwright e2e; wire-контракт снят с живого приложения (TestClient) и совпал с зафиксированным контрактом консоли (состав ключей, `enabled:false`, PUT → удаление переменной, `available:false` + reason). Корреляция с SDD-артефактами: `CiStageTogglePort` добавлен в `specs/001-dark-factory-mvp/contracts/ports.md` (код заявляет 1:1 с этим файлом), эндпоинты `/ci/stages` и scope `ci:write` — в `contracts/api.md`; `docs/adr/ADR-025` (ревизионная пометка) и `docs/descriptions/runtime.md` приведены в соответствие — `api serve` теперь собирает `Runtime` ради биндинга (конституция IV: «устаревшие документы обновляются»); 4 новых теста точки входа (`tests/test_runtime_entrypoint.py`: сборка и передача `ci_toggles`/`ci_repository`, отсутствие конфигурации → `None`, освобождение runtime при ошибке, доставка биндингов в `cli.api.run_api_serve_command`). Ограничение: состояние живёт в настройках GitHub, не в git; per-PR переключатели и read-only `/meta/*` — возможные доработки (ADR-026/ADR-027); App требует права Variables: read/write, включаемые только явной конфигурацией контура)
- [ ] T056 Синхронизация документации и финальная приёмка: актуализировать статусы этого трекера, `docs/hld.md`/ADR при значимых решениях, проверить `quickstart.md` end-to-end, зафиксировать DoD-отчёт — `docs/`, `specs/001-dark-factory-mvp/quickstart.md`

---

## Phase 11: ChangeSet workspace (путь к приёмке MVP)

**Purpose**: довести фабрику до состояния, в котором MVP принимается: оператор ведёт изменение от идеи до работающего приложения из Console/CLI — без низкоуровневых команд и ручных правок артефактов агента. План — `docs/plan-changeset-workspace-mvp.md`; решения — [ADR-030](../../docs/adr/ADR-030-product-registry.md)…[ADR-037](../../docs/adr/ADR-037-console-changeset-workspace-ia.md).

**Independent Test**: на новом чистом репозитории калькулятора пройти `продукт → требования → архитектура → UI → план → исполнение → демонстрация → доставка` из Console и CLI; на каждом шаге виден ровно один следующий шаг (CLI и Console совпадают).

- [x] T061 План и ADR рабочего пространства ChangeSet: `docs/plan-changeset-workspace-mvp.md` (проблема, решения оператора, Phase-проекция, `Guidance`, помощник merge, workstreams W1–W9, milestones M1–M5, DoD приёмки, вне объёма) и ADR-030…ADR-037 (product registry; repository provisioning; phase projection; operator guidance; conversations/rework; document artifacts; merge assistant; console IA); реестр `docs/adr/README.md`, индекс `docs/README.md`, техдолг TD-030/TD-031 и уточнение ADR-011 обновлены. DoD: план зафиксирован, ADR зарегистрированы, задачи T062–T118 (декомпозиция M0–M5) добавлены. — `docs/` (реализовано 2026-09-19: план + 8 ADR + реестр/индекс/техдолг/пометка ADR-011; проверки — номера ADR уникальны, внутренние ссылки разрешаются, битых символов нет; MR против `main`, merge — человек, ADR-011)

Декомпозиция вех (M0 — предусловия, M1–M5 — ценность для оператора). Задачи нумеруются один раз и завершаются независимо; гранулярность M3–M5 уточняется при старте вехи (Native SDD Core, ADR-020).

### M0 — Предусловия приёмки (дефекты, найденные пилотом T043)

- [x] T062 M0 — B1: риск-осознанный маппинг гейтов в `ScmFactsProvider._approvals` (`runtime/facts.py`): провайдер отдаёт ревью plan-CR с гейтом `Gate.REVIEW`, из-за чего человеческая точка `solution` (R2) не закрывается и standard-изменения клинят. DoD: `_approvals` учитывает `RISK_HUMAN_GATES` и маршрут рана; точка `solution` закрывается наблюдаемым approving review на итоговом SHA; тесты на quick/standard × R1/R2 — `src/dark_factory/runtime/facts.py`, `tests/` (журнал T043, B1; закрыто 2026-09-19: фикс доставлен ранее в T043 W1 (MR #103, `bb9a302`) — `_approvals` читает гейт из `required_human_gates(route, stage, risk_class)`; T062 добавляет DoD-матрицу quick/standard × R1/R2 в `tests/test_runtime_facts.py` (планирующий гейт: R1 → fallback `review`, R2 → `planning`; specification/review неизменны), проверки — 19 passed, ruff/mypy чисто)
- [x] T063 M0 — B2: атрибуция гейта входа в construction исходящей стадии: retry перезапускает завершённое planning (лишний LLM-прогон и лишний CR). DoD: гейт входа атрибутируется стадии/точке, которой принадлежит; retry не переделывает завершённую работу; регрессионные тесты — `src/dark_factory/orchestration/` (закрыто 2026-09-19: гейт перенесён в префлайт стадии Construction — `stages/checks.construction_entry_reason` применяют оба `StageExecutor` до workspace/harness/публикации; `StageContext` получил `implementation_contract` + `enforce_contract_entry`, драйвер передаёт `run.implementation_contract`, одиночный `factory stage run` явно отказывается от гейта; `planning` остаётся `succeeded`, retry — следующая попытка construction без LLM/CR; проверки — pytest 1846 passed/74 skipped, ruff/mypy чисто; журнал T043)
- [ ] T064 M0 — TD-030: операторское снятие/отзыв паркованного run. DoD: отдельный переход `withdraw`/`cancel` с терминальным результатом и записью решения; доступен оператору (CLI/API, роль operator); история не перезаписывается; повтор идемпотентен; негативные тесты — `src/dark_factory/orchestration/`, `src/dark_factory/cli/`, `src/dark_factory/api/`, `docs/tech-dept.md` (TD-030)

### M1 — Продукт, репозиторий и intake (T065–T077)

- [x] T065 M1 — Агрегат `Product` + `ProductRepository` + миграция. DoD: поля по ADR-030 (id/name/description/repository/baseline_ref/dev_env_ref/status/created_at); связь `Change.product_id` обратно совместима (изменения без продукта читаются); миграция обратима; round-trip тесты — `src/dark_factory/changes/`, `src/dark_factory/orchestration/state/`, `migrations/` (закрыто 2026-09-19: `Product` + `ProductStatus` + `ProductRepository` (идемпотентный `create` по `id`, `get`, `list`, `update_status` под optimistic `state_revision`); `Change.product_id` опционален и обратно совместим (payload без поля валидируется); миграция `0005_products` обратима (`0005 → 0004 → head`, единственный head); проверки — ruff/mypy чисто, pytest 1864 passed/86 skipped без БД и 89 passed/4 skipped на PostgreSQL; head-константы интеграционных тестов обновлены)
- [ ] T066 M1 — API продуктов: `GET /products`, `POST /products`, `GET /products/{id}`, `POST /products/{id}/validate`. DoD: создание/чтение продукта, статус `validating → ready | error`, требуются scope и роль operator, audit-запись, RFC 7807-ошибки; контракт описан — `src/dark_factory/api/`, `docs/descriptions/api.md`
- [ ] T067 M1 — `RepositoryProvisioningPort` + адаптер `LocalMirror`. DoD: порт (`validate` / `ensure_mirror` / `bootstrap_baseline`); существующее зеркальное поведение вынесено в адаптер без регрессии; contract-тесты на фейке и `LocalMirror` — `src/dark_factory/ports/`, `src/dark_factory/adapters/`
- [ ] T068 M1 — Адаптер `ProviderClone`: clone/fetch с провайдера на GitHub App installation-токене. DoD: PAT не вводится; отсутствие/невалидность кредов — fail-closed диагностика без эха секрета; репозиторий вне установки App — явная ошибка с инструкцией; contract-тесты через эмулятор — `src/dark_factory/adapters/scm/`, `tests/contract/`
- [ ] T069 M1 — `bootstrap_baseline`: применение паков к пустому репозиторию + readiness evidence. DoD: пустой репозиторий (unborn HEAD) — штатный случай; применение идемпотентно (`effect_key`), повтор не создаёт второй эффект; результат — evidence (ревизия, паки и версии) — `src/dark_factory/adapters/`, `packs/`
- [ ] T070 M1 — CLI продуктов: `factory product add|validate|list|show`. DoD: команды создают продукт и показывают статус готовности; exit-коды по контракту CLI; секреты не эхоятся; тесты парсера и рендера — `src/dark_factory/cli/`, `docs/descriptions/cli.md`
- [ ] T071 M1 — Intake с брифом, сценарием и лимитом расхода (домен + API). DoD: `Change` принимает структурированный бриф (проблема/цель/ограничения/вне скоупа), сценарий `specs-only | full` и лимит расхода; лимит сохраняется и участвует в бюджете; тесты валидации — `src/dark_factory/changes/`, `src/dark_factory/api/`
- [ ] T072 M1 — Агент «Помоги сформулировать»: свободное описание → структурированный бриф. DoD: исходный текст сохраняется; при недоступности harness или отказе агента бриф остаётся черновиком, ошибка наблюдаема; тесты на фейковом harness — `src/dark_factory/agents/`, `src/dark_factory/orchestration/`
- [ ] T073 M1 — CLI задач: `factory change create --brief`, `factory change status`. DoD: задача создаётся с брифом и сценарием; `change status` печатает блок «следующий шаг» (`Guidance`), совпадающий с Console; тесты рендера — `src/dark_factory/cli/`
- [ ] T074 M1 — `Guidance` v1: серверная проекция «следующего шага» для продукта и задачи + `GET /changes/{id}/guidance`. DoD: детерминированная функция состояния (`headline`/`why`/`primary`/`secondary`/`blockers`/`after`); покрыты состояния «продукт не готов», «задача без спеки», «ожидание гейта», «blocked», «исчерпан лимит»; exhaustive-тесты — `src/dark_factory/orchestration/`, `src/dark_factory/api/`
- [ ] T075 M1 — Console: каркас IA, уровень «Продукты», страница продукта. DoD: глобальная навигация `Продукты · Требует внимания · Активность`; список продуктов с состоянием; страница продукта (обзор, изменения, база, доставки) и действие «Новая фича»; существующие экраны перенесены в служебную область; vitest + Playwright smoke — `console/`
- [ ] T076 M1 — Console: intake и компонент «Следующий шаг». DoD: форма брифа (4 поля + «Помоги сформулировать»), выбор сценария, лимит и прогноз; компонент «Следующий шаг» рендерит `Guidance` и объясняет причину недоступного действия; e2e smoke — `console/`
- [ ] T077 M1 — Приёмка M1 (живой прогон). DoD: на новом чистом репозитории калькулятора из интерфейса создан продукт `ready` (с evidence провижининга) и задача с выбранным сценарием; CLI и Console показывают один и тот же следующий шаг — `docs/instructions/`

### M2 — Обсуждения, документы и требования (T078–T091)

- [ ] T078 M2 — Домен обсуждений: `Question` / `Answer`. DoD: типы ответа `choice | text | number`; привязка к фазе и фрагменту артефакта; статусы `open → answered → resolved | stale`; миграция; тесты переходов — `src/dark_factory/changes/`, `migrations/`
- [ ] T079 M2 — Домен обсуждений: `Comment` с якорем и `ReworkOrder`. DoD: `Comment` несёт `artifact + anchor_id + revision`; потерянная привязка помечается явно и не переносится на другой элемент; `ReworkOrder` ссылается на версию артефакта и набор замечаний; тесты — `src/dark_factory/changes/`
- [ ] T080 M2 — Семантика решений и staleness. DoD: комментарий не запускает доработку автоматически (нужно «Отправить агенту»); «просмотрено» ≠ «согласовано»; новая ревизия помечает прежние согласования и проверки неактуальными; отметка агента «исправлено» переводит в «готово к повторной проверке», но не закрывает; тесты — `src/dark_factory/orchestration/`
- [ ] T081 M2 — Цикл доработки: счётчик раундов и эскалация. DoD: `ReworkOrder` исполняется через существующий `plan_rework` (≤3 раунда); счётчик считает раунды повторного исполнения, не комментарии; исчерпание — карточка эскалации; интеграционные тесты — `src/dark_factory/orchestration/` (ADR-018 п.5)
- [ ] T082 M2 — Read-model дерева артефактов ChangeSet. DoD: типы `spec | design | adr | ui | plan`, пути, ревизии, frontmatter-свойства поверх git; «артефакта нет» и «ревизия устарела» различимы; тесты — `src/dark_factory/context/`, `src/dark_factory/api/`
- [ ] T083 M2 — Ревизии, diff и свойства документа. DoD: список ревизий и diff между ними; frontmatter-свойства как данные, полный YAML доступен в исходнике; системные идентификаторы защищены от изменения; тесты — `src/dark_factory/api/`
- [ ] T084 M2 — Write-through: правка артефакта → коммит в ветку изменения. DoD: правка из API/CLI становится коммитом через `RepositoryPort.publish_commit` (идемпотентно по `effect_key`); конфликт параллельной правки не разрешается молча; тесты — `src/dark_factory/api/`, `src/dark_factory/ports/`
- [ ] T085 M2 — Черновик автосейва вне git. DoD: незавершённые правки хранятся отдельно и коммитятся явным «сохранить»; состояние сохранения видимо; потеря черновика не затрагивает источник истины; тесты — `src/dark_factory/api/`
- [ ] T086 M2 — API и CLI обсуждений и артефактов. DoD: вопросы (список/ответ), комментарии, поручения; артефакты (`get | edit | diff | versions`); CLI `change status | answer | artifacts`; контракт API обновлён; тесты — `src/dark_factory/api/`, `src/dark_factory/cli/`, `docs/descriptions/api.md`
- [ ] T087 M2 — Прекондиции гейтов фазы. DoD: гейт недоступен при блокирующих вопросах; причина недоступности человекочитаема и содержит действие по снятию; пропуск фазы (например, UI не требуется) фиксируется с основанием; тесты — `src/dark_factory/orchestration/`
- [ ] T088 M2 — Console: оболочка экрана ChangeSet. DoD: верхняя панель (фаза, состояние, бюджет, блокеры); левая колонка фаз; центр с вкладками Результат · Изменения · Проверки · История; правая контекстная панель (сворачиваемая); нижняя панель решения с одной CTA; «процента готовности» нет; e2e smoke — `console/`
- [ ] T089 M2 — Console: фаза «Требования». DoD: дельта требований относительно baseline, стабильные ID, карточки вопросов с ответом в один клик, карточки допущений (подтвердить/исправить/варианты), комментарии к фрагменту, «на доработку» со сводкой «что изменил / что осталось»; e2e — `console/`
- [ ] T090 M2 — Console: markdown-редактор. DoD: режимы Документ / Markdown / Чтение, панель свойств, история версий, комментарии к выделенному тексту, автосохранение черновика; переключение режимов не теряет и не переписывает содержимое; неизвестные конструкции сохраняются дословно; round-trip тесты — `console/`
- [ ] T091 M2 — Приёмка M2 (живой прогон). DoD: цикл «вопрос → ответ → правка → сводка → согласовать / на доработку» пройден из CLI и Console; правка стала коммитом; после правки прежние согласования и проверки помечены неактуальными

### M3 — Архитектура и интерфейс (T092–T099)

- [ ] T092 M3 — Артефакты проектирования: ADR и design в ChangeSet. DoD: ADR/design создаются агентом как артефакты фазы (frontmatter, статус предложено/принято/требует пересмотра); ADR редактируется как документ; тесты — `src/dark_factory/agents/`, `src/dark_factory/context/`
- [ ] T093 M3 — Обзор решений и «Запросить альтернативу». DoD: карточка решения (предложение, обоснование, альтернативы, последствия, влияние, статус); отказ относится к конкретному решению и оформляется поручением; после пересмотра показаны затронутые артефакты; тесты — `src/dark_factory/changes/`, `src/dark_factory/orchestration/`
- [ ] T094 M3 — Домен UI-спеки: сценарии, экраны, связи. DoD: сценарии (шаги), экраны (галерея и состояния loading/empty/error/success/доступ), связи и развилки, маппинг на компоненты UIKit; устойчивые идентификаторы элементов; тесты — `src/dark_factory/changes/`, `src/dark_factory/api/`
- [ ] T095 M3 — Console: фаза «Архитектура». DoD: обзор решений с альтернативами и схемой, ADR как документ, «Запросить альтернативу» без ухода с экрана; согласование фиксирует версию; e2e — `console/`
- [ ] T096 M3 — Console: фаза «Интерфейс» (`Сценарии / Экраны / Связи`). DoD: по умолчанию открываются «Сценарии»; галерея экранов с состояниями; ссылка на dev-URL; комментарии к элементам с явной пометкой потерянной привязки; e2e — `console/`
- [ ] T097 M3 — Гейт UI-фазы и честный статус неавтоматизируемых проверок. DoD: согласование UI фиксирует версию; axe/visual regression показываются как «запланировано на исполнении» без зелёного статуса; backend-only изменение даёт «Не требуется» с основанием; тесты — `src/dark_factory/orchestration/`
- [ ] T098 M3 — Проекция фаз и `GET /changes/{id}/phases`. DoD: восемь фаз выводятся из стадий и точек контроля, включая разделение «Архитектура»/«Интерфейс» внутри `specification`; маппинг покрыт тестами на согласованность с таблицей переходов — `src/dark_factory/orchestration/`, `src/dark_factory/api/`
- [ ] T099 M3 — Приёмка M3 (живой прогон). DoD: архитектура и UI приняты или отправлены на доработку из интерфейса; решения версионно привязаны; правка после согласования помечает согласование неактуальным

### M4 — План и исполнение (T100–T107)

- [ ] T100 M4 — Read-model плана: задачи, покрытие AC, критический путь. DoD: список задач по пользовательским результатам (результат, связанные AC, зависимости, роль, оценка с неопределённостью, состояние); покрытие AC; доступный параллелизм; критический путь; прогноз против лимита; тесты — `src/dark_factory/api/`
- [ ] T101 M4 — Корректировка скоупа из плана. DoD: правка показывает предлагаемую корректировку (затронутые требования, экраны, задачи); изменение требований возвращается на согласование фазы; перестановка задач без изменения смысла согласуется в плане; тесты — `src/dark_factory/orchestration/`
- [ ] T102 M4 — Read-model исполнения. DoD: что завершено, что выполняется параллельно, что заблокировано и почему; последнее событие; связанные задачи и AC; расход и прогноз; ссылка на MR; тесты — `src/dark_factory/api/`
- [ ] T103 M4 — Управление исполнением: pause / resume / cancel / correct. DoD: точные семантики (pause — не запускать новое; resume — после проверки актуальности входных артефактов; cancel — прекратить дальнейшее, сохранив результаты и историю; correct — поручение с определением затронутых результатов); отмена не откатывает совершённые внешние действия, их состояние показывается отдельно; тесты — `src/dark_factory/orchestration/`, `src/dark_factory/api/`
- [ ] T104 M4 — Hard-stop бюджета и прогноз расхода. DoD: лимит из intake останавливает ChangeSet с точной диагностикой; прогноз остатка виден; разрыв по неизвестной стоимости закрыт или явно задокументирован; тесты — `src/dark_factory/orchestration/budget/` (TD-006, TD-007)
- [ ] T105 M4 — Console: фаза «План». DoD: список задач с покрытием AC и критическим путём; корректировка скоупа с предпросмотром последствий; WorkGraph-граф не реализуется (вне объёма); e2e — `console/`
- [ ] T106 M4 — Console: фаза «Исполнение». DoD: карточка активной работы (роль, действие, длительность, последнее событие, связанные задача и AC, переходы «Открыть результат / События / MR»); лента событий; сырые логи отдельно; контролы с подтверждением; e2e — `console/`
- [ ] T107 M4 — Приёмка M4 (живой прогон). DoD: план утверждён или скорректирован из интерфейса; исполнение наблюдаемо и управляемо; лимит расхода останавливает работу

### M5 — Демонстрация, доставка и закрытие (T108–T118)

- [ ] T108 M5 — Матрица AC → результат → evidence. DoD: сводка (реализовано / проверено автоматически / требует проверки / блокирующие); evidence относится к конкретной версии кода и запуску; устаревшая проверка не показывается актуально пройденной; тесты — `src/dark_factory/quality/`, `src/dark_factory/api/`
- [ ] T109 M5 — Замечания из превью, AC и diff. DoD: замечание создаётся из preview/AC/diff с авто-подстановкой контекста и версии; агент предлагает классификацию (дефект реализации / несоответствие UI / изменение требований), оператор может её исправить; возврат в требования даёт перечень затронутых артефактов и согласований; тесты — `src/dark_factory/changes/`, `src/dark_factory/api/`
- [ ] T110 M5 — Превью и smoke в ChangeSet. DoD: dev-URL и результат smoke как evidence фазы демонстрации; неуспешный smoke не даёт статус «выпущено»; тесты и живой прогон — `src/dark_factory/quality/release/`, `src/dark_factory/api/`
- [ ] T111 M5 — Модель промоушена для multi-image продукта (закрывает TD-031). DoD: ADR + реализация — единица релиза это набор digest'ов, промоушен это MR, правящий desired-state overlay, откат это revert; `ReleaseStageExecutor` и release-опции драйвера приведены в соответствие; атрибуция digest → изменение определена; тесты — `docs/adr/`, `src/dark_factory/orchestration/stages/`, `docs/tech-dept.md` (TD-031)
- [ ] T112 M5 — «Помощник merge»: чек-лист и версионно-привязанное решение. DoD: чек-лист (required checks, approving review на head SHA, branch protection, согласования фаз) собирается из наблюдаемого состояния; каждый блокер человекочитаем и имеет действие по снятию; approve в консоли фиксирует версионно-привязанное решение; тесты — `src/dark_factory/api/`, `src/dark_factory/orchestration/policy/`
- [ ] T113 M5 — «Помощник merge»: merge как явное человеческое действие. DoD: подтверждаемый merge через `MergeRequestPort.merge(expected_sha)`; несовпадение SHA блокирует и не создаёт эффекта; решение записывается как человеческое; автомержей нет; после merge фабрика продолжает по наблюдаемому merge; CLI-эквивалент; тесты — `src/dark_factory/api/`, `src/dark_factory/cli/`
- [ ] T114 M5 — Console: фаза «Демонстрация и проверка». DoD: сводка по AC, матрица AC → результат → evidence, замечание из превью, счётчик «доработка N из 3», карточка эскалации; e2e — `console/`
- [ ] T115 M5 — Console: фаза «Доставка» и закрытие. DoD: последовательные состояния (готово к merge → merge выполнен → доставка в dev → smoke → reconciliation → закрыто); «Подтвердить baseline и закрыть» доступно только после обязательных проверок и просмотра дельты; неоднозначные состояния не сводятся к «Ошибка»; e2e — `console/`
- [ ] T116 M5 — Console: inbox «Требует внимания». DoD: сквозной по продуктам inbox (вопрос агента, результат к согласованию, противоречия, превышение бюджета, исчерпаны три цикла, готово к merge, подтверждение baseline); каждый пункт ведёт к конкретному вопросу, фрагменту или решению; e2e — `console/`
- [ ] T117 M5 — CLI-паритет M5: `change merge-status | merge`, `change demo`, `change withdraw`. DoD: те же возможности и тот же `Guidance`, что в Console; тесты — `src/dark_factory/cli/`
- [ ] T118 M5 — Приёмка MVP (сквозной прогон). DoD: на новом чистом репозитории калькулятора пройден маршрут «продукт → требования → архитектура → UI → план → исполнение → демонстрация → доставка» из Console и CLI, без ручных правок артефактов агента и без низкоуровневых команд; согласования версионно привязаны; следующий шаг виден на каждом шаге; лимит расхода соблюдён; отчёт с evidence; синхронизация `docs/hld.md` и описаний — `docs/`

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: без зависимостей — выполнено.
- **Foundational (Phase 2)**: зависит от Setup — **БЛОКИРУЕТ все user stories**. Порядок внутри: T004 → T005 → T006.
- **US1 (Phase 3)**: зависит от Foundational (T005, T006).
- **US2–US6 (Phases 4–8)**: зависят от Foundational; внутри каждой — свой порядок.
- **Pilot (Phase 9)**: зависит от P0-цепочки US3–US6.
- **Polish/Extensions (Phase 10)**: зависит от соответствующих ранних задач; не блокирует MVP.
- **ChangeSet workspace (Phase 11)**: M0 (T062–T064) → M1 (T065–T077) → M2 (T078–T091) → M3 (T092–T099) → M4 (T100–T107) → M5 (T108–T118). M0 — предусловия (дефекты пилота: B1, B2, TD-030). Не зависит от Polish/Extensions, но пересекается с Pilot (T043) по живому продукту; полная приёмка MVP — T118.

### User Story Dependencies

- **US1 (P1)** — MVP, не зависит от других историй.
- **US2 (P2)** — независима; использует T005/T006.
- **US3 (P3)** — независима; использует T005, T014 (US2).
- **US4 (P4)** — зависит от Foundational; использует `ChangeRequestRef`/порты (T006).
- **US5 (P5)** — зависит от Foundational и результата T025 (CI-шаблоны, US4). Фазовая зависимость «US5 после US4» не применяется: T025 и US5 связаны interleaved-цепочкой T029 → T030 → T025 → остальная US5.
- **US6 (P6)** — зависит от Foundational и артефактов US4 (T024) для трассировки.

### Критические межстадийные зависимости (interleaved)

Порядок внутри каждой истории корректен, но несколько рёбер связывают истории между собой (interleaved; унаследованы из исторического реестра) — их нужно учитывать при планировании прогона:

- `T017 [US2 / T-021]` требует `T023 [US3 / T-016]` → T023 выполняется до T017.
- `T014 [US2 / T-011]` требует `T015 [US2 / T-012]` и `T019 [US3 / T-010]` → T019 выполняется до T014.
- `T018 [US2 / T-022]` требует `T041 [Pilot / T-070]`.
- `T025 [US4 / T-031]` требует `T030 [US5 / T-041]` (реестр: docs T-031 зависит от T-041, не T-040).
- `T028 [US4 / T-064]` требует `T029 [US5 / T-040]`.
- `T031 [US5 / T-042]` требует `T035 [US6 / T-050]`.
- `T039 [US6 / T-061]` требует `T024 [US4 / T-030]`.
- `T043 [Pilot / T-072]` требует всей P0-цепочки: T023, T025, T026, T031, T033, T034, T035, T039, T041.

### Внутри каждой задачи

- Миграции/схема → Flow → порты (Foundational).
- Модели/контракты → сервис → endpoint/CLI.
- Реализация → интеграция.
- DoD (`ruff` + `mypy --strict` + `pytest`) зелёные до перехода к следующей задаче.

### Parallel Opportunities

- Foundational: после T004 задачи T005 и T006 можно вести параллельно (разные файлы).
- US2: T015 → T014 (реестр: docs T-011 зависит от T-012); затем T016 → T017.
- US3: T019 и T020 параллельны; затем T021/T022/T023.
- US5: T029 и T030 параллельны.
- US6: T035, T037, T038 параллельны.
- Polish: T045–T055 в основном параллельны (разные модули), при наличии зависимостей из Phase 9.

---

## Sequential Example: User Story 2

```bash
# Последовательно (реестр: docs T-011 зависит от T-012):
Task: "T015 [US2] ContextBundle + KnowledgePort/ExecutionPort — src/dark_factory/context/"
Task: "T014 [US2] Профили ядра Product/Develop/Quality + skills — src/dark_factory/agents/"
Task: "T016 [US2] Native SDD Core + SDDPort — .factory/"
Task: "T017 [US2] Specification gate — src/dark_factory/quality/gates/"
```

## Parallel Example: User Story 3

```bash
Task: "T019 [US3] HarnessPort + PydanticAI-адаптер — src/dark_factory/adapters/harness/"
Task: "T020 [US3] Quality — независимая приёмка — src/dark_factory/quality/"
```

---

## Implementation Strategy

### MVP First (User Story 1)

1. Phase 1 Setup — выполнено (T001–T003).
2. Phase 2 Foundational — критично, блокирует всё (T004–T006).
3. Phase 3 US1 — walking skeleton (T007–T013).
4. **STOP и VALIDATE**: quickstart §1–§2 — CLI-стадия локально и в CI без LLM, идемпотентный повтор.

### Incremental Delivery

1. Foundational → US1 → demo (MVP: одна стадия без LLM).
2. US2 → согласованная спецификация с version-bound approval.
3. US3 → агентная реализация + review + rework.
4. US4 → автономный MR-контур и merge.
5. US5 → релиз в dev через GitOps + smoke.
6. US6 → контекст, трекер, Console, наблюдаемость.
7. Pilot → 10 задач e2e (SC-001…SC-008), затем P1–P3 расширения.

### Parallel Team Strategy

1. Foundational — вместе.
2. Далее потоки: Core/Agents (US2+US3) ∥ Delivery (US4+US5) ∥ Console/API (US6), с учётом interleaved-рёбер из раздела выше.

---

## Notes

- `[P]` — разные файлы, допускает параллельный запуск с другими задачами волны; не отменяет перечисленные зависимости задачи.
- `[Story]` — трассировка к user story `spec.md`; `[docs T-0NN]` — исторический номер из реестра `docs/plan.md` (удалён 2026-09-17). Якорь отсутствует у T007–T013 (implementation slice US1) и T056 (финальная синхронизация) — у них нет отдельных задач в реестре.
- Каждая задача независимо завершаема и проверяема; договорённости — в `contracts/` и `data-model.md`.
- Тесты — часть DoD задачи (не отдельные задачи): unit + contract + integration, включая crash-тесты effect ledger и exhaustiveness таблицы переходов.
- Коммиты — Conventional Commits на английском, только по явной просьбе пользователя (конституция V).
- Останавливаться на checkpoint'ах для независимой проверки истории.
- Избегать: расплывчатых задач, конфликтов по одному файлу, межстадийных зависимостей, ломающих независимость (кроме явно перечисленных interleaved-рёбер).
