# Реестр технического долга

Ведёт роль `architect`. Одна запись — один осознанно отложенный дефект или ограничение: что это, чем платим, когда гасить. Записи не «вечные»: каждая должна иметь план погашения или явную причину жить с этим.

| ID | Статус | Описание | Влияние | План погашения |
|---|---|---|---|---|
| TD-001 | open | **NetworkPolicy не исполняется в docker-desktop** (встроенный CNI без поддержки политик; проверено живым прогоном T-029, fix `fix/t-029-egress-probe` — канареечный детект в `negative-egress-test.sh`): deny-by-default baseline в локальном контуре информационен, негативные блокирующие проверки деградируют в SKIPPED+warn. | Локальная иллюзия сетевой изоляции: безопасность agent-подов (T030) держится на других слоях (SA без токена/RBAC, non-root, нет privileged, разделение ns). В DC-контуре политики станут обязательными. | T-091 (миграция в shared/DC-контур): кластер с исполняющим CNI обязателен; до того — при переносе на кластер с enforcement прогнать egress-тесты в assert-режиме (`policy_enforcement=on`). |
| TD-002 | open | **Контракт подписи Plane webhook требует прокси** (T-033): `verify_webhook` принимает `X-Plane-Signature` = HMAC-SHA256 по `<timestamp>.<body>` и обязательный `X-Plane-Timestamp` (ADR-013 п.6 — «подпись + timestamp в окне»), тогда как stock Plane подписывает только тело и timestamp не присылает. Живой Plane-инстанс не поднимался — поведение проверено in-memory эмулятором (`tests/contract/plane_api.py`) и юнит-тестами. | Webhook от stock Plane без прокси отклоняется fail-closed; конвейер работает без webhook (он ускоритель, не источник истины — ADR-013 п.2), но событийный ускоритель не включается до появления прокси. | При подъёме Plane в пилоте (T-072): добавить webhook-прокси, подписывающий `<timestamp>.<body>`, либо явный body-only режим `verify_webhook` с оговорённой replay-защитой (окно timestamp + dedup по `X-Plane-Delivery`); тогда же — живой e2e через `DARK_FACTORY_TEST_PLANE_*`. |
| TD-003 | open | **Внешний OTLP-backend не подключён** (T-060): `OtlpTelemetryAdapter` реализует `TelemetryPort` через OTel SDK, но экспорт идёт только в job logs (stdout) и JSON-lines artifact; внешний exporter/endpoint (`opentelemetry-exporter-otlp-proto-http`) не добавлен — ADR-009 п.2 делает внешний backend опциональным в MVP, а вне MVP он и не предполагается. | Нет централизованной ретроспективы трасс: цепочка `change → run → stage → agent → tool → CI job → deployment` видна только в логах и артефактах конкретного job, сквозной поиск по нескольким запускам невозможен. | При появлении backend (T-091, shared/DC-контур): добавить `opentelemetry-exporter-otlp-proto-http` и переменную `DARK_FACTORY_OTLP_ENDPOINT`, подключение — только через `TelemetryConfig`/политику экспорта, без правок ядра. |
| TD-004 | open | **Нет scan/redaction атрибутов перед OTel-экспортом** (T-060): `OtlpTelemetryAdapter` экспортирует имена и атрибуты span'ов без фильтрации, поэтому секрет/PII, переданные вызывающим в атрибут, попадают в job log (дефолт) и в JSON-lines артефакт дословно — проверено адверсариально на независимой проверке T-060. Защита — только соглашение «вызывающий не передаёт секреты» (ADR-009 п.8, зафиксировано в `docs/descriptions/ports.md` и докстринге адаптера); негативные exfiltration-тесты ADR-009 п.8 относятся к границе LLM-эндпоинта (T-010) и telemetry-экспорт не покрывают. | Утечка секретов/PII в CI job logs и в публикуемый артефакт трасс при ошибке вызывающего; отсутствует технический барьер, только дисциплина вызова. | При wiring телеметрии в стадии/Flow (следующая после T-060 задача) или в T-010: добавить слой scan/redaction перед экспортом (deny-list ключей + проверка значений) и негативные exfiltration-тесты в объёме telemetry-адаптера, не полагаясь на дисциплину вызывающего. |
| TD-005 | open | **Протокол `dark-factory-runs` реализован только как P0-минимум** (T-061): публикатор `execution/runs/` раскладывает запись в checkout репозитория, но сам её не коммитит и не пушит (это оставлено CI/CD-контуру), не реализует single-writer / merge-request-free append (от гонки защищает только сравнение содержимого перед `rename`: идентичная запись → no-op, иная → отказ), не исполняет retention/архивацию (классы retention у evidence проставлены, но политика не применяется) и не подписывает manifest (целостность подтверждается только побайтовым равенством snapshot и checksum локально разрешимой evidence). | Запись не появляется в `dark-factory-runs` автоматически — до wiring в CI-джобы публикацию запускает человек или скрипт; при параллельной публикации возможна лишняя работа (безопасная) и неограниченный рост репозитория без архивации; происхождение записи не подтверждено криптографически, поэтому компрометация писателя неотличима от штатной записи. | Погасить по этапам ADR-015 §4: (1) wiring `factory run publish` + коммит/MR в `dark-factory-runs` — вместе с первыми пилотными прогонами (T-072); (2) single-writer/append-протокол — там же, где определяется единственный писатель (финализер/CronJob); (3) retention и архивация — после появления реального объёма записей (T-072/T-073); (4) подпись manifest — вместе с усилением supply chain в DC-контуре (T-091). |
| TD-006 | open | **Неизвестная *стоимость* не консервативна** (T-062): «неизвестно» распознаётся только по `Usage.total_tokens is None` — `reserve` отказывает в такой оценке при заданном лимите, `settle` оставляет такую резервацию открытой (`orchestration/budget/coordinator.py`). Стоимость же при сравнении проецируется `_cost()` как `Decimal("0")`, когда `Usage.cost is None`, поэтому вызов с известными токенами и несообщённой стоимостью недоучитывается против `cost_budget`. | `cost_budget` может быть перекоммичен: `check` и `aggregate.state` остаются `WITHIN_LIMITS`, пока фактическая стоимость уже вышла за предел. Консерватизм FR-018 выполняется по токенам и не выполняется по стоимости. | Сделать правило неизвестности по-измеренчески: отказывать в неизвестной стоимости, пока задан `cost_budget`, и так же оставлять такой settlement открытым; закрыть негативными тестами (неизвестная стоимость не резервируется и не «схлопывается» в 0). |
| TD-007 | open | **Журнал координатора процесс-локален и не персистится** (T-062): `BudgetCoordinator` держит ledger в памяти (`_ledgers`/`_reservations`), поэтому резервации и per-role агрегат не переживают рестарт/CI-джоб. Production-вызывающего нет: `cli/stage.py::execute_stage` вызывает `run_deterministic_stage(context)` без `budget_check`, а `execution/runs/store.py::_serialize` вызывает `build_usage_summary(record)` без `roles`, так что `usage.json` всегда несёт `"roles": []`. | FR-016 между джобами по-прежнему держится только на переносимом `BudgetSnapshot`; совокупный бюджет (`BudgetAggregate`/`usage.json.roles`) в реальных прогонах пуст, координатор в production не задействован. | Частично погашено в T-092: снапшот-путь дюрабелен — миграция 0004 (`migrations/versions/0004_runner_state.py`) добавила `execution.budget`/`execution.implementation_contract`, и durable-драйвер восстанавливает `ChangeRun` со снапшотом (частичное погашение, 2026-09-17). Осталось: персистировать полный журнал резерваций/агрегат координатора вместе с durable state store (PostgreSQL, ADR-004; схема — T-006) и подключить источник политики в stage/CI-путь; точка экспорта — `usage.json.roles`. |
| TD-008 | open | **Отсутствие libpq не проверяется** (ADR-022): после перехода на pure-Python psycopg `libpq` — обязательная runtime-предпосылка, но `factory doctor` по-прежнему проверяет только интерпретатор, дистрибутив и конфигурацию `DATABASE_URL` (`src/dark_factory/cli/doctor.py` L153–161; соединение не открывается), а без libpq импорт psycopg падает `ImportError: no pq wrapper available` — уже после проверок, на первом обращении к state store. | Дрейф окружений (машина разработчика или раннер без libpq) выявляется поздней и невнятной ошибкой вместо диагностики doctor'ом; интеграционные тесты env-gated (`tests/integration/conftest.py` L33–37), поэтому в таком окружении прогон против PostgreSQL подменяется пропуском (в CI URL задан, там гейт работает). | Добавить в `factory doctor` офлайн-проверку доступности libpq/драйвера (импорт `psycopg.pq` без соединения), негативный тест и отражение проверки в контракте `docs/descriptions/cli.md`; погасить вместе с первой правкой doctor после ADR-022. |
| TD-009 | closed | **UIKit в паке `packs/web-app` — заготовка** (T-070): `@small/ui` содержал только tokens (не DTCG) и Button без вариантов/story; Storybook, UI-гейты (UIKit-policy ESLint, stylelint, axe/WCAG 2.2 AA, visual regression) и запрет прямых импортов вне `@small/ui` отсутствовали — это был объём T-071. | Продукты из blueprint стартовали с минимальным UI-слоем; без гейтов был возможен дрейф UI-конвенций между продуктами (несогласованные токены, обход UIKit). | Погашено T-071/T042 (2026-09-16): реальный Small UIKit реализован в `packs/ui` (токены DTCG, 12 компонентов + 5 паттернов, Storybook как исполняемая спека, 4 UI-гейта, контракт детерминизма visual — `packs/ui/rules.md`); blueprint `packs/web-app` 0.2.0 несёт полную копию кита (`frontend/packages/ui`, паритет проверяет `tests/test_packs_ui.py`) с гейтами в продуктовом CI (`frontend-ui-gates`); описание — `docs/descriptions/ui-kit.md`. |
| TD-010 | open | **DoD T-070 «blueprint разворачивается в apps-dev» не проверен живым деплоем** (T-041): чарт валидирован `helm lint` + `helm template` (дефолт и values-apps-dev) и структурными тестами (состав workload'ов, securityContext, квота), но сквозной прогон (сборка образов CI продукта → GitOps MR с digest → Argo CD → smoke) не выполнялся — нет ни реальных образов `example-product-*`, ни подключения пакета к `deploy/argocd/gitops-seed`. | Контракт чарта подтверждён только на уровне рендера: возможны проблемы, проявляющиеся только в живом кластере (образы, секрет, поведение миграционного хука при первом апгрейде, лимиты квоты под реальным трафиком). | T-072/US5 (e2e-пилот): применить пак к пилотному репозиторию, прогнать цикл образ→GitOps→apps-dev→smoke; расхождение рендера и живого деплоя — фиксировать как дефекты чарта, а не пакета. |
| TD-011 | open | **Продуктовый CI пака собирает образы на каждый коммит, но не имеет SAST/SCA-гейтов фабрики** (T-041): в `blueprint/.github/workflows/ci.yml` есть gitleaks и fail-closed trivy, но нет bandit/pip-audit/syft-SBOM и rebuild-check из factory-image.yml — осознанное упрощение «упрощённо, без reusable-механики фабрики». | Продукты из blueprint не получают по умолчанию SAST/SCA и SBOM-evidence уровня фабрики (T-033); уязвимые зависимости ловятся позже (trivy на готовом образе) или не ловятся вовсе. | При первом применении пака (T-072) — либо вынести образы в reusable-workflow по образцу factory-image.yml, либо добавить bandit/pip-audit шаги в blueprint CI; решение зафиксировать в ADR, если меняет контракт trusted-build. |
| TD-012 | open | **Dockerfile'ы пака без reproducible-build контракта фабрики** (T-041): наследуют паттерны `deploy/ci/image/Dockerfile` (pinned digest, non-root, frozen-lockfiles, libpq5), но не повторяют контракт целиком — нет SOURCE_DATE_EPOCH, mtime-нормализации, apt-snapshot пинов, build-constraints и rebuild-check (см. header `blueprint/deploy/Dockerfile.backend`). | Пересборка одного SHA продукта может дать разные digest'ы; OS-пакеты (libpq5, apt upgrade) тянутся с живого зеркала и дрейфуют между сборками. | Включить в blueprint полный контракт при первом использовании (T-072) — скопировать механику из `deploy/ci/image/Dockerfile` и `deploy/ci/scripts/build-image.sh`; до того digest-пиннинг базовых образов обеспечивает неизменяемость платформы, дрейф — только в RUN-слоях. |
| TD-013 | open | **Факты риска наблюдаются частично** (T-080): `risk_facts` (`orchestration/policy/risk.py`) выводит из утверждённого Implementation Contract только `boundaries` (`allowed_boundaries`) и `factory_self_modification` (пути `scope.in_scope` в trusted-слое). Факты `irreversible`, `regulated_data`, `documentation_only` и `decision_class` наблюдать в пайплайне нечем — контракт их не несёт (оценка `classify_decision` в этом месте неприменима: без наблюдаемых сигналов `DecisionFacts()` даёт консервативный NEW_PATH для любого изменения), поэтому они остаются в значении по умолчанию (не повышают класс), а не подставляются как измеренные. | Занижение класса через невыводимые факты не детектируется: `regulated_data`/`irreversible`/`documentation_only` не могут сами поднять класс до R3/R0. Компенсация частичная — declared-класс, повышение агентом и гейт спецификации (`risk_class_lowered_without_policy`, T-021); полосы R2+/R4 из границ и trusted-путей работают. | Расширить наблюдаемость (следующие задачи SDD-слоя и профилей ролей, T-081/T-082): перенести оценку деструктивности дельты (`DeltaOperationKind.RETIRE`/`SUPERSEDE`) и признака ПДн из ChangeSet/contract в `RiskFacts`, а `decision_class` — из классификатора решений; до того — обновлять список явно в докстринге `risk_facts` и здесь. |
| TD-014 | open | **Стадийная граница проверяет решения, поданные вызывающим** (T-080): проверка обязательств R2+ (полоса + человеческие точки контроля) вызывается движком `flow.apply_result` (`_escalation_reason`), а решения человека передаются параметром `human_decisions` — продового раннера, который ведёт run через `apply_result` (ADR-006), пока нет, поэтому без решений R2+ стадии останавливаются fail-closed. Merge-половина wired по-настоящему: `evaluate_merge` вызывает reconciler (`reconcile/rules.py`) с решениями, наблюдёнными из store. | До wiring раннера R2+ run не может продвинуться дальше первой стадии с обязательствами (гейт работает, но удовлетворяется только вызывающим, который подаёт решения); плюс observer реконсилятора собирает `MergeRequestContext.risk_class` сам и может нести declared-класс, а не пересчитанный эффективный. | Вместе с wiring раннера (ADR-006): подавать `human_decisions` из state store (таблица `decision`) на каждом продвижении стадии и считать эффективный класс в сборке merge-фактов реконсилятора; негативный тест — R2+ без решений не продвигается, с решением продвигается. |
| TD-015 | open | **Структурный ответ идёт prompted-режимом, а не output tool** (T-046, найдено живой проверкой harness): Chat Completions эндпоинта DeepSeek отвергает `tool_choice: "required"` при активном thinking mode (`400 Thinking mode does not support this tool_choice`) и не принимает `response_format` типа `json_schema` (`400 This response_format type is unavailable now`). Поэтому `PydanticAIHarness._agent_output_type` оборачивает `output_type` в `PromptedOutput` (JSON в тексте, `json_object`), а не в дефолтный `ToolOutput`; thinking остаётся включённым (дефолт модели). | Схему ответа не валидирует провайдер — JSON приходит текстом и проверяется pydantic уже у нас; при невалидном ответе PydanticAI повторяет запрос (лишний раунд и токены). `NativeOutput` (строгая `json_schema`) на этом эндпоинте недоступен вообще — только через Responses API. Стоимость вызова не измеряется (`cost=None`) — продолжение TD-006: лимит `cost_budget` не сработает. | Если строгая схема станет обязательной — либо профиль `OpenAIModelProfile(openai_supports_tool_choice_required=False)` (проверен на живом эндпоинте, но привязывает адаптер к вендорскому флагу), либо Responses API DeepSeek; прайсинг — вместе с решением по бюджету (T-062). Любое изменение режима проверять живым тестом `tests/integration/test_harness_live_llm.py` (скипается без `DARK_FACTORY_LLM_*`). |
| TD-020 | open | **Живой сквозной прогон поставки Этапа 4 не выполнялся** (T-041, T-043, T-045): скрипты, чарты и CLI Этапа 4 валидированы статически (`bash -n`, `shellcheck`, `helm lint`/`template` + `diff`, `kubeconform`, pytest) и покрыты юнит-тестами, но ни один из них не поднимался в живом кластере: Kubernetes в Docker Desktop был выключен. Не подтверждены DoD T-041 (фактическое создание/удаление agent-пода по `ttlSecondsAfterFinished`, RBAC-матрица `verify.sh` через impersonation и probe-job изнутри пода, минтинг registration token в реальном поде) и DoD T-043 (GitOps-MR меняет immutable digest → Argo CD выкатывает его в `apps-dev` → откат revert'ом коммита): репозиторий `dark-factory-gitops` не создавался (bootstrap — действие человека, команды из `gitops-seed/README.md` не исполнялись), digest пилота остаётся плейсхолдером `sha256:__PILOT_IMAGE_DIGEST__`, секрет `dark-factory-gitops-repo-creds` не заводился, состояние Applications (`factory`, `apps-dev`, `pilot-dev`) никем не наблюдалось. Release-цепочка T-045 не соединялась с живым Argo: наблюдаемое состояние подаётся значениями флагов. | Контракт «merge → образ → GitOps → dev → smoke» подтверждён только на уровне рендера и юнит-тестов; в кластере могут проявиться дефекты, невидимые статически (sync-политика и порядок синхронизации root/app-of-apps, RBAC и impersonation, TTL-уборка Job после завершения, поведение компонентов Argo CD в пределах квоты ns `argocd`, `targetRevision: main` как append-only указатель), а `apps-dev` не содержит ни одного реального деплоя. | Первый живой прогон по процедурам `deploy/argocd/README.md` («Первый живой прогон») и `deploy/ci/README.md` (bootstrap → install → verify) + смена digest реальным GitOps-MR и проверка revert'а; делать это одновременно с T-072/US5 — там же закрывается TD-010 (живой деплой пака в `apps-dev`) тем же прогоном. |
| TD-021 | open | **Capacity smoke не проходит на текущем профиле Docker Desktop** (T-040): `deploy/bootstrap/scripts/capacity-smoke.sh` требует allocatable ≥10GiB (warn на 8–10GiB), а на текущей VM узел отдаёт 7.75GiB — проверка падает по дизайну, а не из-за дефекта скрипта. Профиль ADR-010 п.2 (10–12GB RAM, concurrency=1, повышение только после подтверждённого запаса) не достигнут, evidence «свободный запас RAM/CPU/диска» (idle + один e2e job) отсутствует. | DoD T-040 в части «capacity smoke выполнен с запасом до повышения concurrency» не выполнен: запас локального контура не измерен, concurrency повышать нельзя, поведение полного контура под нагрузкой (пик RAM/CPU/диска) неизвестно. | Решение человека: поднять память Docker Desktop до 10–12GB (ADR-010 п.2), затем прогнать `./deploy/bootstrap/bootstrap.sh` (capacity smoke) и зафиксировать peak RAM/CPU/диска как evidence — до любого повышения concurrency и до переноса контура в T-091. |
| TD-022 | closed | **Нет реального адаптера `ExecutionPort` — агентные стадии неисполнимы в проде** (T-092 S2): срез S2 собрал швы — `AgentStageExecutor`, инструменты роли (`WorkspaceTools`), `Runtime.agent_stage_executor()`, — но изолированный workspace реализован только in-memory фейком `FakeExecution`; реального адаптера (worktree от закреплённой ревизии, запись файлов, запуск команд, сбор evidence) не существует (`docs/descriptions/execution.md` §1). Поэтому `build_runtime` не может собрать исполнитель без внешней инъекции: в проде `Runtime.agent_stage_executor()` возвращает `None`, и `factory run advance` идёт детерминированным путём. | Сквозной участок «реализация → MR» на живом репозитории неисполним: S2 покрыт только фейками, и `write_file`, только что добавленный в контракт `ExecutionPort`, не проверен на живом worktree. | Погашено (2026-09-17, инкремент T-092/T057): реальный адаптер `WorktreeExecution` в слое `execution/workspace.py` — git-worktree от `WorkspaceRequest.revision`, изготовленный из локального зеркала оператора (`DARK_FACTORY_WORKSPACE_MIRROR_ROOT`, `<provider>/<slug>`; `git fetch` + `worktree add --detach`; клон с провайдера и credentials — non-goal MVP до T-091), `write_file` с path safety (включая symlink-escape), `run_command` без shell с таймаутом (`DARK_FACTORY_WORKSPACE_COMMAND_TIMEOUT`, дефолт 600 с) и лимитом вывода 1 МиБ на поток, `collect_evidence`, `collect_changes` — дельта относительно закреплённой ревизии (свежий workspace пуст, удаления пропускаются — MVP-ограничение единицы переноса). Идемпотентность replay без состояния: id/каталог workspace детерминированы из `idempotency_key` (слаг + префикс sha256), replay переиспользует валидный worktree и пересоздаёт сломанный — сходимость к закреплённой ревизии. Ошибки git — value-free `WorkspaceError` (ADR-009); конфигурация — `WorktreeExecutionConfig.from_env` по образцу `GitHubConfig`: незаданные переменные → отсутствие адаптера, заданные криво → fail-closed `ValueError`. Подключён в `build_runtime` (явная инъекция приоритетна). Синхронизированы: контрактная сюита парамeтризована по биндингам fake/worktree (`tests/contract/`), юнит-тесты `tests/test_execution_workspace.py`, wiring-тесты `tests/test_runtime_composition.py`, `specs/.../contracts/ports.md`, описания (`docs/descriptions/execution.md` §1.1, `runtime.md`, `cli.md`). Валидация: ruff + format + mypy strict (288 файлов) чисто; pytest без БД 1633 passed + 64 skipped; с PostgreSQL 1692 passed + 5 skipped. Живой прогон агентной стадии на пилотном репозитории — T-072. |
| TD-023 | closed | **Собранный исполнитель не подключён к консольному entry point** (T-092 S2): `Runtime` умеет собрать агентный `StageExecutor` и SCM-derived ревизию, `advance_run`/`run_advance_command` получили необязательные швы `executor`/`revision_of`, но `[project.scripts] factory = dark_factory.cli.main:main` — core-модуль, которому ADR-024 п.5 запрещает импортировать `runtime`. Значит, без внешней инъекции `factory run advance` всегда использует детерминированный исполнитель, а собранное связывание в проде не востребовано. | Срез S2 не даёт end-to-end ценности: композиция проверена тестами, а не рабочим вызовом; «первый потребитель runtime» остаётся тестовым. | Погашено ADR-025 (2026-09-17): `[project.scripts] factory` ре-поинтится на тонкий entry point `dark_factory.runtime.entrypoint:main` в composition-слое, который для `factory run advance` лениво собирает `Runtime` и передаёт собранные `executor`/`revision_of` в `cli.main`, а для остальных команд (`doctor`, `stage run`, `run status`/`publish`, `reconcile`, `outbox …`, `api serve`, `release verify`, заглушки) вызова без сборки сохраняет прежнее поведение; собранный `Runtime` закрывается в `finally`, `python -m dark_factory.cli` остаётся явным core-путём без композиции. |
| TD-024 | closed | **Шаг commit/push в агентной стадии не реализован — change request открывается без изменений** (T-092 S2): `AgentStageExecutor._publish` (`orchestration/stages/agent.py` L242–281) публикует только ветку (`RepositoryPort.ensure_branch`) и change request (`MergeRequestPort.find_existing`/`open`), а commit/push не выражает ни один контракт: `RepositoryPort` состоит ровно из `get_revision` и `ensure_branch` (`src/dark_factory/ports/protocols.py` L41–53), `ExecutionPort` заканчивается на `prepare_workspace`/`write_file`/`run_command`/`collect_evidence` (L165–187); ключи эффектов стадии — только `workspace`/`branch`/`change_request`. ADR-024 п.7 до этой правки подавал commit как часть S2. | Правки агента в изолированном workspace не публикуются в ветку: `OpenChangeRequest.head_sha` берётся из `ensure_branch`, а голова существующей ветки возвращается как есть (`adapters/scm/github/repository.py` L42–45; при первом создании `head = from_revision` = `context.input_revision`), поэтому `head_sha` change request — голова ветки, а не коммит стадии, и ни один путь не несёт правок агента. Сквозной путь «агентская стадия → MR с изменениями» не замкнут, и оба зависящих от него потребителя не получают реального SHA: гейты S3 (итоговый SHA через `CIPort`/`PipelinePort`) и e2e-пилот T-072. | Погашено (2026-09-17, инкремент T-092/T057): `RepositoryPort` расширен аддитивным `publish_commit` (коммит+push — один внешний эффект; единица переноса — файловое множество `path → bytes`, `WorkspaceHandle` в сигнатуре нет — значения собираются через новый read-side `ExecutionPort.collect_changes`), ключ эффекта `commit` рядом с `workspace`/`branch`/`change_request`, replay-dedup lookup-first по маркеру в commit message (переживает холодный адаптер), «нет изменений» — решение стадии: `blocked` до создания ветки/коммита/CR, пустой `changes` в порту — `ValueError`; `head_sha` CR и `revision` артефакта — SHA коммита публикации. Синхронизированы фейк, GitHub-адаптер, эмулятор, контрактная сюита, сюита агентной стадии и `specs/.../contracts/ports.md`; решения и trade-offs — ADR-024, «Дальше» (прогресс-заметка 2026-09-17). Ограничения: удаления файлов и двоичный контент в единице переноса MVP не представимы. |
