/**
 * Public surface of the Small UIKit stub (ADR-014).
 * T-071 grows this into the real UIKit (Radix + adapted shadcn, Storybook).
 */
export { Button, type ButtonProps } from "./Button";
export { tokens, type Tokens } from "./tokens";
